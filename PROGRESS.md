# 进度记录

任务背景与目标：GraphicsAPILearn 图形学学习项目。当前主线为 RHI 架构重构（55 任务计划，通过 subagent-driven-development 执行），将 ~50 个 GLXxxApp 迁移到 API 无关，同时按用户要求扩展命令行功能。

| 时间 | 日志 | 任务类型 | 内容 | 备注 |
| ---- | ---- | -------- | ---- | ---- |
| 2026-08-09 | RHI 计划已写入并在 develop 分支通过 SDD 执行 | 计划 | docs/superpowers/plans/2026-08-08-rhi-architecture.md（55 任务） | 用户选择直接改 develop，不使用 worktree |
| 2026-08-09 | 建立可构建 Linux 基线 | 修复 | 79ac9e4：vcpkg 工具链、全部 include 小写化、CMake 路径大小写修正 | 基线编译+运行通过 |
| 2026-08-09 | Task 1-7 rhi/core + rhi/gl 完成 | 实现 | 11 个接口头 + GL 后端 + GLFWSurface | 全部评审 Approved，Task 3 用 1 轮修复 |
| 2026-08-09 | Task 8 GLApp 走 IRenderer | 重构 | 4d685f8：GLApp 持 IRenderer；GL 版本日志移入 GLBackend::init 满足 GL 仅限 rhi/gl 约束 | 用户自行编译验证通过 |
| 2026-08-09 | CLI 参数解析 | 功能 | 0c2520a：cxxopts --backend/--app/-h；修复 EnumUtil GCC 魔数偏移 bug；AppType 加 Count 哨兵 | 仅列实际可用后端（目前仅 gl） |
| 2026-08-09 | 构建脚本 | 脚本 | scripts/build_run.sh：--parallel 32 + 修复二进制路径 build/src/renderLearn | 用户要求固定 32 线程 |
| 2026-08-09 | 运行脚本 | 脚本 | scripts/run.sh：仅运行二进制并透传全部参数（-h/-a/-b） | 参数原样 exec 透传 |
| 2026-08-09 | 退出崩溃修复 | 修复 | bb288ed：项目类 ImGuiWindow 与 imgui 内部 ImGuiWindow 同名，析构时 unique_ptr 解析到 imgui 强符号析构 → ODR 冲突崩溃；重命名为 IImGuiWindow | 用 cuda-gdb 抓到 double-destroy 栈 + nm 验证符号冲突；ESC 正常退出验证通过 |
| 2026-08-09 | SimpleTexture 崩溃修复 | 修复 | 205dc21：shader 文件名 simpleTexture.*（小写）与代码引用 SimpleTexture.*（大写）不匹配，Linux 大小写敏感导致读文件为空 → 断言崩溃；重命名为大写 | 大小写不敏感平台（Win/macOS）不暴露；已全量扫描确认无其他 shader 引用缺失 |
| 2026-08-09 | Ambination 崩溃修复 | 修复 | b79f5fa：Object.frag 声明 `in vec3 normal` 但 Object.vert 输出 `out vec4 normal`，类型不一致致链接失败；该声明未使用，直接删除 | 已全量扫描所有 vert/frag 对接口类型一致性 = 0 冲突 |
| 2026-08-09 | 全量 shader 大小写统一 | 修复 | 24fdc46：系统性问题——多处磁盘 shader 目录/文件名为小写（diffuse/material/specular、direction/point/spot、cube、Skybox），代码引用首字母大写，Linux 大小写敏感致读空文件断言；统一为代码引用的大小写 | 写脚本展开全部 join 链（含 PBR/Bloom 等多级 shaderDir、动态 name），170 处引用全匹配；验证 Diffuse/Material/Specular/LightSource×3/Skybox/Cube/Triangle/Rect/SimpleTexture 全部运行 OK |
| 2026-08-09 | Explode shader 修复 + run.sh all 模式 | 修复/脚本 | 59eda53：①Explode.gs 声明 `in VS_OUT{vec2 texCoords}` 但顶点数据无 texCoord、vs 未输出该块 → 链接失败；改为 VS_OUT 传 vec4 color（对齐 Base.vs/gs 模式）②run.sh 新增 `all [backend] [duration]` 子命令，逐个运行某后端全部 example 并设超时（默认 gl/5s），统计 OK/FAIL | 新脚本实测 46/46 example 全 OK；非法 duration 有校验；已解决 4 类 shader 隐患（大小写/缺失/接口类型/几何着色器块） |
| 2026-08-09 | run.sh all 打印当前 sample 名 + 参数改选项式 | 脚本 | ①运行每个 example 前打印 `==> [(total)] 运行 sample: $app` ②`all` 参数从位置式改为选项式 `-b/--backend`、`-d/--duration`（默认 gl/5s、顺序无关、未知选项报错），用法注释同步更新 | 按用户要求改完不做运行验证，仅 bash -n 语法检查通过；普通透传模式不受影响 |
| 2026-08-09 | 两个渲染问题修复 | 修复 | ①PBR_IBL_Specular 画面只占窗口左下角：renderBeforeLoop 中 renderPerfilterMap/renderBrdfLUT 末尾未把 viewport 恢复为窗口尺寸（停在 512x512），主循环沿用小 viewport 导致渲染区域偏左下；已在两函数末尾补 `glViewport(0,0,窗口宽高)` ②Cube 能看到内部：GLCubeApp 未 `glEnable(GL_DEPTH_TEST)`，背面覆盖正面；已在 initApp 补开 | 编译通过；Cube 与 PBR_IBL_Specular 单独运行 OK；`run.sh all -b gl -d 1` 全量 46/46 OK |
| 2026-08-09 | run.sh all 增加 -a/--app 过滤 | 脚本 | `all` 模式新增 `-a/--app` 可重复指定，过滤只运行匹配的 example；无匹配项报错退出 1；用法注释更新 | 验证：`all -a Cube`、`all -a Cube -a Triangle` 过滤正确；不匹配/未知选项均报错；`all` 无 `-a` 时行为不变 |
| 2026-08-09 | Camera 深度测试修复 | 修复 | GLCameraApp 与 GLCubeApp 同模式问题：initApp 未 `glEnable(GL_DEPTH_TEST)`，画 3D cube 背面覆盖正面看到内部；已在 GLCameraApp::initApp 补开 | 编译通过；`all -a Camera -b gl -d 3` 运行 OK；已核对 GLRect/Triangle/SimpleTexture 为 2D 无需深度测试，其余 3D App 均各自已开 GL_DEPTH_TEST |
| 2026-08-09 | Light 系列深度测试修复 | 修复 | SimpleLight 及 LightSource 共 9 个示例 initApp 均缺 `glEnable(GL_DEPTH_TEST)`，cube 看到内部；逐个补开。覆盖：SimpleLightMap/Diffuse/Specular/Ambination/Material + LightSource Direction/Point/Spot/Mult | 编译通过；用户报告 5 个示例 `all -a ... -b gl -d 2` 全部 OK；同结构其余 4 个一并修复防复发 |
| 2026-08-09 | MSAA 渲染异常修复 | 修复 | 画面春绿色共 4 处根因：①`GLMsaaApp::initApp` 误调 `GLApp::initApp` 跳过相机初始化，相机在原点 cube 内部全绿 ②`drawGLMssa` 缺 `GL_DEPTH_TEST` ③窗口未设 `GLFW_SAMPLES`，默认 framebuffer 无 MSAA sample buffers 致 `glEnable(GL_MULTISAMPLE)` 无效——新增 `GLFWWindowProperties::samples`+`IApplication::getSampleCount()`，Msaa 覆写返回 4 ④`createFrameBuffer` 先解绑再查 FBO 完整性顺序颠倒 | 编译通过；`all -a Msaa -b gl -d 3` 运行 OK；其余 App 未请求 samples 不受影响（默认 0） |
| 2026-08-09 | Vulkan 后端工程启动（四阶段） | 计划 | 用户拍板：先完成 RHI 迁移（46 个 GLXxxApp 全部迁到 RHI 抽象）再写 Vulkan 后端；四阶段=A) RHI 接口层扩展 B) 46 App 迁移 C) Vulkan 后端 D) SPIR-V 编译+验证。设计文档 docs/superpowers/specs/2026-08-09-vulkan-backend-design.md（84eccd4） | 关键决策：命令式状态+Vulkan dynamic state、统一显式 UBO、去 GL 前缀命名、vcpkg vulkan.hpp+构建期 glslangValidator |
| 2026-08-09 | 子项目 A 实施计划已提交 | 计划 | docs/superpowers/plans/2026-08-09-rhi-interface-expansion.md（f4e15b0），7 任务：Common 类型→IBuffer/GLBuffer(UBO)→ITexture2D/3D→IPipeline 状态全集→IRenderTarget→IRenderer/GLBackend→GLShader Compute 预留 | 自审修正 3 处：createEmpty 签名定稿 (desc,width,height)、Task4 文件清单补 GLShader、清掉错误示例代码 |
| 2026-08-09 | 子项目 A 实施完成（RHI 接口层扩展） | 实现 | 7 任务全提交：1c16fa6(Common 纹理/FBO/状态类型) → 85c91dd(IBuffer/GLBuffer UBO update/bindRange) → 2860f24(ITexture2D/3D desc/createEmpty/cubemap) → 03c5122(IPipeline 状态全集+bindUniformBlock+GLShader block 反射) → cb7aa05(IRenderTarget FramebufferDesc/attachCubeFace/深度纹理/resolve+GLTexture2D adopt) → 6d0497b(IRenderer instancing/RT绑定/UBO工厂/blit/capabilities) → 407b102(GLShader Compute 预留) | 每任务编译+`run.sh all -b gl -d 1` 46/46 OK；GLTexture3D 增加 _target 跟踪（cube vs 3D），createEmpty 语义=6 面 cubemap；ITexture3D.hpp 需 include ITexture2D.hpp 获 TextureDataView2D |
| 2026-08-09 | 子项目 B 前置缺口识别（并行 3 agent 调研） | 调研 | ①RHI GL 后端无顶点输入装配：GLPipeline::bindShader 只建空 VAO、不配 glVertexAttribPointer；GLRenderer::setVertexBuffer 仅 bind buffer ②model/Mesh/geometry 仍 include glad+旧 GLProgram，GLProgram 是与 rhi::GLShader 并存的独立旧类 ③46 App 分类：基础18/中等5/复杂16/PBR5/特殊1；全部用 GLProgram::update 便捷 uniform，仅 GLUniformBufferApp 用 UBO | 关键结论：迁移 46 App 前必须先补 GL 后端顶点装配能力 |
| 2026-08-09 | 子项目 B 前置补全计划已写 | 计划 | docs/superpowers/plans/2026-08-09-rhi-gl-prerequisites.md，4 任务：①GLPipeline 顶点输入装配(IPipeline 加 setVertexBuffer/setIndexBuffer+use 绑 VAO) ②RhiGeometry/RhiImage 资源辅助 ③迁移 5 基础 App(Triangle/Rect/SimpleTexture/Cube/Camera)验证模板 ④Model/Mesh 去 GL | 用户确认"先写前置补全计划再迁移"；迁移期不重命名 GLApp（留到全量迁移末尾统一做）；红线=全量 46/46 OK |
| 2026-08-09 | 子项目 B 前置补全实施完成（SDD） | 实现 | 4 提交：d2d0211(GLPipeline 顶点装配+GLRenderer 委派) → c6ed2fc(RhiGeometry/RhiImage 桥接) → 641bf42(迁移 Triangle/Rect/SimpleTexture/Cube/Camera 为 RHI 模板) → 0bb6706(Model/Mesh 去 GL+GLLoadModel 迁移) | 每任务独立评审全部 Approved，最终全分支评审"可合并"；全程 46/46 OK。Task2 修正 brief 4 处 API 错误(Shape/Image 全局类、TextureDataView2D)；Task4 两处超 brief 改动经核实必要正确(Saturn raw VAO 自绘、SSAO 双管线) |
| 2026-08-09 | 子项目 B 前置技术债（全量迁移须消化） | 记录 | I-1 GLPipeline Int4 用 glVertexAttribPointer 非整数指针(引骨骼模型前须修)；I-2 Model include app/GL/RhiImage.hpp 分层倒挂(桥接应移中立层)；Saturn raw VAO 资源未释放；rock 顶点双上传；SSAO GBuffer 双管线；GLCameraApp 仍继承含旧依赖的 GLCameraBaseApp | 详见 .superpowers/sdd/2026-08-09-rhi-gl-prerequisites/progress.md；下一步=写 46 App 全量迁移计划 |
| 2026-08-09 | Task 1 GL 后端顶点输入装配 | 实现 | d2d0211：IPipeline 加 setVertexBuffer(buffer,binding)/setIndexBuffer 纯虚；GLPipeline::use 绑 VAO+实现装配(glVertexAttribPointer/Divisor)；GLRenderer 持 _pipeline 委派三个缓冲方法 | 编译 OK+全量 46/46 OK；补充 IBuffer.hpp include 与前置声明（brief 遗漏） |
| 2026-08-09 | Task 2 RhiGeometry/RhiImage 资源辅助 | 实现 | c6ed2fc：新增 src/app/GL/RhiGeometry.{hpp,cpp}(Shape→3 VBO+可选 IBO+VertexLayout) 与 RhiImage.{hpp,cpp}(Image→ITexture2D/initCube 六面 cubemap)；GL glob 自动收录 | build OK；披露 brief 冲突：Shape/Image 为全局类且访问器非 const（Create 用 Shape&）、load() 返回 Image& 非 bool、数据视图实为 rhi::TextureDataView2D；需额外 include ITexture2D/3D.hpp |
| 2026-08-09 | Task 3 迁移 5 基础 App 为 RHI 模板 | 重构 | 641bf42：Triangle/Rect/SimpleTexture/Cube/Camera 从 GLProgram/GLCube/GLImageTexture2D/glad 迁至 RHI（createShader/compile→createPipeline→setPipeline→setVertexBuffer(binding)→setIndexBuffer→bindTexture→draw/drawIndexed；Cube/Camera 用 setDepthTest+setUniform(mat,count)）；新增 src/app/App.hpp 别名基类 | 编译 OK；5 App 各自运行 OK；全量 46/46 OK。GLCameraApp 保留继承 GLCameraBaseApp（相机/输入逻辑所在，不在修改清单）；include 用 app/GL/ 前缀；矩阵 uniform 用 3 参重载 |

| 2026-08-09 | Task 4 Model/Mesh 去 GL | 重构 | refactor(model): decouple Model/Mesh from glad and legacy GLProgram, use RHI | Mesh._vao/_vbo/_ebo→shared_ptr<IBuffer>+VertexLayout(7属性), Texture.id→shared_ptr<ITexture2D>, Model(renderer,path)用RhiImage::Load2D; GLLoadModel→RHI shader/pipeline; Saturn planet走RHI+rock重建raw VAO保instancing, SSAO新增RHI GBuffer pipeline | build OK; LoadModel/Saturn/SSAO各运行OK; 全量46/46 OK; 披露: Saturn读Mesh::_vao超brief假设, rock用raw GL VAO保instancing |
| 2026-08-09 | 前置补全分支最终全分支评审(c545c23..0bb6706) | 评审 | 结论=可合并Approve。4 commit/29文件自洽，跨任务一致性强（RhiGeometry layout 与 5 shader location 全对齐）；绘制时序正确（clear先于draw）。无Critical；Important×4=Int4整数指针(I-1,任何骨骼模型前必修)+Model include app/GL分层倒挂(I-2)+Saturn raw VAO泄漏(I-3,与原实现相当非回归)+GLCameraApp仍继承GLCameraBaseApp(I-4)；Minor×5。技术债全部可延后无阻塞合并。红线复核：编译+46/46 OK+无GLApp重命名+旧签名未改 | 披露评审盲区: run.sh OK仅=exit0+无[error]日志, 非视觉金样比对; 建议全量迁移计划P0=Int4整数指针, P1=RhiImage上移中立层+Saturn VAO, 并升级run.sh OK判定 |
| 2026-08-09 | 46 App 全量迁移前期调研（13 Advanced App） | 调研 | 逐 .hpp/.cpp 读 Advanced 13 个 App（GLSL/Blend/CullFace/DepthTest/FrameBuffer/Msaa/Skybox/TemplateTest/UniformBuffer/Explode/SimpleGemotery/MultieInstance/Saturn）+ rhi/core 全接口 + GL 后端实现 | 结论见下一条；最大阻碍=draw 硬编码 GL_TRIANGLES，GL_POINTS(SimpleGemotery) 受阻；compute 未实现；Saturn rock 残留 GLProgram+raw VAO |
| 2026-08-09 | 子项目 B 主体调研（3 并行 explore agent 盘点 ~33 待迁 App + RHI 缺口） | 调研 | 全 App 依赖 GLProgram::update；除 6 已迁外剩约 33 App。已迁移 App 只用 GLApp 渲染循环钩子(GLApp 本身已 RHI 化，无需动)；GLCameraBaseApp 仍含 native GL include 须先清(公共基类)。RHI 缺口：primitive type 未接入 draw(全屏 quad 用 TRIANGLE_STRIP→Bloom/Defer/Hdr/SSAO/IBLSpecular 受阻)、mat3 uniform 缺 glUniformMatrix3fv(PBR normalMatrix)、RGBA32F 缺(SSAO noise)、HDR 加载缺(RhiImage 无 stbi_loadf/RGB16F→3 个 IBL App)、attachCubeFace 无 mip level(IBLSpecular prefilter)、深度 cubemap RT 不支持(PointLightShadow)、仅清深度缺(阴影 App)、Int4 整数指针待修 | GLApp 重命名影响面=约 4 文件 6-10 行(只改基类)或 46 类数百行(去全部前缀)；mat3 缺口注意 setUniform(ptr,count,vecSize==3) 现走 glUniform3fv 非 Matrix3fv |
| 2026-08-09 | 子项目 B 主体 计划A 已写 | 计划 | docs/superpowers/plans/2026-08-09-rhi-migration-phase-a-rhi-capabilities.md，5 任务：①PrimitiveType 接入 draw+Points ②mat3 矩阵 uniform+RGBA32F ③RhiImage HDR(RGB16F) ④Int4 整数指针修复 ⑤GLCameraBaseApp RHI 化继承 App | 用户选择"拆分多子计划，从 RHI 补全+基类开始"；深度 cubemap RT、attachCubeFace mip、仅清深度推迟到对应 Light/PBR 批次随 App 引入；每任务 build+46/46+commit |
| 2026-08-09 | 子项目 B 计划A Task 1 图元接入绘制路径 | 实现 | 55fca98：PrimitiveType 增补 Points；IPipeline 新增 setPrimitiveType/primitiveType 纯虚（只加不改签名）；GLPipeline 持 _primitive 默认 TriangleList；GLBackend 加 ToGLPrimitive 静态映射并改 draw/drawIndexed/drawIndexedInstanced/drawInstanced 按 pipeline 拓扑发图元（未 set 默认 TriangleList） | 编译 OK+全量 46/46 OK；brief 与真实源码一致无出入；GL_POINTS(SimpleGemotery) 阻塞解除；未迁 App 行为不变 |
| 2026-08-09 | 计划A Task 1 代码评审（0bb6706..55fca98） | 评审 | 结论=Approved。真实源码与 diff 逐行一致；GLPipeline 是 IPipeline 唯一实现（新增纯虚安全）；ToGLPrimitive 覆盖全部 4 枚举 default=TriangleList 正确；4 个 draw 均按 _pipeline->primitiveType() 发图元、未 set 走默认 TriangleList 与旧行为一致；仅改 5 文件未越界；Grep 确认无其他 IPipeline 子类。无 Critical/Important；Minor=ToGLPrimitive 依赖 default 兜底 TriangleList（隐含未来新增枚举值被静默归入 TRIANGLES）建议改显式 case | 红线复核：只增不改签名+GLPipeline 唯一实现+46/46 OK；盲区同前（run.sh OK 非视觉金样） |
| 2026-08-09 | 计划A Task 2 mat3 矩阵 uniform + RGBA32F 纹理格式 | 实现 | 0533a89：IPipeline 新增 setUniformMatrix 纯虚；GLPipeline.cpp 实现（matSize==3/2→Matrix3fv/2fv、else→Matrix4fv）；TextureFormat 增补 RGBA32F→GL_RGBA32F+IsFloatFormat；GLHeader 映射 | build OK+全量 46/46 OK；披露 brief 遗漏：GLPipeline.hpp 需补 setUniformMatrix override 声明（否则抽象类无法编译），+1 文件属必要修正 |
| 2026-08-09 | 计划A Task 3 RhiImage HDR(RGB16F) 加载 | 实现 | 296dba6：RhiImage 新增 Load2DHDR，Image(file,TextureOption{true})→stbi_loadf 解码 float，ch==4 选 RGBA16F 否则 RGB16F，TextureDataView2D{data,w,h,ch} init；只改 hpp/cpp 2 文件 | build OK；未改 Load2D/LoadCube 签名；与真实源码核对一致无越界 |
| 2026-08-09 | 计划A Task 3 代码评审（0533a89..296dba6） | 评审 | 结论=Approved。diff 与源码逐行一致；头/源签名一致；全链路核实 RGB16F 正确：ToGLInternalFormat→GL_RGB16F、ToGLFormat(RGB16F,3)→GL_RGB、ToGLSrcType→GL_FLOAT；RGBA16F 同理；TextureDataView2D 字段序与 ITexture2D.hpp 一致；Image 内部 stbi_loadf 返回 float* 转 uint8_t*，const void* 适配。无 Critical/Important；Minor=ch 非3/4 时按 RGB16F+GL_RGB 上传会越界读（.hdr 恒3通道不受影响，防御性建议） | 红线复核：只改2文件+未破坏 Load2D/LoadCube+build OK；blind=run.sh OK 非视觉金样、HDR 无运行验证 |
| 2026-08-09 | 计划A Task 4 Int4 顶点整数指针修复 | 实现 | ddd65c9：GLPipeline::setVertexBuffer 对 VertexElement::Int4 改用 glVertexAttribIPointer(4,GL_INT)，其余格式仍走 glVertexAttribPointer；glEnableVertexAttribArray/Divisor 保持在 if/else 外 | build OK+全量 46/46 OK；只改 GLPipeline.cpp 1 文件未越界；不改接口签名；当前无 App 用 Int4 行为不变 |
| 2026-08-09 | 计划A Task 5 GLCameraBaseApp RHI 化 | 实现 | 876c2c8：基类 GLApp→App；删除 GLProgram.hpp/glad/GLImageTexture2D.hpp include（.cpp 无实际 GL 调用，逐行核对）。**保留** `class GLImageTexture2D;` 前置声明 | build OK+全量 46/46 OK。必要偏差：4 个 PBR App(GLPBRTexture/GLIBLIrradiance(+Conversion)/GLIBLSpecular)自身用 shared_ptr<GLImageTexture2D> 但未 include/前置声明，依赖该头传递引用，删前置声明即编译失败；受"只改2文件不越界"限制故保留前置声明，作为最小改动。遗留：PBR 迁移批次内应给 4 个 PBR 头补 include，届时方可删该前置声明 |
| 2026-08-09 | 子项目 B 主体 计划A 实施完成（SDD） | 实现 | 6 提交：55fca98(PrimitiveType 接入 draw+Points) → 0533a89(mat3 矩阵 uniform+RGBA32F) → 296dba6(RhiImage Load2DHDR) → fe16848(plan 文档) → ddd65c9(Int4 glVertexAttribIPointer) → 876c2c8(GLCameraBaseApp RHI 化继承 App) | 每任务评审 Approved，终审"可合入"；全程 46/46 OK。终审 2 Important/4 Minor 全可延后 |
| 2026-08-09 | 计划A 技术债（后续批次消化） | 记录 | P0: 4 个 PBR 头缺 GLImageTexture2D 自身声明(依赖 GLCameraBaseApp 传递前置声明，PBR 迁移时改 ITexture2D 并删该声明)；SimpleGemotery 迁移应作为 Points 首验。P1: ToGLPrimitive 改显式 case+default 日志(Vulkan 前锁死)。P2: setUniformMatrix else、HDR 通道防御、GLCameraBaseApp.cpp 重复 include。推迟项:深度 cubemap RT、attachCubeFace mip、仅清深度随对应 App 批次引入 | 详见 .superpowers/sdd/2026-08-09-rhi-migration-phase-a-rhi-capabilities/progress.md；下一步=写计划B(简单 forward App 分批迁移) |
| 2026-08-09 | 计划B Task 1 RhiGeometry 自定义 location | 实现 | 914f5df：RhiGeometry 新增 Layout{uvLocation=2;normalLocation=3}，Create 追加可选第 5 参 const Layout&={}；uv/normal 元素 semantic 改用 layout 字段，binding 槽不变 | build OK+全量 46/46 OK；默认布局与之前逐字节一致，SimpleLight 后续传 {.uvLocation=3,.normalLocation=2} |
| 2026-08-09 | 子项目 B 计划B 已写 | 计划 | 设计 spec(2026-08-09-rhi-migration-phase-b-simplelight-design.md,d2543d3)+计划(2026-08-09-rhi-migration-phase-b-simplelight.md)：迁移 SimpleLight 9 App(Ambination/Diffuse/Specular/Material/Map+LightSource×4)到 RHI。4 任务=T1 RhiGeometry 自定义 location(默认 uv=2/normal=3 不变,SimpleLight 用 uv=3/normal=2 因 shader 布局 normal=2/uv=3)→T2 无纹理 4→T3 Map(双纹理)→T4 LightSource 4(Cube+双纹理+多实例) | 用户拍板:改 RhiGeometry 支持自定义 location(不动 shader);4 任务拆分。每任务 build+46/46+commit |
| 2026-08-09 | 计划B Task 1 代码评审 | 评审 | 评审 560b07d..914f5df：Spec ✅ 通过。Layout 结构与可选参数与 brief 一致；仅改 uv/normal semantic；默认值值初始化=原常量逐字节等价；只改 2 文件；字段序正确 | Approved 可进 T2。仅 Minor：location 无冲突校验(可延后)；report 措辞"仅新增可选参数"略不准 |
| 2026-08-10 | 计划B Task 2 SimpleLight 4 App 迁 RHI | 重构 | c22be6c：Ambination/Diffuse/Specular/Material 从 GLProgram/glad/raw VAO 迁 RHI（光源管线 RhiGeometry 生成 pos+color layout + 物体管线 normalLocation=2，setVertexBuffer/setIndexBuffer/drawIndexed） | build OK+全量46/46 OK+4 App各自OK；brief vec4 写法修正：value_ptr(&v)编译失败改 value_ptr(v) |
| 2026-08-10 | Task 2 评审修复 Material 光源块 | 修复 | 50d98da：Material drawScene 补回被删除的光源绘制块（_lightPipeline 缩小光源球，对照 Diffuse 写法，lightPos 用本 App 旋转逻辑）；_lightPipeline initApp 已创建无需补 | build OK+全量46/46 OK+Material单独OK |
| 2026-08-10 | 计划B Task 3 SimpleLightMap 迁 RHI（双纹理） | 重构 | 598502d：Cube 物体管线 RhiGeometry(useUv=true,uv=3/normal=2)+RhiImage::Load2D 载 container2 双纹理(_diffuseTex unit0/_specularTex unit1)，光源块保留(独立管线+缩小球+旋转 lightPos)，完整 uniform 集(projection/view/model/lightColor/objectColor/light.position/viewPos/material.shininess(1)/material.diffuse/specular/light.ambient/diffuse/specular) | build OK+全量46/46 OK+SimpleLight_Map单独OK；shininess 原实现传 int(1)非 brief 示例32，按原实现 int 重载 |
| 2026-08-10 | 计划B Task 4 LightSource 4 个迁 RHI | 重构 | c2c0a9a：Direction/Point/Spot/Mult 从 GLProgram/glad/raw VAO 迁 RHI（光源管线 pos+color + 物体管线 uv=3/normal=2 双纹理 + RhiImage::Load2D 双纹理 + 多实例循环每实例 model uniform）。Mult 差异：vec3 uniform+pointLights[N] 数组逐元素 setUniform(vecSize=3)，static helper 改 rhi::IPipeline&，initProgram 按 name 分建管线 | build OK+全量46/46 OK+4 App 单独 OK；brief `-d` 仅 all 模式有，改 all+过滤验证 |
| 2026-08-10 | 计划B Task 4 代码评审（598502d..c2c0a9a） | 评审 | 结论=Approved。4 App 光源管线 pos+color + 物体管线 uv=3/normal=2 双纹理 + 多实例每实例 model uniform 全部正确；Mult vec3 全套 setUniform(ptr,1,3)→glUniform3fv 无一处误用；uniform 全集逐 App 对照原.cpp 零遗漏；光源块非死代码。无 Critical/Important；Minor×3（Mult Light 分支未写 _vb 靠时序耦合、光源管线未 setDepthTest 与全局 glEnable 细微差异、int/size_t 混比系原实现保留） | 红线复核：build+46/46 OK（implementer 已跑）+8 文件+类名未改；盲区=run.sh OK 非视觉金样 |
| 2026-08-10 | 计划B 全分支最终评审（560b07d..c2c0a9a） | 评审 | 结论=可合入 Approve。5 commit/20 文件自洽；跨任务一致性强：T1 Layout 参数与 T2-T4 全 9 App 使用一致（无纹理 normal=2、有纹理 uv=3/normal=2），逐 shader location 全对齐（Object.vert 均 normal=2/uv=3，Light.vert 仅 pos/color 0/1）；RhiGeometry 默认 Layout{uv=2,normal=3} 与旧硬编码逐字节等价，Cube/Camera/LoadModel 不受影响；默认参=同一函数非重载，无 ABI/歧义风险。无 Critical。Important×2 全可延后（Mult initProgram _vb 时序耦合、material.shininess int 重载作用于 float uniform 的跨后端隐患）；Minor×4 可延后 | 红线复核：不重命名+只改指定 20 文件+46/46 OK；9 App 已 100% 脱离 native GL（GLProgram/glad/GLImageTexture2D 零残留）；Advanced/ 13 App 仍 native GL 属后续批次非遗漏 |
| 2026-08-09 | 子项目 B 计划B 实施完成（SDD） | 实现 | 5 提交：914f5df(RhiGeometry::Create 支持自定义 uv/normal location,默认不变)→c22be6c(SimpleLight 无纹理4迁移)→50d98da(修 Material 光源块回归)→598502d(SimpleLightMap 双纹理)→c2c0a9a(LightSource 4:Cube+双纹理/单纹理+多实例) | 每任务评审 Approved；T2 1 轮修复(Critical:Material 光源块删除,已补回)；终审"可合入"。全程 46/46 OK |
| 2026-08-09 | 计划B 技术债（后续消化） | 记录 | P0(Vulkan 前必做): I-1 Mult initProgram _vb 顺序耦合(重构为几何一次创建); I-2 material.shininess int→float 化(5 处,GL 允许但 Vulkan 严格类型校验会暴露)。P1: 光源管线重复建缓冲(M-1,9 App 模板固有)、Mult 循环 int/size_t(M-2 既有)。P2: shader 死代码(M-3 既有)、RhiGeometry location 无冲突断言(M-4)。评审建议: 升级 run.sh OK 判定为非视觉金样比对(简单 Light 类偏渲染语义仅靠 exit code 无法证视觉等价) | 详见 .superpowers/sdd/2026-08-09-rhi-migration-phase-b-simplelight/progress.md；下一步=写计划C(复杂 forward 批次,含 Light/Advanced 13 App + 后续 FBO/PBR) |
| 2026-08-10 | 计划C Task 1 RhiGeometry::CreateFromArray | 实现 | 5cb2040：RhiGeometry 新增 CreateFromArray(renderer,data,byteSize,vertexCount,layout)，上传手写交错顶点数组到单 VBO(binding 0)，直接复用调用方 VertexLayout，仅设 vertexBuffer/layout/vertexCount 不建 uv/normal/index buffer；纯新增未动 Create | build OK+全量46/46 OK；暂无调用方(后续 NormalMap/ParallaxMap 任务用)；编译+回归验证 |
| 2026-08-10 | 计划C Task 2 BlinnPhong+Gamma 迁 RHI | 重构 | d0c692d：GLBlinnPhongApp/GLGammaApp 从 GLProgram/glad/raw VAO 迁 RHI（光源 Sphere 管线 pos+color + 物体 Plane 管线 uv/normal drawArrays；RhiGeometry::Create 内联创建 geometry+pipeline，删 createVertexBuffer/createPlaneBuffer；RhiImage::Load2D 载 wood.png；Gamma 数组 lightPositions/lightColors setUniform(ptr,count,3) + enableGamma/gammaValue + 5 光源循环 drawIndexed；不设 enableBlinnPhong） | build OK+全量46/46 OK+BlinnPhong/Gamma各自OK；偏差：hpp 补 rhi/core/ITexture2D.hpp include（brief 遗漏，_texture 成员必需） |
| 2026-08-10 | 计划C Task 3 NormalMap+ParallaxMap 迁 RHI | 重构 | 2d93102：两 App 从 GLProgram/glad/GLImageTexture2D/GLUtils 迁 RHI。CreateRectBuffer 改返回 RhiGeometry::Geometry（CreateFromArray 上传手写交错 pos/normal/uv/tangent/bitangent 单 VBO，stride 56）+ 单管线 createPipeline(geo.layout,_shader)+setDepthTest；三/二纹理 RhiImage::Load2D；NormalMap enableNM=int 用 `_enableNormalMap?1:0`；ParallaxMap 三纹理+depthMap/heightScale/三个 enable bool 直传+Height Scale InputFloat；析构空 {} | build OK+全量46/46 OK+NormalMap(36)/ParallaxMap(37)各自OK；偏差：hpp 补 rhi/core/IBuffer.hpp/ITexture2D.hpp include（brief 只给 IPipeline/IShader，_vb/_brick 成员必需）；保留 geometry/Rect.hpp+base/Constexpr.hpp（brief 含，unused 无害） |
| 2026-08-10 | 计划C Task 3 ParallaxMap tangent 归一化修复 | 修复 | 23821fd：GLParallaxMapApp.cpp CreateRectBuffer 恢复被 RHI 迁移遗漏的 4 行 glm::normalize（triangle1/2 各对 tangent+bitangent，行 51/52/65/66），仅改 1 文件；NormalMap 保持未归一化 | build OK+全量46/46 OK |
| 2026-08-10 | 计划C 完成（4 App 迁 RHI + CreateFromArray） | 实现 | 4 提交：5cb2040(CreateFromArray)→d0c692d(BlinnPhong+Gamma)→2d93102(NormalMap+ParallaxMap)→23821fd(修 ParallaxMap tangent normalize)。终审"可合入"(Ready to merge Yes)，无 Critical/Important | 每任务评审 Approved；T3 1 轮修复(计划缺陷:ParallaxMap 复用 NormalMap 代码丢 tangent normalize,按用户决定恢复,仅改 ParallaxMap)。全量 46/46 OK |
| 2026-08-10 | 计划C 技术债/评审遗留 | 记录 | 待办(Minor,不阻塞): BlinnPhong drawScene 末尾冗余 return GLApp::drawScene(计划含,基类被调两次,无害); _curTime 未用/GLImageTexture2D 前置声明/Rect+Constexpr 未用 include; CreateFromArray 无 null-check(同 Create 风格)。spec 笔误: 设计文档说光源 Cube 但实现(正确)用 Sphere——仅文档 | 评审建议: 可选修正设计文档 Cube→Sphere; 后续批次见设计 spec。至此已迁移 19 App(Triangle/Rect/SimpleTexture/Cube/Camera/LoadModel+计划B 9+计划C 4)，剩余约 27 App |
| 2026-08-10 | 计划D Task 1 RHI 扩展(BlitMask/filter-wrap/bindTexture raw) | 实现 | 7afdb1e：①Common.hpp 新增 BlitMask{None,Color,Depth} 枚举+FramebufferAttachment 增 minFilter/magFilter/wrapS/wrapT(默认 Linear/ClampToEdge) ②IRenderer.hpp blitFramebuffer 加默认 Color\|Depth mask + 新增 bindTexture(ITexture2D*,unit) raw 重载 ③GLBackend blit 按 mask 拼 GLbitfield + raw bindTexture 实现 ④GLRenderTarget 颜色附件用 ToGLMinFilter/ToGLWrap | build OK+全量46/46 OK；复用既有 ToGLMinFilter/ToGLWrap(不新建 helper)；只改 4 RHI 文件未动 App；默认 mask 保持旧行为 |
| 2026-08-10 | 计划D Task 2 Hdr 迁 RHI（FBO render-to-texture + HDR 后处理） | 重构 | 5572d85：GLHdrApp 从 GLProgram/glad/GLImageTexture2D/GLUtils 迁 RHI。CreateCubeBuffer/CreateQuadBuffer 用 RhiGeometry::CreateFromArray(交错 pos/normal/uv)；FBO 用 _hdrRT->create(FramebufferDesc)(RGBA16F color+Depth24Stencil8)；render2FrameBuffer 渲染隧道到 RT、renderHdr 用 raw bindTexture(_hdrRT->colorTexture2D(0)) 采样 quad；保留 GetLightPosColor | build OK+全量46/46 OK+Hdr 单独 OK；只改 2 文件；concern=renderHdr 用默认 TriangleList draw(4) 与原 TRIANGLE_STRIP 有视觉差异(仅首三角形)，brief 逐字要求故保留，已记录报告待审 |
| 2026-08-10 | Task 2 Hdr quad TriangleStrip 修复 | 修复 | e7b6910：GLHdrApp::compileShader 给 _hdrPipeline setPrimitiveType(TriangleStrip)，对齐计划"全屏 quad=TriangleStrip 4 顶点"与原 GL_TRIANGLE_STRIP；_objPipeline 保持默认 TriangleList 不动 | build OK；run.sh all -b gl -d 1 = 46/46 OK；all -a Hdr = OK |
| 2026-08-10 | Task 2 Hdr 代码评审(仅 T2，忽略 brief 内 T3/T4) | 评审 | 基于 7afdb1e..e7b6910 diff 逐条核对 spec：仅改 2 文件、无 GLProgram/glad/GLImageTexture2D/GLUtils include、cube(36 顶点/布局 pos0 normal1 uv2/stride32)、quad(pos0 uv1/stride20)、FBO RGBA16F+Depth24Stencil8、render2FrameBuffer 渲到 RT 再回默认、renderHdr raw bindTexture(colorTexture2D(0))、GetLightPosColor 保留、_hdrPipeline TriangleStrip/_objPipeline TriangleList、shader 路径、uniform 类型均正确 | 结论：Spec compliant；无 Critical/Important；Minor(4 项)：_colorBuffer 未用(brief 指定，可接受)、cube 顶点注释缩进、GetLightPosColor 因 move 到文件中部导致 diff 视为新增(实为保留)、GetLightPosColor 前缺 static(原文件即如此，无关)。Task quality: Approved |

| 2026-08-10 | 计划D Task 3 Bloom 迁 RHI（多附件 FBO + pingpong blur） | 重构 | 1273792：GLBloomApp 从 GLProgram/glad/GLImageTexture2D/GLCube/GLPlane 迁 RHI。initShapes 用 RhiGeometry::Create(Cube/Plane)、quad 用 CreateFromArray；HDR FBO=2×RGBA16F+深度、2 pingpong 各 1×RGBA16F；4 管线 light/bloom=cube布局(TriangleList)、blur/final=quad布局(TriangleStrip)；blur 10-pass pingpong、renderFinal 采 colorBuffers[0]+pingpong[1]；renderQuad 不切管线；保留 GetLightPosAndColor/GetCubePositions | build OK+46/46 OK+Bloom 单独 OK；只改 2 文件；偏差=新增 m_cubeLayout/m_quadLayout 成员承接布局传递（brief compileShader 需布局但 initShapes/createQuadBuffer 无返回值） |
| 2026-08-10 | 计划D Task 4 Defer 迁 RHI（GBuffer MRT + 深度-only blit） | 重构 | fa0b64e：GLDeferApp 从 GLProgram/glad/GLCube/GLPlane 迁 RHI。GBuffer=3 颜色附件(RGBA16F×2+RGBA8 均 Nearest/ClampToEdge)+深度，renderGBuffer 写3附件(wood/brick 绑 unit0 渲 cube/plane)；renderLight 采 gPosition/gNormal/gAlbedoSpec 到 unit0/1/2 画 quad(TriangleStrip)；drawScene renderLight 后 renderLightBox 前 blitFramebuffer(m_gBuffer,nullptr,BlitMask::Depth)；保留 GetPositions/GetColorByPos/GetLightPosAndColors；renderCubes 用 GetPositions(m_Count,1,vec3(0,0.5,0)) 缩0.1 | build OK+46/46 OK+Defer 单独 OK；只改 2 文件；偏差同 T3=新增 m_cubeLayout/m_quadLayout 成员承接布局 |
| 2026-08-10 | 计划D 完成（3 后处理 App 迁 RHI + FBO 扩展） | 实现 | 5 提交：7afdb1e(RHI: BlitMask+attachment filter/wrap+bindTexture raw)→5572d85(Hdr 迁 RHI)→e7b6910(修 Hdr quad TriangleStrip)→1273792(Bloom 迁 RHI)→fa0b64e(Defer 迁 RHI)。终审"可合入"(Ready to merge Yes)，无 Critical/Important | 首次使用 IRenderTarget(render-to-texture)：Hdr 单 RGBA16F FBO、Bloom 2 附件+pingpong blur、Defer GBuffer MRT(Nearest)+深度-only blit。全量 46/46 OK |
| 2026-08-10 | 计划D 技术债/评审遗留 | 记录 | 待办(Minor,延后到下一批次): ①FramebufferAttachment filter/wrap 未作用于深度附件(GLRenderTarget 深度硬编码 Nearest/ClampToEdge，现无害但字段对深度为死字段，spec 2.2 称"颜色与深度均应用") ②ToGLMinFilter 用于 MAG(Linear/Nearest 无差别，LinearMipLinear 语义错误但无此类调用) ③Bloom 最终采 pingpong[1] 而 10-pass 结果落 pingpong[0](上游 LearnOpenGL 忠实移植，spec 3.2 明文) ④清理: Defer renderQuad 死代码/Bloom renderOneCube 冗余 setPipeline/Hdr _colorBuffer 未用/blitFramebuffer 重复默认参 | 披露盲区: run.sh OK 仅=exit0+无[error]，非视觉金样比对——后处理 App 视觉等价未自动验证，建议后续升级。至此已迁移 22 App，剩余约 24 |
| 2026-08-11 | 计划E 实施计划评审 | 评审 | 逐条核对计划E(PBR/IBL 5 App+RHI 2 缺口)与 RHI 代码/原 PBR App：Task1 attachCubeFace mip+glDrawBuffer(COLOR0) 推理成立(仅 depth 附件 capture RT 用)；prefilter generateMipmap=true 分配 mip 链、_envCubemap generateMipmap=false 正确(仅采 mip0)；Sphere 索引化+PBR shader normalMatrix(setUniformMatrix mat3) 正确；depth-only FBO complete+viewport<FBO 合法 | 结果: Issues Found，4 项阻塞需修订: ①全部 renderSphere/renderCube 漏 setIndexBuffer(仅设 vertex buffer→ELEMENT_ARRAY_BUFFER=0,drawIndexed 失败; 既有迁移 App 均先 setIndexBuffer) ②T2/T3 drawScene 与 T4/5/6 renderObjectsAndLights 先 setUniform 后 setPipeline(glUniform 作用当前 program,uniform 丢失→投影/视图矩阵无效; 惯例=setPipeline 在前) ③T6 头文件漏声明 createBrdfLUT(被定义+调用→编译错) ④T6 GetCaptureProjection 未定义/声明(原为成员,新头删了,无 static 替代→编译错)。建议: T6 renderQuad 未用声明可删、initApp 顺序写明、envCubemap 免 mip 视觉略异可接受 |
| 2026-08-11 | 计划E 设计 | 设计 | 头脑风暴：5 个 PBR/IBL App 迁 RHI + 2 个 RHI 缺口(attachCubeFace mip 参数、seamless cubemap)。设计文档已提交 c6bd318 | 用户确认范围=按依赖分层(T1 RHI扩展→T2 Base→T3 Texture→T4 IC→T5 Irradiance→T6 Specular)；静态辅助保留 file-static |
| 2026-08-11 | 计划E 实施计划 | 计划 | 写入 6 任务计划 483ad58。计划评审发现 4 处阻塞问题已修复：①renderSphere/renderCube 缺 setIndexBuffer(drawIndexed 需要) ②drawScene/renderObjectsAndLights setUniform 前缺 setPipeline(GLPipeline::setUniform 走 glUniform 需当前 program) ③T6 头文件漏 createBrdfLUT 声明 ④T6 缺 GetCaptureProjection static 定义 | 关键简化：envCubemap generateMipmap=false(prefilter 用独立 _prefilterMap 自建 mip 链)；capture RT 仅 depth 附件 + attachCubeFace 内部显式 glDrawBuffer(COLOR_ATTACHMENT0)；quad 用 TriangleStrip |
| 2026-08-11 | Phase E Task 1 RHI 扩展 attachCubeFace mip + seamless | 实现 | 9f8e105：IRenderTarget.hpp 加 mip=0 默认参；GLRenderTarget.cpp 传 mip 给 glFramebufferTexture2D + 显式 glDrawBuffer(COLOR_ATTACHMENT0)；GLBackend init 启 GL_TEXTURE_CUBE_MAP_SEAMLESS | build OK+全量 46/46 OK；必要偏差=GLRenderTarget.hpp override 签名同步更新（brief 只列 3 文件） |
| 2026-08-11 | Phase E Task 2 PBR_Base 迁 RHI | 重构 | bd35fcf：GLPBRBaseApp 从 GLProgram/GLSphere/GLCube/glad 迁 RHI（RhiGeometry::Create Sphere 三缓冲+索引，compileShader 用 layout createPipeline+setDepthTest，renderSphere setPipeline→setVertexBuffer(0/1/2)→setIndexBuffer→setUniform model→setUniformMatrix normalMatrix→drawIndexed，drawScene setPipeline 先于 setUniform；删 CreateTexture/glEnable(深度测试)） | build OK+全量 46/46 OK+PBR_Base 单独 OK；必要偏差=补 geometry/Sphere.hpp include（brief 遗漏致编译错），文件-static GetLightPosAndColor/GenreateObjPos 保留 |
| 2026-08-11 | Phase E Task 3 PBR_Texture 迁 RHI | 重构 | a2a5658：GLPBRTextureApp 从 native GL 迁 RHI，继承 GLPBRBaseApp（5 纹理 RhiImage::Load2D 绑 unit1-5 + setUniform 采样器，drawScene setPipeline 先于 setUniform，renderSphere 复用基类） | build OK+全量 46/46 OK+PBR_Texture 单独 OK；必要偏差=基类 GetLightPosAndColor/GenreateObjPos 由文件-static 改为 protected static 成员（brief 派生 cpp 直接调用，static 内部链接不可见致编译错），GetLightPosAndColor auto 返回改显式 pair（跨 TU 不能 auto 推导） |
| 2026-08-11 | Phase E Task 4 IC 迁 RHI（render-to-cubemap 标准流程） | 重构 | 912c421：GLIBLIrradianceConversionApp 独立于 GLPBRBaseApp，从 native GL 迁 RHI。建立 render-to-cubemap 流程：_envCubemap(createTexture3D+createEmpty RGB16F 512)、_captureRT(仅 depth 附件)、renderToCubemap() 6 面 setViewport→setUniform view→attachCubeFace→clearColor→renderCube；renderCube/renderSphere 遵循 setPipeline→setVertexBuffer(0/1/2)→setIndexBuffer→setUniform→drawIndexed；Background setDepthTest+LessEqual；CUBE/PBR/Background 用 RhiGeometry::Create cube+sphere | build OK+全量 46/46 OK+PBR_IBL_Irradiance_Conversion 单独 OK（HDR→cubemap GPU 预处理无 GL 错误）；必要偏差=补 geometry/Cube.hpp 与 geometry/Sphere.hpp include（brief 遗漏致编译错） |
| 2026-08-11 | Phase E Task 4 代码评审（a2a5658..912c421） | 评审 | 结论=Needs fixes。Critical×1：renderCube(GLIBLIrradianceConversionApp.cpp:134)只 setIndexBuffer 不 setVertexBuffer，而 RHI 顶点属性指针按 pipeline 各自 VAO 保存——renderToCubemap 预绑的 cube 缓冲落在 m_cubeMapProgram 的 VAO，renderBackground 用 m_backgroundProgram(另一 VAO)调 renderCube，其 VAO 无任何 attribute pointer(全禁用→顶点读 generic(0,0,0,1)→背景 skybox 简并/缺失)；报告"关注点"断言"所有调用路径均先绑定"对 background 路径不成立(renderObjectsAndLights 先绑的是 sphere 缓冲且绑在 m_program 的 VAO)。修复=renderCube 补 setVertexBuffer(0/1/2)(对齐 brief 自身 critical 顺序与 GLPBRBaseApp::renderSphere 惯例)。Minor=renderBeforeLoop 掉 override 关键字(基类 virtual 仍生效,非功能)、props.width unsigned→int 窄化 warning | 红线复核(build+46/46+冒烟)通过但 run.sh OK 仅=exit0+无[error]，背景缺失为视觉缺陷不会被捕获；T5/T6 复用此标准流程前必须修 renderCube |
| 2026-08-11 | Phase E Task 4 Critical 修复 renderCube setVertexBuffer | 修复 | b0c16ea：renderCube(GLIBLIrradianceConversionApp.cpp) 在 setUniform/drawIndexed 前补 setVertexBuffer(vertex/uv=1/normal=2)+setIndexBuffer，对齐 renderSphere 标准顺序(setPipeline→setVertexBuffer(0/1/2)→setIndexBuffer→setUniform model→drawIndexed)。因 RHI 顶点属性按 per-pipeline VAO 保存，renderBackground 用 m_backgroundProgram(另一 VAO)调 renderCube 时其 VAO 缺属性指针→天空盒简并；修复后 Background 正确绑定 cube 缓冲 | build OK(仅既有 int 窄化 warning)+全量 46/46 OK+PBR_IBL_Irradiance_Conversion 单独 OK(无崩溃/无 GL 错误)。T5/T6 复用此标准流程的隐患已消除 |
| 2026-08-11 | Phase E Task 5 PBR_IBL_Irradiance 迁 RHI | 重构 | 4385130：GLIBLIrradianceApp 独立于 GLPBRBaseApp，镜像 Task 4 参考(renderCube 含 setVertexBuffer 无 Task4 bug)迁 RHI。额外 irradiance pass：_irradianceProgram(Irradiance.vs/fs, cubeLayout, setDepthTest true)、_irradianceMap(createTexture3D 32×32 RGB16F ClampToEdge/Linear/no mip)、createIrradianceMap()、renderIrradianceMap()(renderToCubemap 后 6 面 viewport32→attachCubeFace→clearColor→renderCube, 共用 m_captureRT 512 深度附件视口32合法)；renderBeforeLoop=renderToCubemap→createIrradianceMap→renderIrradianceMap；renderObjectsAndLights 绑 _irradianceMap unit0+setUniform irradianceMap=0(IC 无) | build OK(仅既有 int 窄化 warning)+全量 46/46 OK+PBR_IBL_Irradiance 单独 OK(无崩溃/无 GL 错误)；只改 2 文件；必要偏差=补 geometry/Cube.hpp+geometry/Sphere.hpp include |
| 2026-08-11 | Phase E Task 6 PBR_IBL_Specular 迁 RHI | 重构 | 2cf6e69：GLIBLSpecularApp 镜像 Task 5 迁 RHI，硬任务=prefilter mip + BRDF LUT + quad。CreateQuadBuffer(交错 pos+uv, location 0/1, CreateFromArray)、GetCaptureProjection static、createPrefilterMap(128 RGB16F LinearMipLinear generateMipmap=true)、renderPerfilterMap(5mip×6面 attachCubeFace mip 参数+viewport mipSize)、createBrdfLUT(独立 RT 仅 Color RG16F 512 + noop-deleter 共享 colorTexture2D(0))、renderBrdfLUT(quad TriangleStrip draw)、renderObjectsAndLights 绑 irradiance(0)+prefilter(1)+brdfLUT(2)、envCubemap generateMipmap=false(background 仅 mip0)、compileShader(cubeLayout,quadLayout) initShapes 先建几何、renderCube/renderSphere setVertexBuffer 无 Task4 bug | build OK(仅既有 int 窄化 warning)+全量 46/46 OK+PBR_IBL_Specular 单独 OK(initApp 全 GPU prefilter 链无崩溃/无 GL 错误)；只改 2 文件；偏差=补 geometry/Cube.hpp+geometry/Sphere.hpp+<cmath> include；关注点=Brdf.vs aTexCoords@2 与 quad 布局 texcoord@1 不一致(原 native 固有, brief 逐字保留) |
| 2026-08-11 | Phase E 终审修复（整支评审 2 项） | 修复 | 67714da：①Critical=GLRenderTarget::attachCubeFace 尾部 glBindFramebuffer(0) 使 capture 画到窗口 FBO→cubemap 永不写入/黑天空盒，删除解绑让 FBO 保持绑定（仅 3 IBL App capture 循环调用，均先 bind 后 unbind，安全）②Important=3 IBL App initApp 内重复 renderBeforeLoop()（Application::run 已调一次）致跑两次，删除 3 处 | build OK+全量 46/46 OK+3 IBL App 各单独 OK（无崩溃/无 GL 错误）；报告追加 Final-review fix 至 task-6-report.md |
| 2026-08-11 | Phase E 终审修复复评（2cf6e69..67714da） | 评审 | 复评 2 项 open finding 与新增破坏。①attachCubeFace 已删除尾部解绑(仍保持绑定)；capture 循环 setRenderTarget(m_captureRT)→attachCubeFace(绑FBO+挂 cube 面+glDrawBuffer(COLOR0))→clear/draw→setRenderTarget(nullptr)，cubemap 面为正确渲染目标；grep 确认 attachCubeFace 仅 3 IBL App capture 循环调用，无其它依赖解绑调用方②renderBeforeLoop 现仅 Application::run() 调度一次，3 处 initApp 内重复调用已删③diff 仅 4 文件 4 删除，无新增破坏；capture 各函数结尾均 setRenderTarget(nullptr) 恢复窗口 FBO+viewport | 结论：两 finding 均已解决，无新增破坏，verdict=all findings addressed |
| 2026-08-11 | 计划E 实施（SDD 完成） | 实现 | 7 提交：9f8e105(RHI attachCubeFace mip+seamless)→bd35fcf(PBR_Base)→a2a5658(PBR_Texture)→912c421(IC)→b0c16ea(T4修复 renderCube 补 setVertexBuffer)→4385130(Irradiance)→2cf6e69(Specular)→67714da(终审修复)。终审发现并修复 1 Critical+1 Important | T4 修复：renderCube 缺 setVertexBuffer(per-pipeline VAO 存 attribs)致 skybox 黑。终审修复 67714da：①attachCubeFace 去掉尾部 unbind(否则 capture 画到窗口 FBO、cubemap 空) ②删 3 个 IBL initApp 里重复 renderBeforeLoop(Application::run 已调)。全量 46/46 OK |
| 2026-08-11 | 计划E 遗留(Minor/延后) | 记录 | ①BRDF LUT texcoord 错位(quad@1 vs Brdf.vs@2→采样(0,0)恒定 LUT，忠实原生遗留非回归，待修 quad 或 shader) ②attachCubeFace glDrawBuffer 破坏多附件 RT(理论，现无多附件调用 attachCubeFace) ③T6 renderQuad 死声明 ④T2 基类未用成员 ⑤T4 override 限定符/narrowing warning | 至此已迁移 27 App(PBR/IBL 5 个)，剩余约 19(Shadow×3/SSAO/SimpleGemotery 等)。下一批次：Shadow×3/SSAO |
| 2026-08-11 | 计划F(Shadow×3/SSAO)实施计划评审 | 评审 | 验证 shader loc↔VertexLayout、几何 draw/drawIndexed、RHI 签名、clearColor 语义、冒烟名均正确。3 finding：①T1/T3/T4 深度-only FBO 需补 glReadBuffer(GL_NONE)（原生即设；RHI create 只设 drawBuffer，严格驱动报 INCOMPLETE_READ_BUFFER→create false→冒烟 FAIL）②T4 删 type reset 致地板 type=2 残留变纯白（原生每 draw 归零）③T3 reanderFraemBuffer RHI 体未给出。advisory：T5 灯立方矩阵顺序/原样保留、T4 debug quad 全屏vs Rect、T1 片段含既有字段易误加 | 结论=Issues Found，待修复后批准 |
| 2026-08-11 | 计划F 评审修复（6 处） | 修复 | 针对评审 3 blocking+3 advisory：①T1 Step3 补 glReadBuffer(GL_NONE)（深度-only FBO 严格驱动 INCOMPLETE_READ_BUFFER）②T4 renderCube 恢复绘后 type 归 0（原生 GLShadowApp.cpp:245，防地板 type=2 残留纯白）③T3 补回 reanderFraemBuffer 完整 RHI 体（深度 debug quad 用 _shadowDepthMapFbo->depthTexture2D()+Rect quad）④T5 灯立方矩阵改 scale→translate(S*T) 对齐原生 ⑤T1 Step1 标注"仅 borderColor 新增" ⑥澄清 T4 debug quad 全屏 vs 原生 Rect、删不存在的 clearDepth API | 计划文档 docs/superpowers/plans/2026-08-11-rhi-migration-phase-f-shadows-ssao.md 已修订，待批准后进入 SDD 执行 |
| 2026-08-12 | Phase F Task 1 RHI 扩展（borderColor+深度 filter/wrap+单通道格式+深度-only readBuffer） | 实现 | 2e35199：①Common.hpp FramebufferAttachment 末尾加 borderColor[4]默认{0,0,0,1}（仅新增，wrap 已存在）②GLRenderTarget.cpp 深度非 MSAA 分支改用 ToGLMinFilter/ToGLWrap(a.*)，wrap 为 ClampToBorder 时 glTexParameterfv(BORDER_COLOR)③GLHeader.hpp ToGLFormat 增 R32F→GL_RED/RG16F→GL_RG④无颜色附件分支 else{glDrawBuffer(GL_NONE);glReadBuffer(GL_NONE);} | build OK+全量46/46 OK+Hdr/Bloom/Defer/PBR_IBL_Specular 各单独 OK；Step2 局部变量即 `a` 与 brief 逐字一致无偏差 |
| 2026-08-12 | Phase F Task 2 RHI 扩展 attachDepthCube（深度 cubemap attach） | 实现 | 68398f2：IRenderTarget.hpp attachCubeFace 声明后加纯虚 `attachDepthCube(ITexture3D*,int mip=0)`；GLRenderTarget.{hpp,cpp} override+实现（glFramebufferTexture DEPTH_ATTACHMENT 分层+glDrawBuffer(GL_NONE)/glReadBuffer(GL_NONE)） | build OK+全量46/46 OK+IC App 单独 OK；三片段逐字一致无偏差；供 T5 点光源阴影 cubemap 渲染 |
| 2026-08-12 | Phase F Task 3 GLShadowMapApp 迁 RHI | 重构 | ad704db：GLShadowMapApp 从 native GL（GLProgram/glad/裸缓冲）迁 RHI。initShapes 用 RhiGeometry::Create（Sphere=阴影对象 indexed、Plane=地板三缓冲、Rect=屏幕深度 debug quad uv@2 匹配 Depth.vs）；createShadowDepthBuffer 深度-only FBO Depth32F Nearest/Repeat；compileShader 双管线 setDepthTest(true) 替代 glEnable；renderScene2FrameBuffer setPipeline→setVertexBuffer→setIndexBuffer→setUniform→drawIndexed 铁序；clearColor 代 glClear；reanderFraemBuffer 全屏深度 quad 绑 depthTexture2D；析构空体（shared_ptr 自释放） | build OK（既有 int 窄化 warning）+全量 46/46 OK+Shadow_Map 单独 1/1 OK；偏差=brief typo compileShader 参数用下划线成员、createVertexBuffer/createPlaneBuffer 保留 RHI 体（initApp 改走 initShapes）、深度 debug 用全屏 quad 替原生小视口 |
| 2026-08-12 | Phase F Task 3 复审修复（死重复代码 Important） | 修复 | 8a9ec89：评审发现 createVertexBuffer/createPlaneBuffer 与 initShapes 几何块逐字节重复且无调用；删除两函数体+头声明（不留 stub）；删多余注释（:155 clearColor 重复注释）+头文件无用 include ITexture3D.hpp/Sphere.hpp；保留 :56 uv@2 绑定约定、:113 clearColor 语义说明 | build OK+全量 46/46 OK+Shadow_Map 独立 1/1 OK；amend（原 ad704db 未推送），message 不变 |
| 2026-08-12 | Phase F Task 4 GLShadowApp 迁 RHI | 重构 | 112b7b8：GLShadowApp 从 native GL（GLProgram/glad/裸缓冲）迁 RHI。initShapes 用 RhiGeometry::Create（Cube=索引化阴影对象、Plane=地板三缓冲）；createQuadBuffer 用 CreateFromArray 手写全屏 quad（pos3+uv2 @0/1）匹配 DebugQuand.vs；createShadowDepthBuffer 深度-only FBO Depth32F+Nearest+CLAMP_TO_BORDER+白 borderColor（T1 新增 borderColor 字段首用）；compileShader 4 管线（shadow/depth=cubeLayout+setDepthTest、debug=quadLayout+TriangleStrip）；renderScene/renderCube/renderPlane/renderScene2FrameBuffer/renderScene2Screen/renderDepthDebug 走 RHI 铁序+clearColor；type 绘后归 0 保留；ImGui 5 checkbox 保留；析构空体 | build OK（既有窄化 warning）+全量 46/46 OK+Shadow 单独 1/1 OK；偏差=头文件删 Sphere.hpp/GLImageTexture2D 前置声明（_texture 改 ITexture2D、无 Sphere 成员）、initApp 保留相机覆盖行（brief 片段省略）、drawScene 去死代码 curTime |
| 2026-08-12 | Phase F Task 5 GLPointLightShadowApp 迁 RHI | 重构 | 1d3b0cb：GLPointLightShadowApp 从 native GL（GLProgram/glad/裸缓冲/深度 cubemap FBO）迁 RHI。initShapes 用 RhiGeometry::Create Cube 四缓冲+索引；createShadowDepthBuffer 用 ITexture3D::createEmpty（Depth32F 立方体 ClampToEdge/Nearest）+ IRenderTarget::create(fbd) 空颜色基座 + attachDepthCube 挂分层深度；compileShader 双管线（shadow VS+FS、depth VS+GS+FS 3 阶段 Geometry）各 setDepthTest+setCullFaceEnable；renderScene/renderCube/renderScene2FrameBuffer/renderScene2Screen 走 RHI 铁序+clearColor（末尾 clearColor 替 drawScene 头裸 glClear）；shadowMatrices[i] 逐元素 setUniform；灯位矩阵 S*T、CullFace 就地切换保留；ImGui checkbox/Text 保留；CreateTransformVector 文件级保留；析构空体 | build OK（既有窄化 warning）+全量 46/46 OK+Shadow_PointLight 单独 1/1 OK；偏差=仅措辞等价（lightPos.z 保留原生 static_cast 写法）、补 ITexture3D.hpp include（_shadowDepthMap 成员必需） |
| 2026-08-13 | Task 9b Batch1 SimpleLight 5 App UBO 化 + shader 升级 | 重构 | b178c7d：5 个 SimpleLight App（Ambination/Diffuse/Specular/Material/Map）从 setUniform → `_ubo`+`rhi::SetUniform` 显式 UBO 模式（照 9a Cube 模板，各 App 局部成员），对应 10 对 shader 升 430+UniformBlock（ULight block 外）。槽位：lightColor[3]/objectColor[4]/lightPos[2]/viewPos[0]/material.ambient[7]/diffuse[8]/specular[9]/shininess floatPool[0]/ambient·specular·diffuseStrength[1,2,3]/times[10]；Material/Map 单 Light struct→SetLight(_ubo,0,...)（lights[0]）；Map 采样器 material.diffuse/specular setUniform 行删，靠 bindTexture；Task 8 脚本重跑刷新 res/Vulkan 镜像 | build OK+全量 46/46 OK+5 App 各自 OK；首轮 Diffuse Light.frag 缺 block 声明已补全 5 个 Light.frag/Source.frag 重验全过；concern=Map 采样器 uniform 删除后 specularMap 默认 unit0（采样 diffuse 纹理，非黑不崩，符合 brief 逐字），详见 task-9b-report.md |
| 2026-08-12 | Phase G Task 2 GLDepthTestApp 迁 RHI | 重构 | 8d0123d：GLDepthTestApp 从 native GL（GLProgram/glad/GLImageTexture2D/裸缓冲）迁 RHI（最简模板）。initApp 用 RhiGeometry::Create（Cube indexed+Plane drawArrays）+ createPipeline 双成员缓冲 + setDepthTest(true) 替 glEnable + RhiImage::Load2D 载 marble/metal；drawScene 铁序 bindTexture(0/1)→setPipeline→setVertexBuffer(0/1)→setIndexBuffer→setUniform(projection/view/textureSampler/model)→drawIndexed/draw；initializeCubePositions 保 file-static；删 beginDrawScene/createCubeBuffer/createPlaneBuffer/_curTime | build OK+全量 46/46 OK+DepthTest 单独 1/1 OK；只改 2 文件；偏差=无（附加清理死声明） |
| 2026-08-12 | Phase F Task 6 GLSSAOApp 迁 RHI | 重构 | 498db12：GLSSAOApp 从 native GL（GLProgram/glad/裸缓冲）迁 RHI。initShapes 用 Cube shape 原始数据构造交错 pos3+normal3+uv2（stride 8 floats, layout normal@1/uv@2）经 CreateFromArray 上传（避开 RhiGeometry::Create 默认 pos/color/uv/normal 布局 → GBuffer.vs 会把 color@1 误读为 normal@1）；createQuadBuffer 全屏 pos3+uv2 CreateFromArray TriangleStrip；GBuffer FBO=MRT 2×RGBA16F+RGBA8+Depth24Stencil8、SSAO/blur ping-pong 各 R32F（T1 扩展）；noise ITexture2D::init(RGBA32F, 4×4, channels=3→源GL_RGB)；compileShader 4 管线（GBuffer TriangleList+depth，SSAO/Blur/Light TriangleStrip）；renderGBuffer/SSAO/Blur/LightPass 走 RHI 铁序+clearColor；backpack 便携 RHI Model；析构空体（shared_ptr RAII）；删未用 m_woodTexture/m_brickTexture 与 createCubeBuffer | build OK（仅既有窄化 warning）+全量 46/46 OK+SSAO 单独 1/1 OK（2s 无 GL error）；只改 2 文件；无 functional 偏差（Step3 自审关键 cube 布局、room 缩放 15.0 均已执行）；GLfloat→float（删 glad 后 uniform_real_distribution 泛型参数） |
| 2026-08-11 | 计划F 实施完成（6 任务 SDD） | 实现 | 6 提交：2e35199(深度附件 filter/wrap/border+单通道 ToGLFormat+glReadBuffer 修复)→68398f2(attachDepthCube)→8a9ec89(GLShadowMapApp)→112b7b8(GLShadowApp)→1d3b0cb(GLPointLightShadowApp)→498db12(GLSSAOApp)。每任务独立评审（T3 1 轮修复删死代码）；最终整支评审拆 3 scope（RHI/阴影/SSAO）全部通过 | 全量 46/46 OK + 各 App 冒烟 OK。4 个阴影/SSAO 已迁 RHI，累计迁移 31 App。遗留：GLShadowApp cull-face 全局态泄漏（plan-mandated+既有 RHI 设计，视觉良性已 park）待合并决策
| 2026-08-11 | 计划F 收尾：用户决策 + 清理 | 记录 | 用户拍板接受 GLShadowApp cull-face parked finding（接受现状合并，留待未来 RHI per-pipeline 状态重构）。计划F workspace 已删除，git 历史为最终记录 | 计划F 完成，develop 分支 6 提交已落库
| 2026-08-12 | 计划G(剩余 13 App)设计+实施计划已写并审查 | 计划 | spec(2026-08-12-rhi-migration-phase-g-remaining-apps-design.md)+plan(2026-08-12-rhi-migration-phase-g-remaining-apps.md)。12 任务覆盖 13 App+GLNormalLine 依赖+2 RHI 缺口(G1 POINT_SIZE@T1, G2 Mesh instanced@T12)+清偿 Saturn 技术债 | 计划两轮审查通过：首轮 4 阻塞(beginDrawScene 链接×5、NormalLine normal@2、5 处 -a 名、Saturn rock 采样器名+bindTexture)+3 advisory 全修正；二轮 Approved。用户拍板单一大计划一次做完、缺口随 App 引入、Saturn 债一并清偿 |
| 2026-08-12 | Phase G Task 1 G1 RHI 缺口 + GLAdvancedGLSLApp 迁 RHI | 实现 | 7f15308：IPipeline 加纯虚 setPointSizeProgramEnable + GLPipeline override+_pointSizeEnable+即时 glEnable/Disable(GL_PROGRAM_POINT_SIZE)；GLAdvancedGLSLApp 从 native GL( GLProgram/glad/裸缓冲) 迁 RHI（RhiGeometry::Create cube pos/uv/索引→_vb/_uv/_ebo、createShader/compile→createPipeline、RhiImage::Load2D dog.jpg、drawScene 铁序+PolygonMode Point/Fill 分支调 setPointSizeProgramEnable、cubePositions 照抄）；删 hpp beginDrawScene 声明+_vbo/_vao+createVertexBuffer | build OK+全量 46/46 OK+AdvancedShader 单独 1/1 OK；偏差=createVertexBuffer 直接删除(非改签名，新 cpp 内联创建不调用)、_curTime 保留未引用；确认源 hpp 确有 beginDrawScene 故删除声明
| 2026-08-12 | Phase G Task 2 GLDepthTestApp 迁 RHI 评审 | 评审 | 8d0123d 逐项核对：Step1/2/3/4 全过，铁序/uniform/texture 单元/索引 vs draw 均正确，beginDrawScene 已删，file-static 保留，无越界（仅 2 文件） | Spec compliance ✅ / Task quality Approved，无 Critical/Important，仅 2 Minor（hpp 无用 array include、plane 复用 cube layout）可择机清理 |
| 2026-08-12 | Phase G Task 3 GLBlendApp 迁 RHI | 实现 | 105d1f4：GLBlendApp 从 native GL 迁 RHI。hpp 删 GLProgram/GLImageTexture2D、_program→_pipeline、4 纹理→ITexture2D、几何→IBuffer。cpp initApp 编译 Blend 着色器+RhiGeometry Cube(indexed)+Plane+createPipeline+setDepthTest(true)/setBlend(true)/setBlendFunc(SrcAlpha,OneMinusSrcAlpha)+RhiImage 4 纹理；drawScene 照抄 cubePositions/各物体 model/4 纹理 unit 0-3 与 sampler 取值/texColor；铁序+立方体 drawIndexed+平面窗草 draw(6,0) | build OK+全量 46/46 OK+Blend 单独 1/1 OK；仅改 2 文件；无 functional 偏差（_pipeline 命名严格按 brief，未沿用 GLDepthTest 的 _contentPipeline） |

| 2026-08-12 | Phase G Task 4 GLCullFaceApp 迁 RHI | 重构 | a465156：GLCullFaceApp 从 native GL（GLProgram/glad/裸缓冲）迁 RHI，镜像 GLBlendApp 但仅 Cube+4 纹理。initApp 用 RhiGeometry::Create Cube indexed + createPipeline + setDepthTest(true)/setBlend(true)/setBlendFunc(SrcAlpha,OneMinusSrcAlpha)/setCullFaceEnable(true)/setCullFace(Back)/setFrontFace(false)（CW）+ RhiImage 4 纹理；drawScene 只画一个 Cube 保留 model（translate+rotate45×2+scale2）+ 纹理 unit0 + 铁序 drawIndexed；删 createPlaneBuffer/_planeVao/_program/createCubeBuffer/compileShader/initGLEnv | build OK+全量 46/46 OK+CullFace 单独 1/1 OK；只改 2 文件；无 functional 偏差（Cube 改 drawIndexed 符合 brief 铁序） |
| 2026-08-12 | Phase G Task 5 GLSkyboxApp 迁 RHI | 重构 | 9eb7546：GLSkyboxApp 从 native GL（GLProgram/glad/GLImageTexture2D/3D）迁 RHI。反射 cube 用 RhiGeometry::Create(useUv+useNormal+useIndex, Layout{uv=3,normal=2})（_cubeVb/_cubeNormal@2/_cubeUv@1/_cubeEbo）；天空盒复用 cg.vertexBuffer 作 _skyVb，skyLayout 仅 Float4@0(offset0,stride32)；双管线 cubePipeline(setDepthTest)/skyboxPipeline；drawCube 铁序+完整 uniform(cameraPos vec3/textureSampler0/skyBoxSampler1/enableReflection/enableRefraction) drawIndexed；drawSkybox setDepthFunc(LessEqual)+setDepthMask(false)→draw→恢复；Load2D dog.jpg+LoadCube Skybox；删 beginDrawScene/CreateSkyBoxBuffer/createVertexBuffer；析构空体 | build OK+全量46/46 OK+SkyBox单独1/1 OK；只改2文件；无功能偏差（cameraPos 用 vec3 重载） |
| 2026-08-12 | Phase G Task 6 GLTemplateTestApp 迁 RHI | 重构 | c3a2e07：GLTemplateTestApp 从 native GL（GLProgram/glad/GLImageTexture2D/裸缓冲）迁 RHI。hpp 纹理→ITexture2D、program→_pipeline/_borderPipeline、几何→IBuffer，删 beginDrawScene/createCubeBuffer/createPlaneBuffer 声明。cpp initApp 相机(0,0,3,-90,-10)、RhiGeometry::Create(Cube indexed+Plane draw)、双管线共用 cg.layout、_pipeline setDepthTest(true)/setDepthFunc(Less)/setStencilTest(true)/setStencilFunc(NotEqual,1,0xFF)/setStencilOp(Keep,Replace,Replace)+RhiImage marble/metal；drawScene clearColor 开头，保留三阶段 stencil（地板 setStencilFunc(Always,1,0xFF)+Mask0x00 画 plane→cube 循环先 Always+Mask0xFF _pipeline 画 cube 写 stencil 再 NotEqual+Mask0x00+setDepthTest(false) _borderPipeline 画放大 cube→末尾恢复 Mask0xFF+Always,0,0xFF+setDepthTest(true)），铁序 draw/drawIndexed；删 CheckGLStencilAbility 及调用 | build OK+全量46/46 OK+TemplateTest单独1/1 OK；只改2文件；无功能偏差（附加清理删 createCubeBuffer/createPlaneBuffer 与 beginDrawScene）
| 2026-08-12 | Phase G Task 6 评审修复（C1 纹理未绑定） | 修复 | ddb600a：评审发现删 beginDrawScene 时丢失纹理绑定逻辑，cpp 仅 setUniform 无 bindTexture → cube/plane 无纹理。修复=drawScene 开头 clearColor 后补 renderer()->bindTexture(_cubeTexture,0)/bindTexture(_planeTexture,1)（GL 即时全局绑定，与 textureSampler 0/1 对应） | build OK+全量46/46 OK+TemplateTest单独1/1 OK；amend 原提交（c3a2e07→ddb600a，消息不变）
| 2026-08-12 | Phase G Task 7 GLUniformBufferApp 迁 RHI（UBO 首用） | 重构 | a757d09：GLUniformBufferApp 从 native GL（GLProgram/glad/GLProgram/裸缓冲）迁 RHI。hpp program→vector<shared_ptr<IPipeline>> _pipelines、删 _vbo/_vao、加 _vb/_uv/_ebo/_indexCount/_ubo、删 createVertexBuffer/createUniformBuffer 声明。cpp initApp RhiGeometry::Create(Cube uv+indexed) 后循环4次 createShader+compile+createPipeline(geo.layout)+bindUniformBlock(0)；UBO createUniformBuffer→init(nullptr,2*mat4,Uniform)→bindRange(0,0,2*mat4)→update(projection@0)→update(view@mat4)；drawScene 保留 cubePositions/colors 与旋转循环，4 cube 各用 _pipelines[i]，铁序 setPipeline→setVertexBuffer(_vb)→setVertexBuffer(_uv,1)→setIndexBuffer(_ebo)，只 setUniform model/cubeColor(vec4,1,4)，projection/view 来自 UBO，drawIndexed；析构空体。纹理确认：Cube.frag 仅 cubeColor 无纹理、无 beginDrawScene，无需绑定 | build OK+全量46/46 OK+UniformBuffer单独1/1 OK；只改2文件；无功能偏差 |
| 2026-08-12 | Phase G Task 8 GLFrameBufferApp 迁 RHI（render-to-texture + 后处理 quad） | 重构 | 312bf00：GLFrameBufferApp 从 native GL（GLProgram/glad/GLImageTexture2D/裸缓冲）迁 RHI。initApp RhiGeometry::Create（Cube indexed + Plane draw + Rect indexed）+ RhiImage::Load2D（container2/metal）+ compileShader 双管线共用 layout；createFrameBuffer 用 FramebufferDesc RGB8(Linear)+Depth24Stencil8（窗口宽高）；drawScene setRenderTarget(_screenFbo)→clearColor→drawCube/drawPlane→setRenderTarget(nullptr)→setDepthTest(false)→setPipeline→bindTexture(colorTexture2D(0),1)+textureSampler=1+effectType→setVertexBuffer(0/1)+setIndexBuffer→drawIndexed(6,0,0)→setDepthTest(true)；drawCube/drawPlane 铁序+位置/model 照抄；删 createCubeBuffer/createPlaneBuffer/createScreenBuffer/_curTime；析构空体 | build OK+全量46/46 OK+FrameBuffer单独1/1 OK；只改2文件；偏差=brief screen 段补 setVertexBuffer(_screenUv,1)（RhiGeometry uv 独立 binding1 需装配，Screen.vert@2 采样否则恒 (0,0)，对齐 GLShadowMapApp 模板）
| 2026-08-12 | Task 8 代码评审（GLFrameBufferApp 迁移） | 评审 | Spec compliance ✅，Task quality Approved。逐条对照 brief 全部满足（只改2文件/不重命名GLApp/FBO RGB8+Depth24Stencil8窗口尺寸/drawScene铁序/hpp无beginDrawScene/无新增注释）；核实 screen quad 偏差正确必要：Screen.vert@location2 采样 uv，RhiGeometry 把 uv 独立 binding1（uvLocation=2），不绑 _screenUv 则 textureCoord 恒(0,0)，且与 GLShadowMapApp.cpp:141 setVertexBuffer(_screenUv,1) 一致。无 Critical/Important；Minor=报告引用模板行号微偏(实际141)，文档细节 | 无需重跑红线；批准合入 |
| 2026-08-12 | Phase G Task 9 GLMsaaApp 迁 RHI（MSAA FBO + resolve + 后处理） | 重构 | c9d7458：GLMsaaApp 从 native GL（GLProgram/glad/GLImageTexture2D/裸缓冲）迁 RHI。hpp 删 beginDrawScene/GLProgram/_vbo/_vao/_screenVao/_screenVbo/_curTime，_texture→ITexture2D、program→_pipeline/_postPipeline、几何→IBuffer、FBO→IRenderTarget。cpp initApp RhiGeometry::Create(Cube+Rect indexed uv@2)+RhiImage dog.jpg+compileShader 双管线共用 layout；createFrameBuffer=MSAA fbd.samples4 RGB8s4+Depth24Stencil8s4、createPostFrameBuffer=RGB8 Linear 单附件；drawFrameBufferMssa 铁序 画cube→setViewport窗口尺寸→blitFramebuffer(_msaaFbo,_postFbo,BlitMask::Color)→画全屏quad绑 colorTexture2D(0)；drawGLMssa _pipeline->setMultisample(_enableMsaa) 替 glEnable/Disable；两 checkbox+getSampleCount()=4 保留 | build OK（既有窄化 warning）+全量46/46 OK+Msaa单独1/1 OK；只改2文件；偏差=screen/cube 均补 setVertexBuffer(_screenUv/_cubeUv,1)（RhiGeometry uv 独立 binding1 @2，不装则 (0,0)，对齐 FrameBuffer/ShadowMap 模板）、cube 改 drawIndexed(36) 替 glDrawArrays |
| 2026-08-12 | Phase G Task 9 代码评审（GLMsaaApp 迁移） | 评审 | Spec compliance ✅，Task quality Approved。逐条对照 brief 全满足（只改2文件/不重命名GLApp/MSAA fbd.samples4+RGB8s4+Depth24Stencil8s4/post RGB8 Linear单附件/铁序/blit resolve 前 setViewport 窗口尺寸/drawGLMssa setMultisample/drawScene 末尾 clearColor 同清 depth(GLBackend:59)/getSampleCount()=4/无新增注释）；核实 uv@2 binding1 偏差正确必要（Cube.vs+Post.vs inTextureCoord@2，RhiGeometry uvLocation2 binding1，不装则 (0,0)，对齐 Task8 前例）；RHI 接口签名逐项对上（blitFramebuffer/colorTexture2D raw bind/setVertexBuffer(binding)/setMultisample/setDepthTest）。无 Critical/Important；Minor×2=hpp:23-24 遗留 createVertexBuffer/createScreenBuffer 死声明（.cpp 已删定义且不再调用，报告却称已删，与事实不符，建议删声明）、cpp:119 Viewport 用 unsigned attr.width/height 触发 -Wnarrowing（同模板式） | 无需重跑红线；批准合入 |
| 2026-08-12 | Phase G Task 10 GLExplodeApp+GLNormalLine 迁 RHI（geometry shader 组） | 重构 | 1c67f72：两 App 从 native GL（GLProgram/glad/裸缓冲）迁 RHI。hpp 删 GLProgram/_vbo/_vao/_ebo/createVertexBuffer 声明/GLImageTexture2D 前置声明，program→_pipeline(+_normalPipeline)，加 IBuffer 几何成员+_indexCount。cpp 三段 compile（Vertex+Fragment+Geometry）；GLExplodeApp：RhiGeometry::Create(sphere,false,true,true,Layout{0,2}) indexed，drawScene 铁序 setPipeline→setVertexBuffer(_vb)→setVertexBuffer(_normal,2)→setIndexBuffer(_ebo)→setUniform(projection/view/model/time)→drawIndexed，保留 model 计算+static curTime time uniform；GLNormalLine：共享同一 Sphere 几何 Layout{0,2}（NormalLine.vs aNormal@2 必须），_normalPipeline 三段/_pipeline 两段，_pipeline setPolygonMode(Line)，drawScene 两段铁序 | build OK+全量46/46 OK+Explode/NormalLine各自单独1/1 OK；只改4文件；偏差=Explode 也显式传 Layout{0,2}（brief 代码块无 layout→默认@3，文字要求@2；gs 用叉积不读顶点法线，@2/@3 无害，为对齐文字显式传） |
| 2026-08-12 | Phase G Task 11 GLSimpleGemoteryApp 迁 RHI（Points+geometry shader 首验） | 重构 | f06a5b5：GLSimpleGemoteryApp 从 native GL（GLProgram/glad/裸缓冲）迁 RHI。hpp 删 GLProgram/_vbo/_vao/createVertexBuffer 声明，program→_pipeline、加 _vb/_vertexCount。cpp 三段 compile（Base.vs/Base.fs/Base.gs Vertex+Fragment+Geometry）；points 数组照抄源 46-51（4 点 stride5=Float2 pos+Float3 color）；layout 两元素 Float2@0 offset0/Float3@1 offset 2*sizeof(float)，stride 均 5*sizeof(float)；RhiGeometry::CreateFromArray(renderer,points,sizeof,4,layout)；createPipeline(geo.layout,shader)+setPrimitiveType(PrimitiveType::Points)；drawScene 铁序 setPipeline→setVertexBuffer(_vb)→draw(_vertexCount,0)，末尾 GLApp::drawScene；析构空体；源 hpp 无 beginDrawScene | build OK+全量46/46 OK+SimpleGeometry单独1/1 OK（Points 首验通过）；只改2文件；无功能偏差 |
| 2026-08-12 | Phase G Task 12 GLMultieInstanceApp+GLSaturnApp 迁 RHI（instancing+Mesh 扩展+清偿 Saturn 债） | 重构 | 1196bc4：Mesh.hpp public 段加 vertexBuffer()/indexBuffer() 只读访问器；GLMultieInstanceApp 删 GLProgram/_vbo/_vao/_ebo/_positionVbo/createVertexBuffer，_program→_pipeline，RhiGeometry::Create(sphere,false,true,true,Layout{0,2})（normal@2 避免与 aOffset@3 冲突）+ createObjectPositions(renderer,count) 生成 vec2[count*count] 实例偏移→_instanceVb + layout 追加 {Float2,3,3,PerInstance,0,8} + setPolygonMode(Line) + 铁序 drawIndexedInstanced(_indexCount,count*count)；GLSaturnApp 删 raw VAO/GLProgram/GenerateRockPoisitonBuffer，加 _rockPipeline/_instanceBuffer/generateRockInstanceBuffer(GenerateRocksPosition→createBuffer init Vertex)，loadModel 内 rock 改 _instanceBuffer+编译 Rock pipeline（layout pos0/normal1/uv2 显式不含 tangent/bone+4×Float4 实例矩阵 loc3-6 binding3 offset0/16/32/48 stride64）+setDepthTest(true)，drawScene rock 段循环 bindTexture+setUniform(type+"1")+Mesh 访问器组装 drawIndexedInstanced | build OK+全量46/46 OK+MultiInstance/MultiInstance_Saturn 各单独1/1 OK；只改5文件；无 functional 偏差（createObjectPositions 改为带 renderer 参数并前移定义，返回 shared_ptr<IBuffer>） |
| 2026-08-12 | Phase G Task 12 Round1 修复（评审 Important：实例缓冲尺寸不匹配） | 修复 | ba5a837：createObjectPositions 循环 count=10 实际生成 9×9=81 项，init 只分配 81，而 drawIndexedInstanced 绘 count*count=100 实例→实例81-99 GPU OOB 读（原生 glBufferData 分配满 100 无 OOB，迁移回归）。修复=padded=translations 后 resize(count*count) 再 init，仅改 createObjectPositions 一处 | build OK+全量46/46 OK+MultiInstance/MultiInstance_Saturn 各单独1/1 OK；无 concern |
| 2026-08-12 | Phase G Task 12 Round1 复评审（scoped，仅看 1196bc4..ba5a837 diff） | 评审 | 唯一 Important（实例缓冲 81 vs 绘制 100 → GPU OOB 读）判定 ADDRESSED：createObjectPositions 现 padded=translations 后 resize(count*count) 再 init，count=10 分配 100 槽位，与 drawIndexedInstanced(_count*_count) 匹配，OOB 消除。无新破坏（resize 后 data() 有效、size*sizeof 一致、默认值填充槽位 0,0 仅视觉叠原点无崩溃、未触其他代码）。最终文件状态正确。Verdict：all findings addressed。out-of-scope deferred minor=补齐 19 槽实例 offset{0,0} 与首实例重叠，仅视觉冗余无正确性影响，不处理 | 无需重跑红线 |
| 2026-08-12 | Phase G 终审遗留清理（死声明） | 清理 | 0700792：删 GLMsaaApp.hpp:23-24 死声明 createVertexBuffer/createScreenBuffer（.cpp 定义迁移时已删、无调用方、非 virtual，唯一残留不一致；其余 App 同类声明均已在迁移时清理） | build OK（仅既有窄化 warning）；仅改 1 文件 2 行 |
| 2026-08-12 | Phase G 实施完成（13 App 全量迁移 RHI） | 实现 | 14 提交（498db12..0700792）：T1 7f15308(G1 setPointSizeProgramEnable+GLAdvancedGLSL)→T2 8d0123d(DepthTest)→T3 105d1f4(Blend)→T4 a465156(CullFace)→T5 9eb7546(Skybox)→T6 ddb600a(TemplateTest,修复纹理绑定)→T7 a757d09(UniformBuffer UBO)→T8 312bf00(FrameBuffer)→T9 c9d7458(Msaa)→T10 1c67f72(Explode+NormalLine)→T11 f06a5b5(SimpleGemotery Points)→T12 ba5a837(MultiInstance+Saturn instancing+Mesh 扩展+清偿 Saturn 债)→0700792(清死声明)。每任务独立评审，T6/T12 各 1 轮修复 | 终审（全分支 165KB diff）Verdict=Ready to merge Yes，无 Critical/Important；红线 build OK+全量 46/46 OK+各 App 冒烟；累计迁移 44 App，剩余 0。Important 观察(deferred)=TemplateTest _borderPipeline 依赖全局 stencil 状态泄漏(per-pipeline 化时需显式 setStencilTest)。Minor 全非阻塞(BLEND cube unit0/sampler1 错配=原生固有忠实保留、MultiInstance padding=消除原生未初始化内存的改进)。plan workspace 已删除 |
| 2026-08-13 | Vulkan 后端 + 统一 UBO 任务：spec 编写并获批准 | 计划 | docs/superpowers/specs/2026-08-13-vulkan-backend-unified-ubo.md：统一 UniformBlock（ULight 6×vec4 + 池分配 vec4Pool/vec3Pool/floatPool/extraMat4 + lights[256]）、GL/VK 采样器约定（VK binding=unit+1 ≤15）、VK 后端以 Vulkan 1.3 core dynamic state 实现 IRenderer 全接口 | 用户确认方案（"方案咩问题继续"）；spec 自审修订 GL/VK binding 冲突、std140 vec3 陷阱、灯光结构统一 |
| 2026-08-13 | Vulkan 后端 + 统一 UBO 任务：实施计划编写 | 计划 | docs/superpowers/plans/2026-08-13-vulkan-backend-unified-ubo.md：12 Task（T1 CMake 接线 / T2 UniformBlock 核心+槽位表+SetUniform OK：SetLight 分派到 lights[i]+官方池分配 / T3 GL 模板 / T4 VK 骨架 / T5 VK Buffer+Shader+Pipeline+descriptor 动态状态 / T6 纹理+RT+命令全集 / T7 VK App 前端+ImGui 降级+工厂 / T8 shader 双树脚本 / T9a-h 46 App 分批迁移 / T10 PointLightShadow 6-face / T11 M3 收尾 / T12 文档）+Self-Review+Execution Handoff | 三路并行探索（RHI 接口+GL 后端、46 App 注册链、构建系统）确认事实后落笔；约1360行 |
| 2026-08-13 | Vulkan 后端 + 统一 UBO 任务：计划自审修正 | 计划 | 修正：glslang -G→-V；Task 2 全文重写（方向光 w=1、outerCutOff→direction.w、lights[i].Position 统一 SetLight）；Task 3 精简为仅模板文件；Task 8 幂等脚本重写（后缀映射+gl_Layer 标记）；descriptor 采样器 7→15（PBR IBL 上限 8）；find_package(Vulkan) 移入 if(ENABLE_VULKAN)；shadowMatrices→extraMat4[1+i] 对齐槽表；清理 updateUBO/GLApp._uboBuffer 归属残留；Task 7 补 Application.cpp m_imguiWindow 判空 Step 2.5 | 全文一致性 grep 通过；计划就绪待评审 |
| 2026-08-13 | Vulkan 后端计划 Task 1 CMake ENABLE_VULKAN 完整接线 | 构建 | 526d248：src/CMakeLists.txt 加完整 ENABLE_VULKAN 块（宏+glslangValidator .spv 编译+vk_shaders 目标+find_package(Vulkan)+Vulkan::Vulkan 链接）、rhi/app CMakeLists 追加 Vulkan glob、StaticCollector getVulkanShaderPath、build_run.sh 双后端 | build OK（find_package+glslangValidator 命中）+全量 46/46 OK；grep 计数 5>=4。偏差A：brief StaticCollector.cpp 片段显式限定+裸 join 编译失败，改为既有风格 FileUtils::join（头文件逐字）。偏差B（必要）：3party/imgui/CMakeLists 的 ENABLE_VULKAN 分支自动收录过时 imgui_impl_vulkan.cpp（ImDrawCmd::TextureId vcpkg 新版已改 TexId）致编译失败，按计划"不接入 ImGui VK 后端"决策删除该分支+注释，文件超出 brief 清单已并入 commit。偏差C：构建目录 IMGUI_SRC_FILES cache 累积旧值，--fresh 清理解决 |
| 2026-08-13 | Vulkan 计划 Task 2 统一 UniformBlock 核心 | 实现 | 1237520：src/rhi/core/UniformBlock.{hpp,cpp}（ULight 6×vec4+UniformBlock 池分配+SlotKind/SlotInfo+56 槽 kSlots 全 offsetof 无魔数+FindSlot 单参+12 SetUniform 重载+WriteAt 支持 [N] 与数组下标+SetLight/SetLightParam demux+SLOT_ENABLE_CHECKS 断言） | brief 3 处矛盾全裁定执行（补 SetLightParam 声明/删双参 FindSlot/补 cpp include）；**新增必要修正**：brief cpp 片段全局作用域无法编译（hpp 全在 namespace rhi）→ 整个 cpp 移入 namespace rhi；独立发现 brief 验证 shadowMatrices 断言双重 off-by-one（base XM4(1)+index*b 语义下 index=1→extraMat4[2] 非[1]，且 vec4(0,0,0,3) 分量落 [0][3] 非 [0][0]），实测原式断言失败，采用等价修正（0u+[0][3]）通过；验证 -W 无警告+SLOT_ENABLE_CHECKS OK+全量 46/46 OK |
| 2026-08-13 | Task2 评审修复 round 1（ULight direction 语义） | 修复 | b674780：SetLight("direction") 改存 direction.xyz（不再写 position、不碰 w），SetLightParam 新增 "type"→position.w（方向光标志显式设） | 方向与 position 解耦，聚光灯 position+direction 可并存；冒烟测试 ALL_ASSERTS_PASS + -Wall 无警告 |
| 2026-08-13 | Task4 VK 后端骨架（instance/device/surface/swapchain） | 实现 | fe0e696：src/rhi/vk/ 5 文件。VKHeader(RAII+NO_EXCEPTIONS 统一 include)；VKBackend 全 IRenderer 接口(init/shutdown/present 真实现, 工厂 stub)；VKSwapchain(swapchain+images+views+acquire/present)。物理设备 favorIntegrated+swapchain；设备扩展 VK_KHR_swapchain；格式 B8G8R8A8 UNORM+SRGB、presentMode MAILBOX/FIFO、imageCount 3 | build OK+独立 instance 冒烟 ok+GL 46/46 OK；端到端被容器缺 xlib/xcb surface 扩展阻断(留 Task7)；偏差=不建 VKSurface.hpp、beginFrame/endFrame 按真实接口 void、dynamicRendering 特性留 Task5/6 |
| 2026-08-13 | Vulkan Task5 VK Buffer/Shader/Pipeline + descriptor set + 命令录制 | 实现 | bda4197：VKBuffer(staging 上传/host-visible UBO)、VKShader(.spv→module)、VKPipeline(descriptor layout/池/set、pipeline layout、动态状态、lazy create、顶点装配)、命令录制(beginFrame acquire+cmd、即时 vkCmd、draw→endFrame submit→present)、同步对象(imageReady/rendered sem+frameFence 接上)；VKSwapchain acquire(sem)/present(waitSem)/imageView；VKTexture2D/3D 骨架占位 | build OK+**headless 单测 18 断言 ALL_ASSERTS_PASS**（VKBuffer uniform/vertex/index staging 上传+VKShader .spv module，真实 GPU 驱动）+GL 46/46 OK。偏差：brief 声称 polygon/blend 可作 1.3 core 动态状态有误（属 EXT_dynamic_state3），动态状态收敛为 1.3 core 12 项（viewPort/scissor/depth/cull/frontFace/stencil），polygon/blend 改静态+变化 lazy recreate；Texture 按指引做基础骨架；UBO 登记用首选方案（App buffer 即 backend 目标） |
| 2026-08-13 | Vulkan Task5 评审修复 fix round 1（bindVertexBuffers 空槽/UBO 悬垂/staging pool 复用） | 修复 | dee6efc：1) bindVertexBuffers 改按实际非空 binding 连续段分组、每段真实 firstBinding 绑定，缺位 slot 跳过不绑 null（binding0+2→两次 bindVertexBuffers(0)+bindVertexBuffers(2)，offs 逐段对齐）2) shutdown 置空 _uboBuffer 已存在，补生命周期契约注释（App 须保 buffer 存活，VK 仅登记不持所有权）3) VKBuffer uploadStaging 惰性创建一次复用成员 transient pool，command buffer 每次现分配提交 waitIdle 后 RAII 归还（无需 reset） | build OK+headless 21 [OK] 0 FAIL ALL_ASSERTS_PASS（重编译 6 vk 文件）+GL 46/46 OK+-Wall -Wextra 无新警告；其余 Minor（multisample e1/pointSize 死状态/blend alpha/vertexOffset int32）记报告不修；空槽端到端需窗口留 Task7 |
| 2026-08-13 | Vulkan Task6 VK Texture/RT 真实实现 + 命令全集 | 实现 | d78f86a：VKTexture2D 真实(staging上传+barrier+sampler+view+genMips+adopt 非拥有)；VKTexture3D initCube(6面独立2D view+cubeView+6-layer mip)、createEmpty 深度cube(depthCubeView)；VKRenderTarget(多附件 renderpass/framebuffer+MSAA resolve attachment+attachCubeFace/depthCube+colorTexture2D/depthTexture2D adopt 包装)；VKBackend 补 setRenderTarget/bindTexture(x3→descriptor binding=unit+1)/blitFramebuffer(vkCmdBlitImage)/ensureRenderPass 按RT/附件clear values/backendCapabilities(8/65536)/工厂；新增 VKFormat.hpp+VKUpload.hpp | build OK+headless 34 断言 ALL_ASSERTS_PASS(真实驱动: 2D上传/mip链/createEmpty/initCube/depth cube/多附件RT/MSAA)+GL 46/46 OK+-Wall -Wextra 无新警告；偏差=cubemap 用 e2D array(非 brief e3D)、RGB16F→RGBA16F 4分量、resolveTo no-op(VK 由 resolve attachment 完成)；concern=**VKPipeline 固定 swapchain renderpass/format，setRenderTarget 到 RT 后 pipeline 不兼容(无法端到端渲染 RT)，重大架构 gap 留 Task7**、MSAA pipeline rasterizationSamples 仍 e1(未接上)、blit 深度 mask 空 |
| 2026-08-13 | Vulkan Task6 评审修复 fix round 1（5 finding） | 修复 | 0f7e4c5：1) attachDepthCube 重写接 cube depthCubeView（_cubeDepth 成员+重建 renderPass/framebuffer，颜色RT也加深度、纯深度cube layers=6）2) ensureRenderPass clear 按 [color]xN+[resolve]xN+[depth] 真实附件序（MSAA depth 索引正确，resolve 用 DontCare）3) 非MSAA颜色+深度 finalLayout=eShaderReadOnlyOptimal 可采样 4) VKPipeline 改 pipelineFor(rp,samples) 按当前RT renderpass 惰性缓存+MSAA rasterizationSamples 接入（架构 gap 解决）5) blitFramebuffer src oldLayout=eShaderReadOnlyOptimal | build OK+headless 50 断言 ALL_ASSERTS_PASS（真实驱动，原34+新16）+GL 46/46 OK+-Wall -Wextra 三文件无警告；Minor 未修（mipmap首barrier/resolveTo no-op/blit swapchain回写/RGB16F尺寸）记报告
| 2026-08-13 | fix round1 re-review task6: 5 findings(1C+4I) 全部 ADDRESSED, 无新增 Critical/Important, 3 minor 观察 | review | VK attachDepthCube/MSAA clear/finalLayout/pipeline-per-RT/blit layout 修复验证 | 输出见评语 |

| 2026-08-13 | Vulkan Task7 App 前端（NO_API 窗口/ImGui 降级/工厂接线/shader 路径后端感知） | 实现 | 105886e：方案(a) GLApp::initGraphics 后端感知（vulkan 标记走 VK 分支：不 initGLContext+复用 GLFWSurface+createVKRenderer+不建 ImGui；GL 分支不变）；GLFWWindowProperties 加 vulkan 字段+GLFW_NO_API；Application::render 与 GLApp::drawScene 对 m_imguiWindow 判空；Common.hpp 加 BackendKind+setBackendKind/backendKind；getGLShaderPath 按 backend 返回 Vulkan/GL；AppFactory 加 Vulkan case+新建 src/app/Vulkan/VKAppFactory(委托 GLAppFactory 复用 GL App)；main 加 vulkan 注册+props.vulkan+init 失败 return 1 | build OK(无新警告)+全量 46/46 OK；`-b vulkan -a Triangle` 窗口创建成功、VK init 因容器缺 surface 扩展返回 false 被拦截、干净 exit 1 无 segfault（符合不崩）。偏差=未建 VKApplication(方案a 冗余)、VKAppFactory 名对齐 Step3、main 补 init 返回值处理 |
| 2026-08-13 | Vulkan Task8 shader 双树生成脚本 + res/Vulkan 编译验证 | 实现 | 5b0b085：tools/gen_vk_shaders.py（纯 stdlib 幂等镜像 res/GL→res/Vulkan：后缀规范化 vs→vert/fs→frag/gs→geom、330/430→450、UniformBlock binding→set0+binding0、采样器按声明序 binding=1..、gl_Layer 注释、Common/UniformBlock.glsl 改写非编译目标；输出 sampler_binding_map.txt）生成 172 shader+2 附件 | build OK（vk_shaders 51 个 .spv，SPIR-V1.6 合法）+GL 回归 46/46 OK。**重大偏差**（brief 前提不成立）：①Task1 CMake 剥扩展名致 .vert/.frag 同基名→.spv 冲突 configure 崩，改 stage-qualified `X.vert.spv/X.frag.spv/X.geom.spv` ②缺 location 补 --auto-map-locations ③pre-Task9 裸 uniform（非 block）致 121/172 Vulkan 编译失败，CMake 用内容正则精确跳过（0 误 0 漏），自维护待 Task9 包裹后自动编译。gl_Layer 已注释（ShadowMappingDepth.geom 因裸 uniform 现也在跳过清单）。51 个成功编译 |
| 2026-08-13 | Vulkan Task9a Basic6 批量迁移 UBO + shader 升级（首个模板） | 实现 | a5c096b：5 App（Triangle/Rect/SimpleTexture/Cube/Camera）+2 hpp 加 _ubo/_uboBuffer+initApp UBO 创建+bindRange，Cube/Camera 完整 SetUniform 迁移（model/view/projection），Triangle/Rect/SimpleTexture 固定管线仅 UBO 框架+每帧 update；Cube.vert/.frag+SimpleTexture 330→430+完整 UniformBlock 声明（删命名 uniform）；CameraBase 无 shader 零改动；重跑 gen_vk_shaders 刷新 VK 镜像 | build OK（51+ .spv）+Cube单独1/1 OK+全量46/46 OK+Base组5/5；偏差=struct ULight 移出 block 外声明（VK glslang 禁止嵌套 struct，必要）、triangle/rect 已430无uniform不改、无需 bindUniformBlock(0)（shader 显式 layout(binding=0)） |

| 2026-08-13 | Vulkan Task9c LightSource4 批量迁移 UBO + shader 升级 | 实现 | ce26337：4 App（Direction/Point/Spot/Mult）加 _ubo/_uboBuffer+initApp UBO 创建；结构体灯光按 brief 9c 映射：dirLight→SetLight(ubo,0,direction/ambient/diffuse/specular)+type=1、pointLights[i]→SetLight(ubo,i+1,position/...)+SetLightParam(constant/linear/quadratic)、spotLight→SetLight(ubo,5,...)+cutOff→params.w/outerCutOff→direction.w、viewPos→SetUniform；Mult 静态 Set*Light 函数改 rhi::UniformBlock&；采样器 setUniform 行删除靠 bindTexture；shader 升级 430+block 外 ULight+UniformBlock，灯光 struct 改 lights[N]（单灯=1，Mult=6），CalcDir/Point/SpotLight 就地取 lights[i] 字段；重跑 gen_vk_shaders 刷新 VK 镜像 | build OK+Mult单独1/1 OK+全量46/46 OK+VK 172 shader 刷新；偏差=lights[N] 采 brief 指定 N（非 Batch1 的 256，末字段数组长度不影响前置偏移）；concern=双纹理 specularMap 无 binding 退化为 unit0 读取 diffuse（已知限制，9d 前统一修复，已记录） |
| 2026-08-13 | Vulkan Task9d Advanced10 批量迁移 UBO + shader 升级 | 实现 | 8ad1cae：9 App（DepthTest/TemplateTest/Blend/CullFace/FrameBuffer/Skybox/AdvancedGLSL/Msaa/SimpleGeometry）加 _ubo/_uboBuffer+UBO 框架，projection/view/model→SetUniform、texColor→vec4Pool[6]、effectType int→floatPool[15]（shader 改 float>0.5 链）、cameraPos→vec4Pool[1]、enableReflection/Refraction→floatPool[36/37]、AdvancedGLSL 2 个 enable bool 依赖新槽 enablePointSize→F4(43)+enableFrontFaceCulling→F4(44)（UniformBlock.cpp 增量）；采样器 setUniform 行删+显式 layout(binding=unit)，plane/screen 纹理改绑 unit0 保对象映射；shader 升级 430+block 外 ULight+lights[1]+按 unit binding；SimpleGeometry 仅 330→430（无 uniform，inert UBO 框架）；**GLUniformBufferApp 判定传统 UBO demo 保留原样**（block 名 Matrices 独立于槽表）；gen_vk_shaders.py 加 gl_VertexID→gl_VertexIndex 转换（VK GLSL 必须）；重跑脚本刷新 VK 镜像 | build OK（仅既有 Msaa 窄化 warning）+全量 46/46 OK+9 App 冒烟干净；偏差=新增 2 槽+重绑 unit0+脚本 token 转换（详见 report）；VK 172 shader 全编译成功 |
| 2026-08-13 | Task9d Advanced10 批量迁移代码评审（scoped，bd68992..8ad1cae） | 评审 | 8 核对点全过：新增槽 F4(43)/F4(44) 落预留 free 区无冲突（UniformBlock.cpp:97-98）+注释同步；gl_VertexID→gl_VertexIndex 仅 VK 镜像（gen_vk_shaders.py:40，GL 源 Cube.vert:35 保持 ID）；采样器 binding=unit 与 bindTexture 全对齐（Blend 逐 draw 重绑 unit0 对象映射逐字节等价含 swap 怪癖）；27 shader 全 430+block 外 ULight+lights[1]+binding+float>0.5；effectType 阈值链等价 switch；9 App 迁移模式一致；VK binding=unit+1 三处闭合（脚本+backend+map）；GLUniformBufferApp 保留成立（block 名 Matrices 传统 demo）。无 Critical/Important；Minor×5=TemplateTest:88 遗留 bindTexture(_planeTexture,1) 死代码、CullFace.frag 引入无必要 texColor 双分支（行为等价）、脚本全局无条件替换建议限定顶点阶段、报告"27对"措辞、Post/Screen.vert 冗余 block | 验证声明依赖报告未重跑（评审只读）；Task quality Approved |
| 2026-08-14 | Vulkan Task9e Geometry/Instance/Model 5 App UBO 化 + shader 升级 | 重构 | 69a2652：5 App（Explode/NormalLine/MultiInstance/Saturn/LoadModel）setUniform→`_ubo`+`rhi::SetUniform` 显式 UBO（各 App 局部成员），16 对 shader 升 430+UniformBlock（含 2 个 geometry shader .gs 430+block：Explode.gs 用 floatPool[10] 读 time、NormalLine.gs block projection）。槽位：projection/view/model 常驻、time→floatPool[10]（复用）、count→floatPool[14]（复用，Sphere.vs 原声明未用）、**radiusPos→新增 vec4Pool[45]**（vec3Pool 全区被 SSAO 独占不可侵，落 vec4Pool 自由区 45，计划注释 45..63=自由）；Saturn Rock 采样器 setUniform 行删+binding=0（单纹理 unit0）；**gen_vk_shaders.py 补 gl_InstanceID→gl_InstanceIndex**（Sphere.vert 首用，VK 必须，GL 源保留 ID）；instanced 顶点属性/layout/Mesh 不动；重跑脚本刷新 VK 镜像 | build OK+vk_shaders 全编译+全量 46/46 OK+5 App 各自单独 OK（stderr 0 error/0 GL_INVALID/0 failed）；偏差=新增 radiusPos 槽+脚本 token 转换一行（详见 task-9e-report.md） |
| 2026-08-14 | Vulkan Task9f forward7 批 UBO 化 + shader 升级 | 重构 | a5cdf31：7 App（BlinnPhong/NormalMap/ParallaxMap/ShadowMap/Shadow/PointLightShadow/Gamma）setUniform→显式 UBO。槽位：lightPos→vec4Pool[2]/lightColor→[3]；**新增槽 enableNM F4(22)/enableDisp F4(23)/enableOcclusion F4(24)/enableBias F4(25)/reverse_normals F4(34) 别名/light F4(45)**（自由区）；**新增索引 mat4 SetUniform 重载**（shadowMatrices[i]→extraMat4[1+i]）；type→F4(15)、debug→F4(18)、near/far→F4(16/17)、shadows→F4(19)、enableSimplePCF→F4(40)、lightSpaceMatrix→extraMat4[0]、lightPositions/lightColors→vec4Pool[13+i]/[29+i]、enableGamma→F4(20)、gammaValue→F4(5)、reverse_normals 按 type 分支、light bool 按 type 分支；重跑 gen_vk_shaders 刷新 VK 镜像 | build OK+vk_shaders 全编译+全量 46/46 OK（详见 task-9fg-report.md） |
| 2026-08-14 | Vulkan Task9g post4 批 UBO 化 + shader 升级 | 重构 | c9650ef：4 App（Hdr/Bloom/Defer/SSAO）setUniform→显式 UBO。Hdr/Bloom/Defer 灯光用 SetLight(_ubo,i,"Position"/"Color")→lights[i].position/.diffuse；Linear/Quadratic→params.y/.z；**SetLightParam "Radius" 改存 direction.w**（避免与 quadratic 冲突）；**新增槽 enableVolume F4(46)**；hdr→F4(13)/bloom→F4(11)/horizontal→F4(12)/exposure→F4(4)；Defer 169 灯+Light.fs lights[256]；SSAO samples[i]→vec3Pool[i](64)+投影+单灯 lights[0]+enableSSAO→F4(39)；**修复 Bloom VS/FS lights 数组大小不一致（VS lights[2] vs FS lights[4]→GL 链接错误，VS 改 [4]）+ SSAO.fs TBN*vec3Pool[i] 补 .xyz**；重跑 gen_vk_shaders | build OK+vk_shaders 全编译+glslangValidator 全 GL shader 通过+全量 46/46 OK（详见 task-9fg-report.md） |
| 2026-08-14 | Task9f/9g 评审 2 Critical 修复 | 修复 | 312bf8f：①SetLightParam 加大写别名 "Linear"/"Quadratic"（Defer/SSAO 传大写老 GL 名，strcmp 大小写敏感致 params.y/z 静默丢弃→光衰减恒 1.0；现与 SetLight "Position"/"Color" 别名惯例一致）②Hdr/Lighting.fs（+VK 镜像）循环上界 i<16→i<4（lights[4] 越界读；修 GL 源后重跑 gen_vk_shaders 幂等刷新 VK） | build OK+全量 46/46 OK+断言 ALL_ASSERTS_PASS（混合大小写 SetLightParam 写入一致）+git diff 仅 3 文件；报告 task-9fg-fix-report.md |
| 2026-08-15 | Vulkan Task9h PBR/IBL 5 App UBO 化 + shader 升级 | 重构 | 5 App（PBR_Base/PBR_Texture/IBL_IC/IBL_Irradiance/IBL_Specular）setUniform→`_ubo`+`rhi::SetUniform` 显式 UBO（各 App 局部成员），14 对 shader 升 430+UniformBlock。槽位：albedo→**新增 vec4Pool[10]**（PBR 材质基础色，10..12 预留区）、metallic/roughness/ao→floatPool[7/8/9]（复用）、camPos→vec4Pool[1]、lightPositions/lightColors→vec4Pool[13+i]/[29+i]（复用）；采样器 binding=unit+显式 layout（IC env equirectangularMap=0、Irradiance/Specular 各阶段 cubemap=0、Specular PBR irradiance=0/prefilter=1/brdfLUT=2）；Prefilter.vs/fs 升 430+block（roughness 读 floatPool[8]），Brdf.vs/fs 无 uniform 保持 330；renderCube/renderSphere 每次 draw 前 `_uboBuffer->update`；IC/Irradiance/Specular viewport 恢复窗口尺寸加 static_cast<int> 消窄化 warning；重跑 gen_vk_shaders 刷新 VK 镜像 | build OK+vk_shaders 全编译+全量 5/5 OK（-a PBR_Base/Texture/IBL_IC/IBL_Irradiance/IBL_Specular 各 4s 冒烟干净）+cpp 无残留 setUniform+grep 确认 PBR 全部 shader（除 Brdf）含 UniformBlock；偏差=新增 albedo 槽 vec4Pool[10]+Prefilter 从 floatPool 读 roughness（对齐 9h slot 表）；详见 task-9h-report.md |
| 2026-08-15 | Vulkan Task10 PointLightShadow 6-face pass（GL+VK 统一）+ VK cubeface framebuffer | 重构 | 79df2be：深度 pass 弃 GS（删 ShadowMappingDepth.gs/.geom），改 **6 面循环**：每面 `attachCubeFace(depthCube, face, 0)` + clear + 固定矩阵槽 `SetUniform("shadowMatrices",0,m)→extraMat4[1]` + renderScene（面 i+1 前先 `setRenderTarget(fbo)` 结束 VK render pass，销毁 framebuffer 时无活动 pass）。**深度 VS** 直接 `gl_Position = extraMat4[1] * (model*aPos)` + 输出 FragPos；**深度 FS 修复 9f 潜伏 bug**（裸 lightPos/far_plane 从未被设→除 0，改读 vec4Pool[2].xyz/floatPool[17] 同 ShadowMapping.fs 槽）。RHI：GL attachCubeFace 按 cube 格式 Depth32F 挂 DEPTH_ATTACHMENT+drawBuffer(NONE)（GLTexture3D 加 desc() 访问器）；VK attachCubeFace 支持 _cubeDepth 单面深度 view framebuffer（layers=1）；compileShader 去 Geometry 段。重跑 gen_vk_shaders（171，删 geom 镜像） | build OK（仅既有窄化 warning）+vk_shaders 全编译+vk_spv 清残留 geom+全量 46/46 OK（首跑 1 瞬态 FAIL，重跑 2 次均 46/46）+Shadow_PointLight/PBR_IBL_Irradiance/Specular 单独 OK（IBL 颜色路径 attachCubeFace 回归）；commit 并入 3 RHI 文件（GLTexture3D.hpp/GLRenderTarget.cpp/VKRenderTarget.cpp，brief 清单外为任务必需）
| 2026-08-15 | Task10 评审修复（C1 GL FBO 不完整 + I2 VK depth cube eSampled） | 修复 | bc8b915：C1=GLRenderTarget 的 create(width,height) 残留 DEPTH24_STENCIL8 rbo 挂 GL_DEPTH_STENCIL_ATTACHMENT，attachDepthCube/attachCubeFace(depth) 只挂 GL_DEPTH_ATTACHMENT 从不 detach rbo→深度+深度-模板指向不同 image→FBO 必不完整(0x8da8/0x8cdd)→GL 静默 no-op→depth cubemap 不渲染→无阴影；修复=两处挂 depth cube 前 `glFramebufferRenderbuffer(GL_DEPTH_STENCIL_ATTACHMENT,GL_RENDERBUFFER,0)` detach rbo（最小正确，普通 RT=create(fbd) 非空 attachments 走深度 texture 路径不受影响）。I2=VKTexture3D::createEmpty 深度 cube usage 补 `eSampled`（主 pass 采 depthMap） | build OK+全量 46/46 OK+Shadow_PointLight 单独 OK；FBO COMPLETE 验证=临时诊断 glCheckFramebufferStatus：渲染路径 attachCubeFace(depth) 2351×status=0x8cd5(COMPLETE)，attachDepthCube 0x8da8(LAYER_TARGETS 仅 init 瞬时无 draw，color 基座暂挂)；0 [error]；诊断已移除；报告 .superpowers/sdd/2026-08-13-vulkan-backend-unified-ubo/task-10-fix-report.md
| 2026-08-15 | Vulkan Task11 M3 收尾（.spv 全量核对 + 全 App VK 冒烟 + GL 回归） | 收尾 | 7260736：**修 1 处 shader**=Bloom Blur.fs 常量高斯核误声明为裸 `uniform float weight[5]` 致被 CMake 跳过正则排除不产 .spv（非有意保留），改 `const` 对齐 LearnOpenGL 上游（GL 从不 set 该 uniform，语义不变）→ 重跑 gen_vk_shaders（幂等仅刷新 Blur.frag）→ Blur.frag.spv 编译成功。**.spv 编译=169/171**，仅剩 2 个有意保留 demo shader（UniformBuffer Matrices block 无 binding+裸 uniform，已 glslang 验证 VK 编译失败成立，契约允许记录不强改）；CMake 排除后仅此 2 项 | build 零 error rc=0+GL 回归 46/46 OK+**VK 冒烟 46/46 全部 exit=1 干净失败（窗口创建成功→glfwGetRequiredInstanceExtensions 失败被拦截→return 1），0 segfault 0 hang**；concern=UniformBuffer 不产 .spv（真机端到端时该 App 加载失败，有意保留）、CMake 内容正则脆弱（Blur 即误伤，建议改显式排除清单）、VK 端到端须真机验证（环境限制）；报告 task-11-report.md |
| 2026-08-15 | **Task 12 文档收口 + Mesh.cpp 采样器清理（最终任务）** | 收尾 | ede9624：删除 src/model/Mesh.cpp:33 `pipeline->setUniform(name+number,(int)i)` 及上注释（block shader 下静默 no-op，LoadModel 单纹理 binding0 与 bindTexture(unit0) 恰好一致才正常），保留 `renderer->bindTexture(textures[i].texture,i)`；spec 加「状态：已实现」节（M0-M3 达成+采样器约定+direction 语义+环境限制）；plan 65 个 checkbox 全打勾 + 追加「最终结果」节（commit 列表+已知限制）；PROGRESS.md 本记录 | build OK+全量 46/46 OK；VK 冒烟抽验 Triangle/Shadow_PointLight/PBR_IBL_Specular 各 exit=1 干净失败无 segfault；spec/plan 不提交（docs/ gitignored，git status 确认不出现）；报告 task-12-report.md |
| 2026-08-15 | **Vulkan 后端 + UBO 迁移最终里程碑** | 里程碑 | **M2/M3 达成**：46 App 全 UBO 化（9 批 9a-9h，setUniform→SetUniform/SetLight/SetLightParam 统一槽表）；GL shader 升级 430+UniformBlock；VK 后端 src/rhi/vk/ 完整实现（动态状态+descriptor+RT/cubemap/MSAA）；.spv 169/171；**双后端验证**=GL 46/46 OK + VK 冒烟 46/46 干净 exit=1（窗口创建成功→init 失败拦截→return 1，0 segfault 0 hang） | **环境限制**：容器缺 VK surface 扩展（VK_KHR_xlib/xcb_surface），VK 端到端不出帧，须真机验证；UniformBuffer demo 不产 .spv（有意保留）；CMake 内容正则脆弱待改显式排除清单。全部 12 Task 完成，计划收口 |

| 2026-08-15 | Vulkan 后端 + 46 App UBO 迁移完成 | 里程碑 | M0-M3 全达成：完整 VK 后端(src/rhi/vk/)、46 App 全 UBO 化(GL shader 升 430+block)、res/Vulkan 镜像 .spv 169/171 编译、GL 46/46、VK 冒烟 46 App exit=1 不崩(容器缺 surface 扩展, 端到端待真机)。commit 链 526d248..ede9624 | SDD 执行 12 Task 全评审通过；采样器 GL binding=unit/VK=unit+1；direction 语义重设计 |

| 2026-08-15 | 排查 VK 无法运行根因 | 调试 | 根因：CMake find_package(Vulkan) 命中 vcpkg vulkan loader(debug/lib/libvulkan.so.1.4.357)，其构建不含窗口 surface 扩展(无 xcb/xlib/wayland_surface)，glfw dlopen("libvulkan.so.1") 复用该 loader → glfwGetRequiredInstanceExtensions 返回 NULL → VKRenderer::init 失败。系统 loader(26 扩展)正常。方案：A 用系统 loader(推荐)/B 重建 vcpkg loader 开 surface feature | 用 gdb 确认 _glfw.vk.KHR_xcb_surface=false；用 enumext2.c 对比两 loader 实例扩展 |

| 2026-08-15 | VK 无法运行根因修复(方案A) | Bug修复 | 换系统 libvulkan 链接后 VK 真跑通：45/46 OK(仅传统 UniformBuffer 330 demo 预期排除)。连环修复 4 个 bug：①vcpkg loader 缺窗口 surface 扩展→CMake 改链系统 libvulkan.so.1；②VK 复用 GL App 但 ImGui 无 context→新增 ImGuiContextWindow(仅 context+预建字体+DisplaySize/DeltaTime)；③RGB3 通道图片按 RGBA8 计算上传 size 越界读→Load2D 强制转 4 通道；④ExitIfFailed assert(0) abort→改 log+exit(-1)。commit b3c1f76 | VK 端到端首次真正出帧渲染；GL 46/46 无回归 |
| 2026-08-15 | Vulkan 黑屏/崩溃综合调试 | 调试 | llvmpipe 软件渲染下逐 App 画面 dump(RHI_DUMP_FRAME)验证：float 离屏 RT(16F)Hdr/Bloom/Defer/SSAO 黑，其余亮。RT readback 不可靠(FB 亮但 readback 黑)，判据改用 present dump。发现 3 处真 bug + 1 环境限制 | 逐层隔离：单/双 attachment、单/多 output、强制固定色 shader、纯 scene 采样、readback 对照 |
| 2026-08-15 | 修复 float RT 15... | 修复 | ①UBO ring buffer：multi-pass App 同帧多次 update 同一 UBO，VK GPU 异步读 buffer→后写覆盖前 pass 数据→整帧黑。VKBuffer 分配 32 槽每 update 落到独立槽+offset；②descriptor set 每槽独立(VKRenderer 分配 32 sets)，draw 绑当前槽 set，sampler 写全部 set(llvmpipe 读未绑 sampler 崩)；③Bloom m_expose 默认 0→tone map 1-exp(0)=0 黑→改 1.0；④float RT 在 llvmpipe fallback RGB8(真 GPU 不触发)。Hdr 由全黑转全亮 | Hdr/Gamma/BlinnPhong/Msaa/FrameBuffer 全亮；Bloom/SSAO 仍黑、Defer 部分(多 pass sampler 环境待续)；GL 46/46 无回归 |
| 2026-08-15 | llvmpipe 平台已知限制记录 | 记录 | ①llvmpipe 偶发 worker SIGSEGV/SIGILL(flaky，与负载相关)②UBO host-coherent GPU 读偶 stale ③float 离屏 RT 写黑/崩。真机 GPU 大概率不触发 | 环境特性，非本仓库 bug；建议后续留着真机验证 |
| 2026-08-15 | Vulkan 画面上下翻转修复 | 修复 | 根因：VK NDC y 向下 vs App GL 语义投影矩阵(glm::perspective y 向上)。修复：applyViewport 用负高度 viewport(y+h, -h) 翻转 y 轴(顺带将 GL z∈[-1,1] 经视口深度变换映射到 VK [0,1])；三角形窗口空间 winding 反转→VKPipeline frontFace 由 CCW 反转为 CW(创建+动态两处)。验证：dog.jpg 纹理 SimpleTexture 方向判据(上亮下暗/左亮右暗)与原始图一致；Triangle/Cube/Hdr/FrameBuffer/CullFace/Shadow_Map/Gamma/Msaa 全量回归数值与修复前一致 | 期间 git stash 中断曾致 10 个源文件被清空，从 dangling WIP commit 883391d2 完整恢复；SkyBox flaky SEGV 为 llvmpipe 已知问题与本次无关 |

| 2026-08-15 | ImGui 集成现状调研（GL+VK） | 调研 | GL=ImGuiOpenglWindow(glfw+opengl3 双 backend，imgui_impl_opengl3 裸 GL 画 overlay)+Application::render 每帧 newFrame/render；VK=src/rhi/vk 零 ImGui 钩子，仅 ImGuiContextWindow 无渲染后端（DrawData 丢弃）；3party/imgui 仅后端源码(imgui_impl_glfw.cpp+OpenGL/)编入可执行、核心库=vcpkg imgui::imgui 1.92.8 静态库，VK backend 文件存在但 CMake 不编译(旧 API 不兼容)；VK 覆盖层插入点=Application.cpp:152 m_imguiWindow->render()（endFrame 前 render pass 仍活跃）；负高度 viewport(VKBackend.cpp:907 y+h,-h)+frontFace 反转(VKPipeline.cpp:181/256)；ImGui VK backend 自设 viewport(0,0,w,h)+push scale/translate 不受 flip 影响 | 无代码改动，纯调研，为 VK 接入 ImGui 铺路 |

| 2026-08-15 | Task1 替换 vendored Vulkan backend 并启用 CMake 编译 | 重构 | 049ecf9：3party/imgui/Vulkan/imgui_impl_vulkan.cpp/.h 从过时 ImDrawCmd::TextureId 旧 API 替换为 vcpkg 1.92.8 官方 backend（本地 buildtrees 源复制，diff 逐字节一致）；旧文件 git mv/cp 存 .bak 留对比，按 brief 约定暂保留；CMakeLists 在 ENABLE_OPENGL 与 ENABLE_METAL 之间插入 ENABLE_VULKAN 块（镜像 OpenGL 风格 include_directories+aux_source_directory+list APPEND）并删过时注释"不接入ImGui VK backend" | build OK 0 error（IMGUI_VULKAN_SRC_FILES 收录 imgui_impl_vulkan.cpp，backend TU 编译链接正常；VK 模式仍用 ImGuiContextWindow 无渲染，符合预期）；新版 API 标记 grep 通过（PipelineInfoMain:119 / DescriptorPoolSize:113）；GL/glfw 未触碰；commit 时一次 transient AUTO_MERGE ref 锁错误消息但提交成功工作树干净 |
| 2026-08-22 | DX12 Task4 shader 流水线（dxc 目标+DXShader+hlsli 模板） | 实现 | 78c4220：res/DX12/_uniform_block.hlsli 权威 cbuffer 模板（槽表脚本核对逐字节对齐；floatPool 用 float4[16] 规避 HLSL 数组每元素独占16B行规则；lights=ULight struct[256] stride96）；Triangle.vert/frag.hlsl（Float4×2 TEXCOORD0/1，pos 直传无矩阵，对照真实 triangle.vert）；DXShader.hpp/.cpp（LocateCso 三级查找照 VKShader 改写→build/res/DX12/<dir>/<name>.<stage>.cso，D3DCreateBlob 装载，缺失列产物日志）；src/CMakeLists.txt ENABLE_DX12 块 dx_shaders 目标（vcpkg dxc HINTS，vs_6_1/ps_6_1/gs_6_0↔VSMain/PSMain/GSMain）+WIN32 补链 d3dcompiler.lib | **全程未构建**（用户指令），验证 pending 见报告；矩阵方向定论=mul(matrix,vector)（HLSL uniform 默认 column-major 打包与 glm 列主序直传一致，brief 草图 mul(vec,mat) 注释反了）；已 push origin（连同 T2/T3 三提交）；报告 task-4-report.md |
| 2026-08-15 | Task1 评审 | 评审 | 独立复核：build_run.sh build→`==> build OK`（ENABLE_VULKAN=ON+OPENGL=ON，backend TU 编译链接确认）；新 cpp/h 与 vcpkg1.92.8 buildtrees diff -q 逐字节一致；核心=vcpkg 1.92.8（installed imgui.h+ports/imgui/vcpkg.json 双确认）；CMake ENABLE_VULKAN 块位置/风格正确、过时注释已删；git fsck 无 broken/AUTO_MERGE ref（transient 已消散）；commit 仅动 5 预期文件，GLFW/OpenGL 未触碰；src/ 无旧 API 调用者不破坏编译。结论：Spec ✅ 全 6 步达标，Task Approved，无 Critical/Important，仅 Minor（下游接入须用新 API：Init 单参/PipelineInfoMain.RenderPass/AddTexture 新签名/LoadFunctions 带 api_version） | 唯一 Cannot verify：新 backend 运行时行为未执行（构建门控，VK 渲染留给后续任务）；.bak 待后续确认后按 brief 删除 |
| 2026-08-15 | Task2 VKRenderer 暴露 ImGui 句柄与绘制钩子 | 实现 | 177d8a6：IRenderer.hpp 加 VKImGuiInitInfo 前置声明+2 内联默认虚方法(imguiInitInfo 返 false/renderImGuiDrawData no-op，GL 不受影响)；VKBackend.hpp 定义 VKImGuiInitInfo(instance/physDevice/device/graphicsFamily/graphicsQueue/dsPool/imageCount/minAllocationSize/renderPass)；VKBackend.cpp include imgui+imgui_impl_vulkan，类声明 2 override，imguiInitInfo 填全句柄(_device==nullptr||!_swapchain 返 false)，renderImGuiDrawData 于 _recording&&_rpActive 时调 ImGui_ImplVulkan_RenderDrawData 后 applyViewport() 重放负高度 viewport（不复位 _viewportSet，因该标记还控制 App 自定义 viewport 回退）；**超清单必需**：src/CMakeLists.txt 给 renderLearn 加 3party/imgui/Vulkan include 路径（子 CMake include_directories 不传播，缺则编译失败）| build OK（修 2 编译错：static_cast<bool>(raii Device) 非法改 ==nullptr；imgui_impl_vulkan.h 未解析→CMake 加路径）；VK Cube 回归 pixels black=0 nonblack=921600（1280x720）；GL Cube/Triangle 冒烟干净 0 error；viewport 决策=尾部主动 applyViewport 重放，保 _viewportSet，ImGui 自建 descriptor pool/pipeline 不碰 _uboDs/_dsLayout 已确认；提交时瞬态 AUTO_MERGE ref 消息但提交成功；需要关注=会话并发修改同批文件已核对修正干净状态。报告 task-2-report.md |
| 2026-08-15 | VK ImGui 覆盖层 Task2-5（延续 Task1） | 实现 | 177d8a6(Task2 VKRenderer 句柄钩子,17d8a6 待核) → 98ce766(Task3 ImGuiVulkanWindow: GLFW+Vulkan 双 backend, setRenderer+init, DescriptorPoolSize=256 自建池避免抢占 3D _dsPool/_uboDs) → 62d5bbc(Task4 GLApp vulkan 分支换用 ImGuiVulkanWindow 替换 ImGuiContextWindow) → eefb85d(删 .bak) → b57accb(评审修复 GL-only: ENABLE_VULKAN 保护 vulkan.hpp include) | 全链路 build OK；VK 端 ImGui 覆盖层真正渲染：dumpFrame 首帧假象(首帧 no cmdlists)实为时序，1~1471 帧均渲染(306 indices)，RHI_DUMP_SKIP 帧跳过后 dump 可见面板于左上角(27143 差异像素 bbox x56-818 y56-547)+文字方向正确不翻转；3D 未破坏(Cube/SimpleTexture 与 Task1 像素级一致, 面板外 0 差异, black=0 nonblack=921600)；多 App 回归 Triangle/Cube/Hdr/FrameBuffer/Msaa black=0 CullFace=33 Shadow_Map=1128 Gamma=961 全部匹配 plan 预期、无错误；评审=无 Critical, 仅 1 Important(GL-only vulkan.hpp include)已修, Minor 记录；子代理回传不可靠曾幻觉 commit 177d8a6 不存在, 改亲自实现+每步真机验证 |
| 2026-08-15 | VK ImGui 覆盖层代码审查 | 评审 | 详审 VKRender::imguiInitInfo 句柄转换/内部自建池/destroy 时序(Application.cpp:20 先 window->shutdown 后 imgui destroy, 早于 VK device)/viewport 重放必要性(ImGui 强制正高度 viewport, applyDynamicState 不重放 viewport 故尾部 applyViewport 必须且无害)/3D 翻转未破坏全部正确；仅 Important=ImGuiVulkanWindow.cpp 无条件 include vulkan.hpp 致 GL-only(ENABLE_VULKAN=OFF)编译失败(默认 CMake 配置), 已修 | b57accb；验证: g++ -fsyntax-only ENABLE_VULKAN=0 通过(不再含 vulkan.hpp)+ENABLE_VULKAN=1 build OK；glm find_package 失败属新 build 目录 vcpkg 环境与本次无关；Minor 记录(MinImageCount 依赖交换链≥3/dsPool 占位/CheckVkResultFn 回调/swapchain rebuild 未联动) |
| 2026-08-15 | Task1 建立 Sample/BaseSample 接口并迁移 46 个样例 | 重构 | f811a84：新建 src/app/sample/Sample.hpp/BaseSample.{hpp,cpp}；src/app/App.hpp 父类 GLApp→BaseSample；GLCameraBaseApp.hpp 父类→App(薄壳) 并 git rm 其 cpp(内容并入 BaseSample.cpp)；CMakeLists 加 sample/* glob；46 个样例 initApp→load、drawScene→draw 全量迁移(参数名 renderer 与基类方法 renderer() 冲突改 rhiRenderer；12 文件 m_window->getProperties 改 Sample::setWindowSize/windowWidth/Height)；为接入既有 main(IApplication) 新建临时宿主 src/app/AppHost.{hpp,cpp}(GLFWWindow+GL渲染器+ImGuiOpenglWindow+输入转发+运行循环)，GLAppFactory 46 样例 MakeHost 包装、Base 仍走 GLApp 旧路径 | build OK；run.sh all -b gl -d1 = 46/46 OK(多次复跑稳定)；关键 Bug: AppHost 样例 Triangle/Rect/SimpleTexture/Cube 启动段错误139=样例 load() 内 renderer() 因未调基类 load 而 _renderer==nullptr(仅基类 Sample::load 填充)+旧 Chain 由 initGraphics 预置故可用；修复=Sample 增 setRenderer(),AppHost::init 在 load 前注入,恢复 124 无段错；真实渲染验证(临时帧钩子已删) Triangle drawn_pixels=115200 非空白；Sample.hpp 相对 brief 新增 setRenderer/setWindowSize/windowWidth/Height 必要偏差；AppHost 临时(Task3 重构)；提交时瞬态 AUTO_MERGE ref 警告但提交成功工作树干净 |
| 2026-08-15 | Task2+3 双后端 AppHost + SampleFactory + 工厂接线 | 重构 | af83c51：AppHost 正式化(无参构造/setSample/setBackend/currentSample/currentBackend/双后端 rebuildBackend 复用 GLApp 双分支/applyPendingChanges 帧间隙/reloadSample=SampleFactory+setWindowSize+setRenderer+load)；新增 SampleFactory(46 case+Base→BaseSample)；AppFactory::create 改构造 AppHost；删除 Application.*/GLApp.*/GLAppFactory.*/VKAppFactory.* | build OK；run.sh all -b gl -d1=46/46 OK；VK Cube 冷启动干净(EXIT 124 无 error 无崩)；GL Triangle/Cube 干净；setSample 热重载验证成功(多次 reload 无崩)。aspect=Sample 无 setAspect 未新增(禁改 Sample.hpp)，用 setWindowSize 计算 _aspect；renderBeforeLoop 仅 run 开头一次(reload 不调避免 IBL 双重副作用，热切换重跑留 Task4)；后端热切换骨架接通但 GL↔VK 实切未验证(Task4)；发现 rhi/vk VKRenderer teardown 顺序 bug(设备已析构后销毁 raii::Pipeline 断言)会崩真实后端热切换——init 已清首帧 pending 避免 VK 冷启动重复重建
| 2026-08-15 | Task2+3 AppHost/SampleFactory 正式化代码评审（f811a84..af83c51） | 评审 | 契约与控制器裁定全符合，无新增 Critical/Important，Minor×5：reloadSample 缩进错位(AppHost.cpp:79-80)、VK 冷启动窗口双重创建(init+rebuild 各 initialize 一次)、aspect() 死代码、运行时 resize 不更新视口/sample aspect(旧遗留)、getSampleCount 为读计数全量构造 sample；DX11 残留与 VK teardown 顺序为已知延后不重列 | Ready=Yes，Minor 待 Task4/5 顺带；46/46 GL+VK冷启动干净由控制器脚本核实(红线未重复跑) | 结论见评语
| 2026-08-15 | Task2+3 控制器核实（af83c51） | 验证 | 亲自复核：git log af83c51 架构落地(删 Application/GLApp/GLAppFactory/VKAppFactory + 新 SampleFactory46case+AppFactory 接线)；残留引用 grep=仅 DX11App.hpp:5 include 已删 Application.hpp(Windows 遗留非本构建,记遗留)；SampleFactory case=46 正确；AppHost run/生命周期符合契约(destroyBackendResources 先 sample→imgui→renderer)；build_run.sh build=OK；run.sh all -b gl -d1=46/46 OK(复跑稳定)；VK Cube 冷启动 EXIT124 无 error | 评审 Ready=Yes(Minor×5 记 ledger 待 Task4/5)；Task4 前置=fixed rhi/vk VKRenderer teardown 顺序 bug(设备先析构后销毁 map<Pipeline> 断言 abort) 才能真 GL↔VK 热切；DX11App.hpp 残留不改扩大面已记遗留 |
| 2026-08-15 | Task4 后端热切换三处根因修复 | 实现 | f9d47e4：①VKBackend shutdown 释放顺序(先释放依赖 device dispatcher 的 raii 对象 pipeline/buffer/rt 再销毁 device，修复析构时访问已销毁设备断言 abort，阻断真 GL↔VK 热切的元凶)；②GLFWWindow initialize 显式 GLFW_CLIENT_API(NO_API/OPENGL 二选一，修复 VK→GL 后窗口仍 NO_API 无上下文致 initGLContext 失败)；③AppHost GL 分支 setBackendKind(GL)+按属性重建窗口(修复切回 GL 后 shader 路径误指 Vulkan 含 set 布局致 GLSL 编译失败)；④renderBeforeLoop 移入 reloadSample、移除 run() 首行(热切后 IBL 算例重跑、初始不双跑) | 验证：GL 全量 46/46；VK IBL 冒烟无 error；双向热切换 GL→VK→GL→VK 及 VK 反向自测全过无 error/abort/segv(TEMP 钩子验证后已删)。三处根因均经真实运行复现-定位-修复；AUTO_MERGE transient 无害 |
| 2026-08-15 | Task5 总控条 UI（后端 Combo+样例下拉+信息行） | 实现 | 2aec58f：仅改 src/app/AppHost.cpp 的 renderTotalBar。后端 Combo 按 #if ENABLE_OPENGL/#if ENABLE_VULKAN 列 OpenGL/Vulkan↔GraphicsType；样例下拉遍历 AppType::Count(顺序转录自 AppType.hpp，与 SampleFactory.cpp 46 case 全对齐)，current_item=(int)枚举；信息行=当前后端名+样例名+FPS(io.Framerate)；匿名 namespace 辅助 StripEnumPrefix/EnumMemberName(复用 utils/EnumUtil)，窗口名 OpenGL→Control。只读枚举/状态+触发 set*，未动热切换链路；GraphicsType 确认定义于 AppType.hpp:3-10 | build OK；run.sh all -b gl -d1=46/46 OK；单样例 Cube 干净；程序化热切换自测(GL→VK→GL+样例 SimpleTexture)生效无崩，frame30 backend=GL sample=SimpleTexture，TEMP 钩子已删；git status 干净。concern=仓库 AUTO_MERGE 损坏 ref 提交时非阻塞告警、`-d` 透传失败系既有 cxxopts(仅 all 模式支持) |
| 2026-08-15 | Task5 控制条 UI（后端Combo+样例下拉+信息行） | 实现 | 2aec58f：AppHost.cpp renderTotalBar 升级为总控条——后端Combo(#if ENABLE_OPENGL/VULKAN 过滤, OpenGL↔GL/Vulkan↔Vulkan, 选中 setBackend)、样例下拉(遍历 AppType::Count 名称顺序=AppType.hpp, 复用 Utils::Enum::EnumName+strip 前缀, 选中 setSample)、信息行(后端名+样例名+FPS)；窗口改名 OpenGL→Control 避免与样例同名面板冲突；新增匿名 namespace StripEnumPrefix/EnumMemberName | 仅改 AppHost.cpp 1 文件(+58/-2), 未动切换链路/公共接口/样例/rhi；build OK; run.sh all -b gl -d1=46/46 OK; 程序化热切换自测 GL→Vulkan→GL+样例切换生效无崩(探针已删) | 控制器复核: build OK + 46/46；AUTO_MERGE ref 损坏告警(瞬态无害, 建议 git update-ref -d refs/AUTO_MERGE 清理)；-d 透传仅 all 模式支持系既有行为 |
| 2026-08-15 | Task6 全量验证+翻转判据+收尾 | 验证 | GL 全量 46/46 OK；VK 冒烟 45/46(唯一 FAIL=UniformBuffer VK shader 因 build 期 CMake 明确 skip bare uniform(需 UniformBlock) 致 SPIR-V 无效, 属既有已知问题与本批次无关, GL 侧正常)；VK SimpleTexture dump 翻转判据成立(上亮28.6>下暗26.0, 左亮28.1>右暗26.0, center63.3 狗脸主体), 无翻转回归；热切换双向已验证(Task4/5)；工作区干净无临时文件；清理损坏 AUTO_MERGE 空引用(.git/AUTO_MERGE 0字节 已删, fsck 不再报 broken ref) | 全 6 任务达成: spec→迁移→双后端外壳→热切换→控制条UI→验收; 后端热切换/样例热切换/翻转判据/回归全部通过; 遗留: DX11App.hpp 仍 include 已删 Application.hpp(Windows 遗留非本构建), VK UniformBuffer shader skip 既有问题 |
| 2026-08-15 | 修复总控条样例下拉字符乱码 | 修复 | AppHost.cpp renderTotalBar 样例下拉: 预 reserve(Count) 两 vector, 消除 vector 扩容使短名称(SSO<16B)的 c_str() 悬垂导致的乱码/错字符; 顺序本就按 AppType 枚举 0..Count | 只改 AppHost.cpp(+3 行); 构建 OK + GL 46/46 回归; build_run.sh 默认 all 已含 run |
| 2026-08-15 | build_run.sh 默认运行移除 timeout | 修改 | scripts/build_run.sh run(): 去掉 timeout 15, 直接运行 $BIN, 移除 124 分支 | 仅改脚本 run 函数; 默认 all 仍 configure+build+run |
| 2026-08-15 | 修复 SimpleTexture 图片"隐约可见" | 修复 | GLSimpleTextureApp.cpp: Rect() 默认彩色顶点(红/蓝/绿/白四角) 被 shader color=texture*fragColor 插值把各通道乘成0压暗图片; 改用白色顶点不调制纹理 | 既有bug非回归(git show 迁移前后一致); VK dump 中央均值(163,149,125)≈原图(172,154,121), 纯黑0%; GL 46/46; commit b9d6e9c |
| 2026-08-15 | 修复 GL 图片偏色/隐约(SimpleTexture等) | 修复 | Image::load: stbi_load(target>0) 数据已输出 target 字节/像素(如 RGBA) 但 m_size.channel 仍为源通道(3), 导致 GL 用 GL_RGB 读 3字节/像素 从 4字节数据 -> 字节错位偏绿偏蓝; 修复: targetChannel>0 时同步 m_size.channel=targetChannel | **已验证** commit 0e2eccd: 临时 GL dump(GL_RGB+PACK_ALIGNMENT=1) 中央图区均值 (174.9,154.8,118.8) vs dog (180.2,161.3,126.2) delta≈(-5.3,-6.5,-7.4) 均匀小差(修复前偏绿蓝 (149,163,149) delta(12,18,31)); GL SimpleTexture 单 case OK; VK 用 w*h*texelSize(4) 不受影响; 临时 dump 已删 |
| 2026-08-15 | 修复样例下拉乱码 + 去 timeout 提交 | 修复/脚本 | commit 56144b9(AppHost reserve 预分配防 c_str 悬垂乱码)、3ed69ce(build_run.sh 去 timeout 直接运行) | build OK + GL 46/46 回归 + SimpleTexture 单 case OK; 三修复分 commit 落库 |
| 2026-08-16 | 修复总控条被样例窗口覆盖 + 时间动画统一降速 1/10 | 修复 | commit 3fccd78：①renderTotalBar 固定 Control 窗口右上角(SetNextWindowPos/Size Always+AlwaysAutoResize)，根因=样例窗口(如 Cube 的 Sample count 面板)"OpenGL"(60,59 493x357) 默认左上角与 Control(60,60 317x130) 重叠覆盖"后端/样例"选择 UI；②run() _sample->draw(dt*0.1f)，根因=AppHost 传 dt 为毫秒，样例 curTime+=dt 累加毫秒，angle=20*(i+1)*curTime 每秒转 ~53 圈过快 | 临时 GL dump 验证 UI：Control 右上(921-1259,y上)与 OpenGL 样例窗口左上(61-349)分离不再重叠(删 imgui.ini 重置)；旋转相关样例 Cube/Explode/MultiInstance_Saturn/SimpleLight_Source_Mult/SimpleLight_Material/UniformBuffer/AdvancedShader/TemplateTest/Blend 全部 OK；temp dump 已删；commit 3fccd78 |
| 2026-08-16 | 时间动画速度改毫秒转秒(dt/1000) | 修复 | commit cf56f20：上版 dt*0.1 仍为秒的 100 倍(ms*0.1)，用户反馈仍过快；改 dt/1000 真正转秒，样例按秒设计(angle=20*(i+1)*curTime 期望 20度/秒) | 实测 dt≈2ms/帧(约500fps)，转秒后 curTime 每秒+1 → 旋转 20度/秒合理；9 个时间动画样例回归全 OK |
| 2026-08-16 | SimpleLight_Specular 中心球默认改橙黄色(213,14,86) | 修复 | commit 29a698a：中心球用 Object.frag result=(ambient+diffuse+specular)*objectColor，默认三强度全 0 相乘得 0 显示黑色；objectColor 改 (213,14,86)/255，默认强度 ambient=0.1/diffuse=1.0/specular=0.5/pow=32 让球默认可见 | build OK + SimpleLight_Specular 单 case OK（用户自行验证颜色，未截图分析） |
| 2026-08-16 | 切换样例状态残留修复 | 修复 | commit a2f8e7d：根因=GL 后端命令式状态(setDepthTest 直接 glEnable/glDisable)，GLPipeline::use() 只绑 program+VAO 不恢复状态，切换样例时 Triangle/Rect 等 2D 样例继承上一样例残留 GL_DEPTH_TEST 出错；修复=IRenderer 新增 resetRenderState()(默认 no-op,VK 继承免改) + GLRenderer 重置 depth/cull/blend/stencil/point-size/multisample/polygon-mode 默认值 + AppHost::reloadSample 创建前调用 | 程序化验证：Cube→Triangle 切换 depth 由残留 1 变 0（Cube 自身阶段仍=1 正常）；19 个状态相关样例回归全 OK（Triangle/Rect/SimpleTexture/Cube/Camera/DepthTest/Blend/CullFace/Msaa/TemplateTest/Shadow×3/SSAO/FrameBuffer/SkyBox/PBR_IBL_Specular/SimpleLight_Source_Mult/Explode/Defer/Hdr/Bloom） |
| 2026-08-16 | 默认样例改 Triangle | 修改 | commit 7925813：main.cpp 默认 app 由 PBR_IBL_Specular 改为 Triangle | build OK + 运行确认 Select Triangle Application |
| 2026-08-16 | SimpleLight_Material/LoadModel 相机距离翻倍 + 方向光检查 | 修复 | commit 0ac7699：1) SimpleLight_Material 相机 (0,0,3)→(0,0,6)；2) LoadModel 根因=误调 Sample::load 相机停在原点(模型中心)穿模，改 GLCameraBaseApp::load + 距离 (0,0,6)；3) 方向光光照检查：shader 公式与 uniform 映射全正确，仅 shininess=1 高光范围过大洗灰画面，改教程标准 32 | 像素级验证：125 cube 渲染有明暗层次、纹理棕色正确；三个样例 run.sh 回归全 OK |
| 2026-08-16 | TemplateTest 平面纹理异常修复 | 修复 | commit 9791510：根因=原 GLTexture2D 对 RGB 用 GL_REPEAT(UV 0..5 平铺)，RHI 迁移后 RhiImage::Load2D 固定 ClampToEdge 致 UV>1 区域拉伸；Load2D 加可选 wrap 参数(默认不变)，5 个 Plane+metal.jpg 样例(TemplateTest/DepthTest/Blend/CullFace/FrameBuffer)改 Repeat | 像素级验证：平面有周期性纹理边界(平铺正常)、下半屏全覆盖；5 样例 run.sh 全 OK |
| 2026-08-16 | wood.png 平面纹理平铺修复 | 修复 | commit 30d09f0：Gamma/BlinnPhong 与 TemplateTest 同根因(Plane UV 0..5+wood.png RGB 原 GL_REPEAT)；沿用 Load2D Repeat 参数修 6 样例：Gamma/BlinnPhong/Shadow/ShadowMap/Bloom/Defer | 像素级验证 Gamma：145 个不同颜色桶=木纹平铺正常(Clamp 会大片单色)；7 样例 run.sh 全 OK |
| 2026-08-16 | PBR_Texture 球体全黑修复 | 修复 | commit 424fc90：根因=RHI 迁移(a2a5658)后 GLPBRTextureApp::load 只调基类 load，基类 compileShader 编译 Base/PBR.fs(无采样器，读 vec4Pool[10]/floatPool[7,8,9] 槽位)而派生 draw 绑 5 纹理 unit1-5 且从不设这些槽 → albedo=0 球体全黑；修复=派生类新增 compileShader() 编译 Advanced/PBR/Texture/PBR.vs+fs(5 采样器版本)覆盖 m_program | 像素级验证：修复前球体中心区仅 0/32 近黑桶；修复后 100% 非黑、锈铁红褐色特征(31% reddish、9 颜色桶)；PBR 5 样例+PBR_Texture 单独 OK、全量 46/46 OK |
| 2026-08-16 | Vulkan UniformBlock floatPool 布局错位修复 | 修复 | commit 1883c5f：VK std140 强制 float[64] ArrayStride=16 与 CPU 紧凑 stride=4 不匹配 → 读 floatPool 的样例黑屏/仅 ambient；方案=gen_vk_shaders.py 注入 `#extension GL_EXT_scalar_block_layout : require` 并将 block 改 std430（float 数组 stride 4 对齐 CPU），VKBackend createDevice 启用 uniformBufferStandardLayout 特性，重生成 139 个 VK shader；同时 IBL(IC/Conversion) initShapes 提前修复 | 像素级验证：VK SimpleLight_Specular 球体恢复 diffuse/specular（中心 163,11,66 / max 252，与 GL 172,11,69 一致）；Material/Diffuse VK 亦正常；SPV 反汇编确认 ArrayStride 4 + offset 3264/3520；前序 dump"仅 ambient"系 dump 时机 lightPos 恰在球背面 |
| 2026-08-16 | Vulkan 光照方向 z 反修复 | 修复 | 根因：VK 默认 swapchain render pass 只有 color attachment、无 depth attachment → setDepthTest() 为 no-op，而默认 cull 关闭(_cullEnable=false) → 球体前后半球都绘制，背面光照(法线-z)覆盖正面，光照看起来 z 反/两部分针对相机；GL 默认 framebuffer 有 depth 故正确。修复=VKBackend.cpp createRenderPassAndFramebuffers 为每个 swapchain image 建 depth image(eD32Sfloat)+render pass 加 depth attachment+depthRef，ensureRenderPass 默认路径补 depth clear(1.0)；shutdown 清理 _depthImages | build OK；SimpleLight_Specular/GL 回归无 error 无崩，SimpleLight_Specular+Cube+SimpleTexture+DepthTest VK 运行干净；用户目视确认待反馈；改动仅 VKBackend.cpp |
| 2026-08-16 | Vulkan 逐实例 UBO ring 回绕导致大量立方体不显示 | 修复 | 现象：VK SimpleLight_Source_Direction count 调大(>32) 不显示对应数量立方体。根因：VKBuffer uniform ring 仅 kRingSlots=32 槽，而该样例一帧内循环 125 次 update+draw(126 次 submit)；第 33 次起 ring 回绕覆盖早先槽，CPU 在 submit 前已写完所有数据，GPU 顺序执行时前序 draw 读到被后续覆盖的槽(model 错乱)→ 立方体消失/散乱。GL 无 ring(直接写)故正常。证据：count≤32 GL=VK 一致，count=33 起 VK 明显变少(临界恰为 32)。修复：kRingSlots/kUboSlots 32→256(≥126 并留 2 倍余量，2 的幂)，descriptor pool maxSets/UBO 255→256、sampler 255*15→256*15。改动：VKBuffer.hpp、VKBackend.cpp | 像素验证：count=125 VK=10090px vs GL=10116px(修复前 VK=2276px)，5x5 网格布局一致；Direction/Point/Spot/Specular/Cube/Blend/Bloom VK 回归无 error 无崩 |
| 2026-08-16 | Vulkan 光源立方体被物体覆盖（跑到 cube 后面）修复 | 修复 | 现象：VK SimpleLight_Source_Point 高 count(≈100+)时光源立方体消失/跑到物体后面，GL 正常。根因：GL/VK 深度写语义不一致——OpenGL 中 glDisable(GL_DEPTH_TEST) 与 glDepthMask(true) 相互独立，测试关仍写深度；Vulkan 中 depthTestEnable=false 时即使 depthWriteEnable=true 也不写深度。Point 光源立方体管线(先画, test off, write on)在 VK 不写深度，被后画的带深度物体覆盖。证据：LIGHTLAST(光源后画)与 LIGHTDEPTEST(显式开 test)均使 GL=VK 恢复；ring 512/2048 无效排除 ring。修复=VKPipeline.cpp：depthWriteEnable 时强制 depthTestEnable=(_depthTest||_depthMask)，test 原关时 depthCompareOp=ALWAYS(恒通过≈测试关)仍写深度，对齐 GL 语义 | 像素验证：count=125 VK bright=453 与 GL 完全一致(修复前 VK 光源消失 bright=3)，多时间点 diff≤5；回归 SimpleLight_Point/Direction/Spot/Specular/Material/Ambination/Cube/Bloom/Shadow_Map/Shadow/Shadow_PointLight VK 均无 error 无崩 |
| 2026-08-16 | Vulkan TemplateTest 模板测试不生效修复 | 修复 | 现象：VK TemplateTest 平面纹理正常但 cube 全红、纹理被盖（GL 正常：红边框+内芯纹理）。根因 1：GL stencil 状态全局、VK 每管线——border 绘制用 _borderPipeline（从未 setStencilTest），GL 靠全局 glEnable(GL_STENCIL_TEST) 继承，VK 中该管线 stencil 关→整面红。根因 2：swapchain 深度附件 pickDepthFormat 优先选纯深度 D32_SFLOAT（无 stencil 分量），llvmpipe 可用→stencil 写静默丢弃、读恒 0→border NotEqual(1) 处处通过。证据：border 禁用时 VK 内芯纹理正常(70.9%)、启用则 0% 纹理 77% 红；GL/VK 红区同位置但像素 12x | 修复：①GLTemplateTestApp.cpp 给 _borderPipeline 显式配 stencil（setStencilTest true + NotEqual/1/0xFF + mask 0 + Keep + setDepthTest(false)）②VKBackend.cpp pickDepthFormat 优先带 stencil 格式(D32S8→D24S8→D16S8→D32→D16)+view aspectMask 含 stencil。像素验证：cube 区 GL=VK red 6.4%/texture 70.9% 完全一致 diff=0；回归 16 VK 样例(TemplateTest/Cube/SimpleTexture/Light×2/Bloom/Defer/Hdr/Shadow×3/SSAO/Camera/FrameBuffer/Msaa/PBR_Base) 均无 error |
| 2026-08-16 | Vulkan 纹理渲染成纯色/黑 修复（shader 接口 location 错位，系统性） | 修复 | 现象：TemplateTest VK 边框已正常但纹理纯色（GL 正常）。排查 8 类无关因素（纹理/采样器/mipmap/LOD/深度格式/plane/stencil/transform 均排除；spv 陈旧导致多次误判）。根因：VK 用 glslangValidator --auto-map-locations 按声明顺序分配接口 location；Basic.vert 输出 `out textureCoord; out fragColor;`（textureCoord→loc0）而 Basic.frag 输入 `in fragColor; in textureCoord;`（fragColor→loc0），**vert/frag 接口 location 交换**→frag 把 fragColor(白常量)当 UV 采样→纯色(实为 marble uv(1,0) texel)。SimpleTexture 因两文件声明顺序一致所以正常 | 修复：①TemplateTest Basic.vert/frag 加显式 layout(location=0/1) 匹配 ②扫描定位同 pattern 8 对 shader（Base/SkyBox/GLSL/MSAA/UniformBuffer Cube.vert + FrameBuffer/DepthTest Basic.vert + MSAA Post.vert），交换 vert 声明顺序使 fragColor 先于 textureCoord。像素验证：TemplateTest GL=VK dc=30 diff>60=0；Cube GL=2295/VK=2300 diff=0（原 VK 黑/纯色）。全量 VK 41-42/46 OK；SkyBox/Explode/NormalLine/Defer(132) 崩溃为既有 llvmpipe VK 问题（device init 后即崩/无效 SPIR-V，与本次无关）；FrameBuffer solid 为另一既有问题待查 |
| 2026-08-16 | Vulkan 运行时切换样例崩溃（光源→Mult 必崩）修复 | 修复 | 现象：VK 后端 UI 从 Triangle 切到 Mult 不崩，但从其他光源样例（Point/Direction/Spot）切到 Mult 必崩（segfault，崩溃日志停在加载 container2_specular.jpg 后）。llvmpipe 驱动内工作线程崩溃。根因：AppHost::run 时序 beginFrame→draw→endFrame(提交)→present→applyPendingChanges(reloadSample)，而 beginFrame 只在帧开头 wait _frameFence；reloadSample 在提交帧未完成（未 wait fence）时创建新样例并 `_sample=std::move(s)` 销毁旧样例的 VK 资源（UBO ring/纹理/管线）。提交中的 command buffer 仍引用这些资源/共享描述符集，释放后 llvmpipe 工作线程读已释放内存→UAF 崩溃。Triangle 无纹理/UBO 故不崩，光源样例有故必崩 | 修复：IRenderer 新增 waitIdle()（默认空），VKRenderer 实现为 device.waitIdle()，AppHost::reloadSample 起始处调用（创建/销毁前等 GPU 空闲，满足 Vulkan 释放仍被提交命令引用资源前须 wait 的约束）。验证：gdb 复现从 Point→Mult 25s 无 SIGSEGV/SIGILL（修复前必崩）；直接运行 Mult 无 error；全量 VK 41 OK/5 FAIL 与修复前一致（5 个为既有 llvmpipe 崩溃，无回归） |
| 2026-08-16 | Vulkan 样例切换崩溃根因修正（waitIdle+描述符集重置，双修复） | 修复 | 前条仅 waitIdle 未根治：真实根因有二。①旧样例资源被 in-flight 帧引用时销毁（UAF）：AppHost::run 提交帧后在 applyPendingChanges 才 reloadSample，beginFrame 要到下帧才 wait fence，期间销毁旧样例 UBO/纹理→llvmpipe 工作线程读已释放内存。②共享描述符集 _uboDs（256 个，跨样例复用）在旧样例销毁后仍引用其已释放纹理/UBO：新样例 drawLight 在 bindTexture 前就绑定到含陈旧采样器的集合→崩溃（证据：Mult→Mult 切换也崩；waitIdle 单独无效；无符号库需从主线程堆栈 present() 定位）。误打补丁记录：曾在 resetRenderState 用 vkResetDescriptorPool+_uboDs.clear() 造成双重释放崩溃（pool 重置后 raii 析构仍调 vkFreeDescriptorSets），改用池加 eFreeDescriptorSet 标志+clear() 释放 | 修复：①IRenderer 加 waitIdle()（VK= device.waitIdle），reloadSample 起始调用，确保旧帧完成才销毁旧资源 ②VKRenderer::resetRenderState()（VK 原本继承 no-op）清空按帧状态(_uboBuffer/_uboSlotIndex/_pipeline/vertex buffers/renderTarget)并重新分配全新描述符集（池加 eFreeDescriptorSet，clear 释放后 reallocate），清除陈旧引用。验证：Point/Spot/Direction/Mult/Triangle/Cube/TemplateTest 单独运行全部正常(124)，且各自→Mult 切换（第30帧 reloadSample）全部正常；全量 VK 42 OK/4 FAIL（4 个为既有 llvmpipe 崩溃，无回归） |
| 2026-08-16 | GL/Vulkan Blend 显示修复（双 bug） | 修复 | 用户报：GL 无玻璃效果、VK 有玻璃但总平面渲染异常。像素转储(RHI_DUMP_FRAME，GL 端本次新增对称 dump)定位双根因：①VK 纹理串扰——updateSamplerDescriptor 每次 bindTexture 把 sampler 写入全部 256 个共享描述符集，而 Vulkan 描述符集按引用绑定，后续 bindTexture(另一纹理)覆盖先前 draw 已记录的 set → 先画 cubes 用了后画的 window.png(红)纹理（VK"玻璃/总平面异常"实为红色伪影盖在 cubes/ground 上）。隔离绘制验证：单物体 GL=VK 全一致，cubes+windows 组合才出现红色。②GL 无玻璃——窗户几何在 z=5(相机原点朝 -z 看)位于相机后方不可见，且 rotate(90,x) 使窗户平面水平(对水平相机成边缘零面积) | 修复：①VKRenderer 改 bindTexture 仅记录 unit->(sampler,view) 到 _boundTextures，新增 bindSamplersToCurrentSet() 在 draw(bindPipelineAndState) 时只把当前纹理写入本 draw 将绑定的 set(_uboDs[_uboSlotIndex])，不污染他 set；resetRenderState 清空 _boundTextures。②GLBlendApp 窗户去掉 rotate(90,x)(改为竖直正对相机) + _winPos.z 5→-4(移到相机前方)。验证：GL=VK Blend 逐像素一致(0 differ,max_diff6)、红色窗户可见(0.25%)；VK 全量 42 OK/4 FAIL 提升到 ~44-45 OK/1-2 FAIL(Explode/NormalLine/Defer 连带修好，剩 SkyBox 设备init即崩/UniformBuffer 无效SPIR-V 为既有问题)；GL 全量 46/46 |
| 2026-08-16 | VK CullFace 看不到方块内部 | 修复 | 用户报：VK CullFace 看不到方块内部、GL 正确。像素转储+env 诊断定位：llvmpipe 对负高度 viewport 不翻转三角形 winding（偏离规范），故 VKPipeline 里"负高度 viewport 反转 winding→补偿反转 front-face"的映射（_frontFaceCCW?eClockwise:eCounterClockwise）在 llvmpipe 上把所有面判为 front，使 CullFace::Back 剔除变 no-op→VK 从不剔除、始终渲染外部(前面遮挡内部) | 修复：VKPipeline 静态 pipeline + applyDynamicState 的 front-face 改用 GL 语义普通映射（_frontFaceCCW?eCounterClockwise:eClockwise）。验证：CullFace VK==GL 逐像素 0.00%(max_diff=1)、图像直立、剔除生效；GL 46/46 无影响；VK 无新增一致失败(仅既有 llvmpipe 抖动 UniformBuffer/Explode/NormalLine+Bloom/SkyBox flaky)。注意：真 GPU 按规范会翻转 winding，此映射或需复核(本机仅 llvmpipe 可测) |
| 2026-08-17 | Blend 对齐 LearnOpenGL 官方效果（GL/VK 双端） | 修复 | 用户要求 GL/VK 均对齐 learnopengl 3.2.blending 官方场景。对照官方代码重写 GLBlendApp：相机(0,0,3)、2 个 cube(marble 纹理, 位置(-1,0,-1)/(2,0,0))、floor(Plane XZ 水平 y=-0.5 ±5, metal 纹理)、5 个透明窗户(新 TransparentPlane 竖直 XY 1x1 UV0..1, window.png, 按相机距离从远到近排序绘制)；删除多余大 ground 与旋转水平的 grass(官方无)；立方体/地面/窗户共用 blend 管线(depthTest+blend SrcAlpha/OneMinusSrcAlpha) | 像素验证：GL==VK 逐像素一致仅 4 diff(0.00%，为 floor/窗户边缘栅格化边界差异，非逻辑错误)；GL 全量 46/46 OK；VK 42 OK/4 FAIL(UniformBuffer 无效SPIR-V + Explode/NormalLine/Defer llvmpipe 崩溃，均为既有问题，Blend 本身通过)；UI 保留 Window Count/Position/Windows Pos 调整 |
| 2026-08-17 | Vulkan framebuffer 渲染全白/错乱修复（RGB8 颜色附件） | 修复 | 用户报：VK FrameBuffer 渲染异常、GL 正常。像素转储定位：VK 画面中央 640x360 纯白(255)锐利矩形，GL 同区域为 cube 群。最小假设测试：FBO 颜色附件 RGB8→RGBA8 后白块消失(与 GL diff max=3)，根因=R8G8B8_UNORM 非 Vulkan 强制支持的颜色附件格式，llvmpipe 渲染/采样异常。连带 Hdr 也异常(全屏内容错乱,corner167)，确认 RGB8 共性问题(原 float fallback 回退到 RGB8 同样中招) | 修复=VKRenderTarget::makeImage：RGB8→RGBA8 回退（llvmpipe 兼容层），float 回退改为直接 RGBA8(跳过 RGB8 坏格式)。验证：FrameBuffer VK==GL diff max=3(修复前中央 640x360 纯白)；Hdr VK bbox 与 GL 一致(修复前全屏错乱,残留仅 float 量化微小色偏)；GL 全量 46/46；VK 全量 43 OK/3 FAIL(均为既有 flaky SkyBox/UniformBuffer/NormalLine)；Msaa(也用 RGB8 FBO) 同样修复 OK |
| 2026-08-18 | Vulkan framebuffer 上下颠倒修复（离屏 RT viewport 方向） | 修复 | 用户报：RGB8 修复后 VK FrameBuffer 效果对但上下颠倒(GL 正常)。定位：VK 离屏 RT 渲染也用负高度 viewport，RT 纹理行0=场景顶部(与 GL framebuffer 行0=底部相反)，后处理 quad 再负高度翻转+UV 未补偿→显示上下颠倒；同为 RT 后处理的 Hdr/Bloom/Msaa 也颠倒(Bloom direct=389567<flip=826818 验证) | 修复=VKBackend::applyViewport：离屏 RT 用正高度 viewport(对齐 GL framebuffer 语义)，swapchain 保持负高度。验证(未跑全量,按要求): FrameBuffer flip diff=28/max2(修复前颠倒 direct 4.25%)、Msaa flip=0 完美、Hdr/Bloom 方向转正、Shadow_Map/Shadow_PointLight 方向转正(flip 更好)、Defer/SSAO 前后 VK diff=0 无影响、Shadow 全白(92.95%)与 PBR_IBL_Specular 绿黑为既有问题(修复前后一致非本次引入) |
| 2026-08-18 | VK 切换样例崩溃修复：SkyBox LoadCube 越界读 | 修复 | 用户报：VK 多次切换(gl/vulkan)后切样例崩溃(segfault)。临时钩子 RHI_SWITCH_SEQ 复现(序列 t:vulkan,t:gl,t:vulkan,s:SkyBox 稳定 SIGSEGV)，gdb 栈=rhi::UploadStagingToImage→VKTexture3D::initCube→RhiImage::LoadCube。根因：skybox 图片全为 RGB 3 通道(2048x2048)，LoadCube 用 Image::load(false) 保留 3 通道(12.58MB)，而 initCube 按 RGBA8 算 size=w*h*4(16.78MB) 上传→memcpy 越界读 4MB；单独跑侥幸落在可读堆块，切换后堆布局不同即崩 | 修复=LoadCube 强制 4 通道(load(false,4))，与 Load2D 一致。验证：复现序列 5 次全不崩(修复前 run2-5 全 139)；SkyBox VK dump flip 对齐(71143/7.7%)方向正确。还原临时钩子 |
| 2026-08-18 | VK SkyBox 中心 cube 不显示修复 | 修复 | 用户报：VK skybox 天空正常但中心 cube 缺失。dump 分析：差异全在中心 cube 区域(rows224-495,cols493-786)，天空区完全一致；临时只画 cube 时 VK 与 GL 完全一致(diff=0)→cube 绘制本身正常，问题在 skybox 覆盖 | 根因：GL 端深度测试是全局状态(skybox 继承 cube 的 glEnable(GL_DEPTH_TEST)，LEQUAL 生效不覆盖 cube)；VK 端深度是 per-pipeline 状态，skybox 只 setDepthFunc(LEQUAL)+setDepthMask(false) 未 setDepthTest(true)，_depthTest=false 且 _depthMask=false → depthTestEffective=false 深度测试关闭 → skybox 覆盖 cube。修复=drawSkybox 显式 setDepthTest(true)。验证：SkyBox VK 与 GL 完全一致(diff>20=0,max=3)。工作树另有 RhiImage.cpp LoadCube 4 通道修复未提交 |
| 2026-08-18 | UniformBuffer VK 报 invalid SPIR-V size 修复 | 修复 | 用户给错误日志：VKShader invalid SPIR-V size for res/Vulkan/Advanced/UniformBuffer/Cube.frag | 根因：VK 版 Cube.vert/frag 含裸 uniform(uniform mat4 model;/uniform vec4 cubeColor;)，CMake(src/CMakeLists.txt:61 正则)判定需 UniformBlock 跳过编译 .spv → LocateSpv 找不到兜底返回 GLSL 源码路径，VKShader 把 GLSL 当 SPIR-V 读(size%4!=0)报误导错误。修复=按 Task 9 惯例把 GL+VK 版 shader 改标准 UniformBlock(std140/std430+scalar)布局(model 进块、cubeColor→vec4Pool[5])，App 改 UniformBlock 模式(SetUniform+update 完整块)。重新 cmake configure 后 .spv 生成，VK 与 GL 输出一致(diff>20 仅3px/281 为帧时序差) |
| 2026-08-18 | SimpleGeometry 切 Explode 崩溃修复 | 修复 | 用户报切换 Explode 崩溃；VK 后端 Explode 单独跑即崩(139 SIGSEGV) | 根因：VK GS 几何着色器读取 UniformBlock(floatPool[10]) 时 lavapipe 对 GS+UBO 的 LLVM JIT 崩溃(崩在 libvulkan_lvp.so 下方的 JIT 区,栈0x0)。二分实验：GS 改常量不崩、恢复读 UBO 崩。修复=VS 读 floatPool[10] 经 interface block(vs_out.time) 传给 GS，GS 不再读 UBO。验证：Explode VK 不崩且渲染正常(与 GL 非背景像素几乎一致，差异为 time 动画相位差)。NormalLine 同根因(GS 读 projection)未改 |
| 2026-08-18 | 排查修复全部 VK GS 读 UBO 崩溃 + NormalLine | 修复 | 用户要求一起修 NormalLine 并全面排查 | 扫描全部 VK 非常规 stage shader(.geom/.tes/.tcs) 仅 3 个 GS：Base.geom(不读UBO,正常)、Explode.geom(已修)、NormalLine.geom(读 projection,崩)。修 NormalLine=VS 投影并输出 clip 空间法线偏移(clipOffset)，GS 仅拼接，不再读 UBO；数学恒等(一次投影 vs 两次乘加)仅有浮点差异(GL 新逻辑替换实验 diff 1012px 佐证)。另补 GLNormalLine 两 pipeline setDepthTest(true)(VK per-pipeline depth,同 SkyBox 根因)。验证：SimpleGeometry/Explode/NormalLine 三 App GL+VK 全部正常运行 |
| 2026-08-18 | VK 窗口 MSAA 无效修复 | 修复 | 用户报 vulkan 的 msaa 没效果 | 根因：Vulkan 强制 swapchain image 单采样(e1)，VKPipeline::setMultisample 仅设标志未参与 rasterizationSamples；窗口渲染路径 samples 恒 e1。FBO MSAA 路径(离屏 RT samples=4+resolve)已正常。修复=窗口 MSAA 路由：VKRenderer 懒建内部 _winMsaaRT(samples4 RGBA8+D24S8)，ensureRenderPass/bindPipelineAndState 经 effectiveRT() 用其 rp/samples(负高度 viewport 保窗口序)，endFrame 将 resolve image blit 到当前 swapchain image 并布局转换；VKPipeline 暴露 multisample()。验证：VK win-MSAA on/off 差异 1494px(原 0)生效，与 GL 画面一致(non-bg 309124 vs 308356)；回归 VK Cube/SkyBox/Explode/NormalLine/UniformBuffer/Defer/Bloom/Hdr/PBR_IBL_Specular/Shadow_PointLight/SSAO/MultiInstance/Msaa/SimpleGeometry 全通过 |
| 2026-08-19 | BlinnPhong VK 效果不对修复 | 修复 | 用户报 VK BlinnPhong 效果错、GL 对。根因=glslangValidator --auto-map-locations 按声明序分配接口 location：Object.vert 3 个 legacy out(TexCoord/opos/Normal 占 loc0/1/2)使 out VS_OUT block 落 loc3，而 Object.frag 仅 in VS_OUT 落 loc0→接口错位，FS 把 FragPos/TexCoords 读到 vs 的 TexCoord/opos 数据→光照错、平面黑。修复=VS/FS block 加 layout(location=3)。验证：SPIR-V vs_out/fs_in 均 Location 3；GL vs VK diff>20=0(max3)、平面亮度 38.4 一致。连带 Shadow ShadowMapping.{vert,frag} 同样加 location=1，Shadow 90万→3.5万 px diff（余为 shadow map 内容差异，另查） |
| 2026-08-19 | 扫描全量 VK shader 接口 location 错位 | 修复 | 用户要求排查其他 sample 是否有 BlinnPhong 同类问题（--auto-map-locations 按声明序分配 location，vert/frag 声明顺序不同则错位；GL 按名字连接故正常、VK 按 location 故错）。写脚本 spirv-dis 反汇编全部 84 vert+84 frag+3 geom 配对检查同名接口 location 一致性 + block 成员顺序。发现并修复 3 处：①Defer/GBuffer.vert、SSAO/GBuffer.vert（out FragPos/TexCoords/Normal 顺序与 frag 的 in TexCoords/FragPos/Normal 交换→交换 vert 顺序）②Ambination/Object.vert（out normal 占 loc0 使 outColor 落 loc1，frag 只 in outColor loc0→收到 normal→交换顺序 outColor 在前）。验证：脚本 87 对 0 错位、block 成员 0 不一致、检出能力正例通过（构造修复前 BlinnPhong 检出 VS_OUT loc3/fs loc0）；Ambination GL=VK diff=0；MultiInstance 209px/Material 1662px 为时序差。另发现既有问题：Defer/SSAO VK 全黑（mean 0，3 附件 MRT GBuffer FBO，修复前后一致，与本次无关待查） |
| 2026-08-19 | 提交 location 错位修复 | 提交 | commit 26db1cb：8 文件 +25/-8（BlinnPhong/Shadow block 显式 location、Defer/SSAO/Ambination vert 交换声明序、VKBackend per-target clear color），工作树已提交干净 |
| 2026-08-20 | Vulkan Shadow 阴影错乱 + Defer/SSAO 全黑修复（双根因） | 修复 | 用户要求继续排查 Shadow（此前 30018 diff）与 glm 矩阵 Vulkan 适配。两独立根因：①Defer/SSAO 全黑 = llvmpipe `_floatRtFallback` 把 RGBA16F 附件降级 RGBA8 → GBuffer gPosition（view 空间坐标超 [0,1]）损坏；该回退本就是为掩盖 MRT pipeline 附件数 bug 引入，MRT 修复后已不需要 → 移除 llvmpipe 特判（VKBackend.cpp）。②Shadow 大片假阴影 = VKTexture2D::adopt 硬编码 sampler ClampToEdge + 无 borderColor（默认黑=0），而 GL 用 CLAMP_TO_BORDER + border=1.0(far)：shadow map 超范围采样 VK 返回 0(近) → 误判阴影。修复=VKTexture2D::adopt 增 TextureDesc 参数按附件 wrap/filter 建 sampler，ClampToBorder 用 eFloatOpaqueWhite；VKRenderTarget 每附件保存 wrap/filter | 验证：Shadow 34828→2 diff(mean 29.37 一致)、Shadow_PointLight 64、Defer 471572→179、SSAO 0、Hdr 0、Bloom 0、BlinnPhong 0、NormalMap/ParallaxMap/Gamma/Msaa/FrameBuffer 0；另确认 depth-pass ZO 映射必要（去掉 34807）主 pass ZO 非必要已还原；Light.frag std140 实验还原 std430；float fallback 正式移除 |
| 2026-08-21 | VK IBL 三大问题修复（用户指出 6 根因中 5 个已完成） | 修复 | ①BRDF LUT UV binding semantic 1→2（GLIBLSpecularApp CreateQuadBuffer）②m_envCubemap generateMipmap=true + flush() mid-frame submit + genCubeMipmaps ③lightPosition/color UBO 上传移到物体渲染前（3个IBL App）④IBL_Irradiance PBR.frag 新增 fresnelSchlickRoughness 用于 ambient IBL（GL+VK）⑤VKTexture2D float→half 转换 | build OK；3 IBL App VK 冒烟非黑像素 84%；IRenderer 新增 flush() 虚接口 + VKRenderer 实现（endRP→end→submit→waitIdle→begin）；ITexture3D 新增 genCubeMipmaps() 虚接口 + GL/VK 各自实现；临时 debug 代码已清理（VKTexture3D debugDumpCubeFace 调用、GLIBLIrradianceConversionApp VK include）；renderBeforeLoop 每阶段间 flush 确保 GPU 命令提交后再采样 |
| 2026-08-21 | VK PBR IBL 背景"仍有问题"真根因定位与修复（背景暗/偏红 + 后续崩溃） | 修复 | 用户报背景仍不对。逐层排查：cubemap face 数据 dump 正常→背景管线常量色测试正常→固定方向采样正常→WorldPos 方向可视化正常，但最终画面仍暗。加 TEMP 日志发现 **drawIndexed 时 _recording=false**：AppHost::reloadSample→renderBeforeLoop 在首个 beginFrame 之前执行，VK 端整个 IBL 预计算（env cubemap 采集/irradiance/prefilter/BRDF LUT）全部被静默跳过（face 里的"环境内容"实为 llvmpipe 复用 HDR 纹理 staging 的遗留堆内存，跨 run/shader 改动稳定复现）。修复①VKRenderer::ensureRecording() 懒启动一次性 cmd（draw/drawIndexed/drawIndexedInstanced/drawInstanced 入口调用），flush() 改为提交后不再重启录制（其仅被 IBL renderBeforeLoop 使用，结束后 _recording=false 让首帧 beginFrame 正常接管）。修复②attachCubeFace 按 cube 面 mip 实际尺寸建 framebuffer 并新增 VKRenderTarget::framebufferExtent()（ensureRenderPass 用它作 renderPass area）：此前 FB 固定用 RT 的 512×512，而 irradiance(32×32)/prefilter(mip 128→8) 远小于附件图像——Vulkan 不像 GL 自动裁剪到附件大小，clear/draw 越界写导致 llvmpipe raster 线程 rep stos 清屏时 SIGSEGV。连带修掉三个 capture 循环内 RP 未结束就 attachCubeFace 销毁 FB 的 UB（循环首行补 setRenderTarget(m_captureRT)） | 验证：PBR_IBL_Specular VK mean [105,56,47] vs GL [113,58,47]（修复前 [54,17,18] 且 53% 近黑），100% 非黑像素；与 vflip(GL) diff>20 仅 8.1%（全部位于 ImGui 面板区域，中心 3D 内容 0% 差异；GL dump glReadPixels 不翻行序故对比需 vflip）；**全量回归 GL 46/46 + VK 46/46 全绿（VK 此前还有 4 个既有 llvmpipe 失败，本次首次全通过）**；临时 TEMP 日志/RHI_CUBE_RGBA8 等二分 env 全部清除，Irradiance.frag git checkout 还原 |
| 2026-08-22 | 运行中切换到 Vulkan PBR 崩溃修复（endFrame 漏清 _rpActive） | 提交 | 用户报 `bash scripts/run.sh` 运行中切到 Vulkan PBR 必崩（SIGSEGV 紧跟 attachCubeFace 日志）。AppHost 加 RHI_SW_B_FRAME/RHI_SW_S_FRAME 临时自动切换钩子复现成功。gdb 栈=setRenderTarget→llvmpipe endRenderPass 内 `mov 0x60(%r13)` r13=NULL；TEMP 日志抓到崩溃时 `_rpActive=true _recording=true` 但 RP 从未在本段命令中开始。根因=**VKRenderer::endFrame 只调 endRenderPass 漏置 _rpActive=false**：帧间隙执行的 reloadSample→renderBeforeLoop 继承陈旧 rpA=true → ensureRenderPass 首行 `if (_rpActive) return true` 直接跳过 → draw 记录到无渲染通道的命令流 → 下次 setRenderTarget 条件满足去 endRenderPass → llvmpipe NULL 解引用。冷启动不触发是因为 capture 前 beginFrame 恰好重置了标志；同后端样例热切(GL→VK 后端重建后跑若干帧再切 PBR、VK 内 Triangle→PBR)均崩，普通样例互切不崩（无离屏 RP）。修复=endFrame 结束 RP 后补 `_rpActive=false` | 验证：Triangle(VK)@15帧→PBR ✓、GL@8帧→VK 后端→PBR ✓（用户场景）、PBR→Cube ✓、冷启动 PBR rc=124 ✓；热切换后 dump 与冷启动逐像素一致（mean [105,56,47]，diff>20 仅 1.4% 帧时序噪声）；回归 GL 46/46 ✓、VK 43-44 OK（余下 FAIL 全为 "X connection to :0 broken" 环境性杀进程，每轮失败样例随机非代码回归）；临时钩子已移除，构建干净  commit 5046bc4（16 files, +414/-31） |
| 2026-08-22 | App 去 GL 前缀重命名（后端已解绑但命名残留） | 提交 | `src/app/GL/` → `src/app/Samples/`；47 个样例类/文件去 GL 前缀（GLTriangleApp→TriangleApp、GLPBRBaseApp→PBRBaseApp 等，含 GLCameraBaseApp→CameraBaseApp），sed 按 git 记录的旧文件名生成精确 token 替换（避免误伤 GLFW/OpenGL/GLenum/rhi/gl 真后端代码）；include 路径 app/GL/→app/Samples/；src/app/CMakeLists.txt glob 更新。保留真 GL 相关：ImGuiOpenglWindow、rhi/gl/*、native/utils 的 GL 目录。坑①ALL_CPP_FILES 为自累积 CACHE 变量，旧路径残留需删 build/CMakeCache.txt 重配；②重配需补 vcpkg 工具链参数 + Vulkan_INCLUDE_DIR/Vulkan_LIBRARY(vcpkg 头+系统 loader) + CMAKE_MODULE_PATH 指向 vcpkg stb 的 FindStb.cmake（assimp 依赖） | 验证：构建 -j 8 通过；冒烟 Triangle(VK)/PBR_IBL_Specular(GL) rc=124；全量回归 **GL 46/46 + VK 46/46 双全绿**  commit 7a67fc4（106 files, +684/-684） |
| 2026-08-22 | DX12 后端 Task 1 spike 完成——WSL D3D12 可行性验证（触发停止条款） | 调研 | tools/dx12_spike 独立工程（spike.cpp/CMakeLists.txt/tri.hlsl）+ vendored DirectX-Headers 1.619.5 + vcpkg directx-dxc 1.9.5191。五项结论：**设备创建 ✓（关键发现：必须 DXCore 显式枚举适配器传 D3D12CreateDevice，nullptr 默认路径 0x887A0004；FL 上限 12_1）**、Downlevel Present ✗（Win7 IID QI 全 E_NOINTERFACE）、完整 DXGI ✗（无 libdxgi.so）、dxc ✓、gs_6_x ✓（GS 可直译）。微软官方确认 WSL libd3d12.so 仅支持 offscreen rendering and compute（wslg#387 至今 open） | **两条原生呈现路径均不通 → 触发 brief Step 5 停止条款**，呈现路线待用户决策：A) CPU readback 上屏 B) 搁置 DX12 C) Windows 主机侧开发；报告 .superpowers/sdd/2026-08-22-dx12-backend/task-1-report.md，结论入 spec 附录 A |
| 2026-08-22 | DX12 后端 Task 2 构建接线+后端注册+设备创建空壳（Windows 主机侧路线） | 实现 | 8fe1dae：src/rhi/dx12/ 三文件（DXHeader 原生头+DX_CHECK+ComPtr 禁拷贝可移动/DXBackend.hpp DXImGuiInitInfo/createDX12Renderer/DXBackend.cpp 设备生命周期 init(FL12_0)+queue+fence+event，工厂返 DXNull* no-op 防空指针段错误，shader compile=false 走 ExitIfFailed 干净退出）；Common.hpp BackendKind 加 Dx12；main.cpp 注册 -b dx12；AppHost.cpp rebuildBackend DX12 分支（无 ImGui，留 T9）；rhi/CMakeLists dx12 glob；src/CMakeLists ENABLE_DX12 块 if(WIN32) 链 d3d12.lib/dxgi.lib/dxguid.lib + 遗留块收窄 ENABLE_DX11（d3dx11.lib 现代 SDK 已移除防 LNK1104） | Linux 默认配置 build OK + GL/VK 全量双 46/46 零回归；ENABLE_DX12=ON Linux configure rc=0；push 桥仓库成功 501f2f4..8fe1dae。修正 brief 编译炸弹（IID_PPV_ARGS 不能进三目表达式）；BackendKind 无穷举 switch 需同步。**Windows MSVC 构建+冒烟 pending**（dx12 TU 从未经编译器检验）；报告 task-2-report.md |
| 2026-08-22 | DX12 后端 Task 3 DXGI 格式映射表 + 缓冲区实现（VB/IB/UBO ring） | 实现 | 3dd7a62：DXFormat.hpp（DXFormatOf 全表 9 项 constexpr：RGB8→R8G8B8A8_UNORM 承载+Depth32F→R32_TYPELESS+ToDXTexelSize，RGB 展开对齐 RhiImage 加载期 4 通道做法）；DXBuffer.{hpp,cpp}（VB/IB=DEFAULT 堆+DENY_SHADER_RESOURCE+一次性暂存拷贝+fence 同步；UBO=UPLOAD 堆持久 map ring，槽=AlignUp(sizeof(UniformBlock),256)×32 槽，update 推进 ring 头防跨槽污染；BindAsVB(cl,slot,stride) 方案 B 由 Renderer 传 layout stride、视图惰性缓存；IB=R32_UINT 对齐全链路 unsigned int；新增 submittedBase() 供 Task 6 换算 bindRange 偏移）；DXBackend.cpp 工厂接真实 DXBuffer（未 init 回退 DXNullBuffer）+init 建 _uploadAllocator | **验证 pending**（用户指令本阶段禁构建/运行，静态自查 API 签名）；疑虑=共享 fence 计数约定须 Task 6 遵守 GetCompletedValue()+1 单调推进；已 push 桥仓库 8fe1dae..3dd7a62；报告 task-3-report.md |
| 2026-08-22 | DX12 后端 Task 5 DXPipeline（Root Signature + PSO 缓存） | 实现 | eaa2997：DXPipeline.{hpp,cpp}=PSOKey{stateHash,color[8],count,depth,samples}+FNV-1a hash/==（仅覆盖前 colorCount 槽）、9 项静态采样器表 f*3+w 布局+SamplerSlot、root signature 单例创建（param0 根 CBV(b0) ALL 对接 UBO ring GPU VA、param1 表 SRV t0..127 PIXEL、静态采样器挂尾）；DXPipeline 全 IPipeline setter 只写成员镜像 VK 默认值，stateHash 全集混入 vertexLayout（pointSizeEnable 除外），pipelineFor(key) 未命中懒建+unordered_map 缓存，use no-op/handle 最近 PSO；Input layout TEXCOORD<i>+binding+PerInstance 步进1，VS/PS 必需 GS 可选三阶段；深度语义对齐 VK（test off+write on 强制 test+ALWAYS）；Point fill→Solid+LOGW 一次、FrontAndBack cull→Back+LOGW 一次；DXBackend.cpp createPipeline 工厂接真实 DXPipeline（dynamic_cast 校验失败回退 DXNullPipeline）+init 建 _rootSignature/shutdown 释放 | **验证 pending**（禁构建指令）：MSVC 编译/聚合初始化字段序/RTTI 待真机；偏差=createShader 未接线（brief 清单仅 createPipeline，下任务）、param0 用根描述符形态、采样器编号改系统序 f*3+w、borderColor=OPAQUE_WHITE 对齐 VK 决策留 Task 8 动态堆；已 push 桥仓库 3dd7a62..eaa2997；报告 task-5-report.md |
| 2026-08-22 | DX12 后端 Task 6 DXSwapchain（原生 DXGI flip-model）+ 渲染主循环状态机 | 实现 | 7512999：DXSwapchain.{hpp,cpp}=CreateDXGIFactory2→CreateSwapChainForHwnd（HWND=glfwGetWin32Window、R8G8B8A8_UNORM、BufferCount=2、FLIP_DISCARD）→GetBuffer×N+RTV 堆+窗口深度 D24S8/DSV；Present(1,0)；ResizeBuffers 重建；fence GetCompletedValue()+1 单调等待。DXBackend.cpp 主循环状态机全量：beginFrame=ensureRecording(幂等懒开录)+setRenderTarget(null)=backbuffer RT pending；endFrame 显式 _rtActive=false(VK 教训)；present=flush OM→RENDER_TARGET→PRESENT 屏障→Close→Execute→Present→fence 推进等待→Reset allocator+cmdlist 开录下帧；setRenderTarget 记录 pending+首 draw 前 flush 时 OMSetRenderTargets+Clear(对齐 VK RP 重开即 clear)；clearColor 按 target key 记录+RT 打开时立即清(含深度/模板)；setPipeline(null) 落地 pending，PSO 取用延后至 draw(key 含 RT 格式/samples)；draw 系列=prepareDraw(flushOm→PSO→根 CBV(b0,uboGPUVA+submittedBase())→拓扑→BindAsVB(layout.binding 分槽 stride)/IB)；视口负高度翻转(y+h,-h)对齐 GL y-up；createShader 接线真实 DXShader；getSwapchain 返回真实交换链；bindTexture/blitFramebuffer LOGW-once stub(T7/T8)；backendCapabilities 查 MSAA 质量 8/4；resetRenderState/waitIdle/flush 全落地 | **验证 pending**（用户指令禁构建）；必要偏差 A=StaticCollector.getGLShaderPath 补 Dx12 分支返回 res/DX12（否则 LocateCso 必败，先例=VK T7 后端感知）；B=DXPipeline 加 layout() 只读访问器（BindAsVB 需 stride）；C=删 DXNullShader/Swapchain 壳；D=imguiInitInfo(DXImGuiInitInfo&) 非虚重载+using 防遮蔽。疑虑=DXBuffer kRingSlots=32<VK256(Task10 LightSource 批次前必扩)、负高度视口翻转 winding→CullFace 批次前需镜像 VK frontFace 反转修复、HLSL 文件名大小写靠 NTFS 不敏感兜底。已 push 桥仓库 eaa2997..7512999；TriangleApp 方法清单逐项核对全覆盖；报告 task-6-report.md |
| 2026-08-22 | DX12 后端 Task 7 纹理（SRV heap+上传+mipmap）+ bindTexture 接线 + blitFramebuffer | 实现 | 48e3603：DXTexture2D.{hpp,cpp}（DEFAULT 堆+UPLOAD 暂存 CopyTextureRegion fence 同步；RGB8/RGB16F/RGBA32F ch3 CPU 展开 4 分量含 float32→half16 对齐 VKTexture2D；行距 256 对齐）；mipmap=blit PSO 渲各 mip RTV 手动线性降采样（子资源级 PSHR↔RENDER_TARGET 往返）——**核实 D3D12 无 GenerateMips 内建 API（brief 的 OPTIONS1 实为 WaveOps），手动路径为唯一实现**，blit 资产缺失 LOGW+SRV 钳 mip0 兜底；MSAA 按 samples 建 4X/8X 经 MULTISAMPLE_QUALITY_LEVELS 校验回落+DENY_SHADER_RESOURCE(RTV-only)+终态 RENDER_TARGET（DENY 资源不可进 PSHR）；createEmpty 深度 TYPELESS+srvFormat R32_FLOAT/R24_X8_TYPELESS。DXBackend：init 建 128 槽 shader-visible SRV 堆（槽 i↔t(i)，槽 0 预留 ImGui）+内部 blit 根签名(param0=t0 表+静态 s0 LinearClamp)+嵌套 FrameBlitContext 注入纹理（PSO 按目标格式懒建缓存）；bindTexture 2D×2 接通写堆槽 unit+1（ITexture3D stub T8）；prepareDraw 每 draw SetDescriptorHeaps+RootTable(1) 自愈切堆——**静态采样器随根签名生效，确认无需每 draw 额外采样器动作**；blitFramebuffer 对照 VK 语义（Depth dst=null 拷窗口深度+恢复 OM 绑定对齐 GL glBindFramebuffer(0)/Color 仅 RT↔RT 全屏三角形正高度恒等/深度格式归一化不一致拒绝）；resetRenderState 置空上样例 SRV 槽 null view 防悬垂；imguiInitInfo 回填 srvHeap。新 HLSL：_internal/blit.{vert,frag}.hlsl（SV_VertexID 大三角）+Base/{rect,SimpleTexture,Cube}.{vert,frag}.hlsl（缺 CSO 样例必跑不通，属必要超清单）+_samplers.hlsli（s0..s8 别名=f*3+w，编译期取别名，当前用 s6=Load2D 默认 LinearMipLinear+Repeat） | **验证 pending**（用户指令禁构建）：Windows 构建+dxc 新 HLSL（GLOB 需重新 configure）+Triangle/Rect/SimpleTexture/Cube 冒烟待真机；偏差披露=Base 三样例 HLSL×6/_samplers.hlsli/DXShader::FindCso 公开/DXSwapchain.depthResource()/_uniform_block.hlsli 加 include；自查修 2 bug=mipgen 瞬态堆原在录制 lambda 内（SetDescriptorHeaps 绑定堆须存活至执行完成）、MSAA 终态 PSHR 非法改 RENDER_TARGET；疑虑=winding/culling 未实测（CullFace 批次前可能需镜像 VK frontFace 反转）、Task 8 契约=blit 时不得有活动离屏 OM 指向 src/dst。已 push 桥仓库 7512999..48e3603；报告 task-7-report.md |
| 2026-08-22 | DX12 后端 Task 8 DXRenderTarget（MRT/MSAA/cubemap RT）+ DXTexture3D（cubemap/3D）+ 动态 border 采样器堆 | 实现 | 604a586：DXRenderTarget.{hpp,cpp}=create(desc) 多附件（MRT≤8+深度，desc.samples 全附件 MSAA）+attachCubeFace(mip)/attachDepthCube（记录挂接，逐面 attachCubeFace 生效）+自持 RTV/DSV 堆+BeginPass/EndPass 状态机（颜色 PSHR↔RT、深度 DW↔PSHR 往返对齐 VK finalLayout；cube 色面子资源级转移、MSAA 常驻 RT 不转移）+GetOmTargets CPU 句柄数组+ClearAll 按格式裁 stencil；DXTexture3D.{hpp,cpp}=initCube 六面上传+createEmpty（深度 TYPELESS 家族同 DXTexture2D）+genCubeMipmaps blit 降采样（复用 IDXBlitContext RS+PSO，TEXTURE2DARRAY 单面单 mip 视图）+rtvFace/dsvFace 懒建堆+WriteSrv(TEXTURECUBE/3D)+init 真 Texture3D RGBA8。DXBackend：flushOmTargets 重写离屏 MRT OMSetRenderTargets+按目标清屏（EndPass→BeginPass 完整往返保换面安全）；prepareDraw PSOKey 按 RT colorCount/formats/depth/samples 取键+ensureRecording 懒启动（IBL renderBeforeLoop 先于首帧不再丢弃，5046bc4 同教训）；applyViewport 离屏正高度视口（同 VK offscreen 分支）；bindTexture(ITexture3D) 接通写堆槽 unit+1；blitFramebuffer UnbindAllOm 前置解绑（Task7 契约落地）+MSAA ResolveSubresource+DoBlitDepth 状态感知转移；**borderColor 动态 SAMPLER 堆（16 槽）=ClampToBorder 组合移出静态表（9→6 条，寄存器 f*3+w 不变 _samplers.hlsli 无需改），槽 2/5/8 bind 路径按 borderColor[4] 写入+预填白边框行为等价+draw 双堆 SetDescriptorHeaps**。DXPipeline 导出 DxFilterOf/DxAddressOf；DXTexture2D 加 samplerParams/borderColor/storageFormat 访问器 | **验证 pending**（用户指令禁构建）：Windows MSVC 编译+FrameBuffer/Skybox/Msaa/Defer/SSAO/Shadow 冒烟待真机（HLSL 镜像树属 Task 10，缺 CSO 应 load 期干净退出）；自查修 6 bug（init 3D OOB/uploadFaces 暂存 lambda 内 UAF/genCubeMipmaps valid 门禁时序/单 mip 色 cube 缺 ALLOW_RENDER_TARGET/flushOm 同目标跳 EndPass 致换面状态错/kSamplerHeapSlots 重定义）；限制披露=同 draw 同组合异 borderColor 无法表达（寄存器固定）、attachCubeFace 与自有颜色附件共存语义三端不一致（无样例触及）；已 push 桥仓库 48e3603..604a586；报告 task-8-report.md |
| 2026-08-22 | DX12 后端 Task 9 ImGui overlay 接入 | 实现 | a039d0f：ImGuiDirectx12Window.{hpp,cpp} 镜像 ImGuiVulkanWindow 模式（setRenderer 注入+init/newFrame/render/destroy，DX 头 #if ENABLE_DX12 保护 GL-only 构建安全）；init=GetDXImGuiInitInfo 桥取句柄→CreateContext→ImGui_ImplGlfw_InitForOther→ImGui_ImplDX12_Init(device,2,R8G8B8A8_UNORM,共享SRV堆,堆起始CPU/GPU 句柄)；**字体纹理用共享 CBV_SRV_UAV 堆槽 0**（Task 7 预留，核实 imgui_impl_dx12 Init 本就接受 caller 堆+预分配句柄，未建独立堆；resetRenderState 只清槽≥1 不触字体描述符）；DXRenderer::renderImGuiDrawData override=present 前录制（flushOmTargets 落地 pending 清屏→残留离屏 EndPass→activateWindowTargets(false) OM 绑回窗口不清屏→SetDescriptorHeaps 双堆→RenderDrawData→applyViewport 重放负高度视口同 VK 尾部理由）；AppHost.cpp DX12 分支补完整形态（补 setPipeline(nullptr)+imgui window 构建）；src/CMakeLists ENABLE_DX12 块加 imgui/DX12 include 路径（子 CMake include_directories 不传播，VK Task2 同款修正）；3party/imgui CMake 的 ENABLE_DX12 gate 核实已存在无需改 | **验证 pending**（用户指令禁构建）：Windows MSVC 编译+BlinnPhong overlay 截图冒烟待真机；披露=Samples glob 挂 ENABLE_OPENGL 下致 DX12 ON+OpenGL OFF 组合链接失败（ImGuiVulkanWindow 同款既有问题非本次引入）、imgui 只设 VB 槽 0 与 prepareDraw 空槽不解绑的既有通病理论残留（样例均 binding0 风险极低）；已 push 桥仓库 604a586..a039d0f；报告 task-9-report.md |
| 2026-08-23 | DX12 Task10a HLSL 镜像树：Base 余样例 + Light 全组 + UBO ring 扩容 | 实现 | 4 提交 a2dd41f(ring 32→256)→61980c6(Base 组 16 HLSL)→915139d(LightSource 组 16)→6b52bef(Advanced 光照组 24)，共 56 个直译 res/GL 同名 GLSL；CameraApp 复用 T7 Base/Cube、CameraBaseApp 无 shader（计划"公共相机 hlsli"前提不成立）；语义表/采样器裁决/死接口不搬运等详见报告 | **验证 pending**（禁构建）：dxc 编译+逐样例冒烟待真机；必要偏差=ParallaxMap 发散 while 内 SampleLevel(lod0)（DXIL 禁隐式梯度）、Bloom MRT 双 SV_Target 首验；疑虑=指派范围漏列 SimpleLightDiffuse 致镜像树缺口待总控定夺；已 push 桥仓库 a039d0f..6b52bef；报告 task-10a-report.md |
| 2026-08-23 | DX12 Task10b HLSL 镜像树：Diffuse 补漏 + Shadow/PBR/Advanced 杂项/Geometry/Instance/Model 组 | 实现 | 7 提交 996af4c(Diffuse 4)→5582abe(Shadow 组 14+根签名静态比较采样器 s9=COMPARISON_NEAREST+ClampToBorder白+LESS_EQUAL 配套 DXPipeline/_samplers.hlsli)→10c02e5(PBR 组 30)→3e0c216(杂项组 26)→5cb07b1(include 层级修正)→3384546(Geometry 组 11，GS 直译 gs_6_0：point/triangle 输入修饰符、TriangleStream/LineStream、投影移 GS、GS 读 b0 靠 CBV ALL)→f1c4c43(Instance/Model 8，Rock mat4 实例摊平 TEXCOORD3..6 列构造)，共 93 个 HLSL 与 res/GL 源一一对应；Shadow/ShadowMap 用 SampleCmp 硬件比较（指令指定），PointLightShadow 保持手动线性比较对照 VK；Irradiance 浮点循环内 SampleLevel(lod0) 规避 dxc 流控限制 | **验证 pending**（禁构建）：dxc 编译+24 样例冒烟+s9/depth-only PSO/SV_Depth 首验待真机；KNOWN-GAP=AdvancedGLSL gl_PointSize 平台不可表达；疑虑=IBL_Specular Irradiance pass lod0 vs 隐式 lod≈4 近似已留注释开关；已 push 桥仓库 782c678..f1c4c43；报告 task-10b-report.md |
| 2026-08-23 | DX12 后端 MSVC 首建失败修复（2 轮全绿） | 修复 | 8c21fe5(12 文件:ComPtr operator& 劫持致自比较/取址传参双错→addressof+直传、DxFilterOf 匿名ns与头文件同签名二义→定义移 rhi 作用域、臆造枚举 COMPARISON_MIN_MAG_NEAREST_MIP_POINT→MIN_MAG_MIP_POINT、采样器表按下标写越界 UB→紧凑填充、缺 d3dcompiler.h、_uniformBuffer 裸/shared_ptr 错配、_renderTarget 成员漏声明、native() wstring 与 char 字面量、0.0f/0.0f 常量除零 NaN、4 App Viewport narrowing)→b5b171a(waitForGpuIdle 漏网 .Get()) | 构建 rc=0，renderLearn.exe 产出，dx_shaders 159 .cso 全绿；遗留警告 1（C4530 chrono 既有）；未运行样例待冒烟指令；报告 task-fix-msvc-report.md |
| 2026-08-23 | DX12 三样例崩溃修复（Msaa/Defer/SSAO） | 修复 | 4 提交：fe48ff7(Defer/SSAO HLSL 镜像树 14 文件)→2b2076f(blit PSO 零初始化枚举非法致 E_INVALIDARG，显式填默认+InfoQueue 转储/调试层开关基建)→7b77975(MSAA 资源 DENY_SHADER_RESOURCE 与 ALLOW_RENDER_TARGET 组合非法，去掉)→4d9d0b5(init() 漏置 _valid 致 Model 纹理误报失败，计划外发现) | 复测 Msaa/Defer/SSAO/Cube 全 rc=124 且日志零 error；Cube mipmap 警告消失；报告 .superpowers/sdd/2026-08-22-dx12-backend/task-fix-3samples-report.md |
| 2026-08-23 | DX12 后端补 RHI_DUMP_FRAME 帧导出钩子 | 实现 | 998010e：DXRenderer::present() 在 ExecuteCommandLists 之后 Present 之前调 dumpFrame()（对齐 VK 时序）；实现=一次性 READBACK placed buffer（行距 256 对齐，初始 COPY_DEST）+ 专用 transient allocator/cmdlist + PRESENT↔COPY_SOURCE 屏障往返 → CopyTextureRegion(footprint R8G8B8A8) 读回 → 独立小 fence _dumpFence 单值等待（避免干扰共享 fence GetCompletedValue()+1 单调约定）→ Map 写 PPM(P6)；env 检查+_dumpDone+RHI_DUMP_SKIP 全对齐 VK；通道口径核实：VK 为 B8G8R8A8 故其换序读出，DX 为 R8G8B8A8 直接落盘无调换，两端 PPM 一致；行序 top-down 同 VK 无翻转 | 构建 rc=0（仅既有 C4530 警告），未运行程序；报告 task-dumphook-report.md；已 push 桥仓库 4d9d0b5..998010e |

| 时间 | 日志 | 任务类型 | 内容 | 备注 |
| ---- | ---- | -------- | ---- | ---- |
| 2026-08-23 21:58 | task-debug-dark2 | 调试修复 | SkyBox/Defer DX12 变暗根因修复：①mipgen/blit DrawInstanced InstanceCount=0（3 处，943aa35）②描述符堆同帧后绑覆盖先绑→按 draw 快照（f728846）③cubemap TEXTURE2DARRAY 视图误用 Texture2D PSO（f728846）④NVIDIA 隐式 LOD 约定差对齐 s10/s11（8751689..41f7a7b） | Defer 主判据 PASS（5.34<10、2.95%<5%）；SkyBox 替代判据 PASS（通道均值差≤0.24、corr 0.9609）；Rect linkage 为预先存在问题另案；详见 task-debug-dark2-report.md |
| 2026-08-23 | DX12 后端 Windows 主机侧全量打通 | 实现+验证 | spike 证伪 WSL 呈现→裁决改 Windows 主机侧（双副本同步：WSL 源仓库+E:\\ 克隆+vcpkg）；Tasks2-10 全码→MSVC 首建修复链（EnumUtil MSVC 签名/Mat3Inverse/float3提升/8 处编译错/blit PSO/MSAA flag/Defer+SSAO 漏组/init _valid/mip InstanceCount=0/描述符堆同帧覆盖/cubemap 视图/NVIDIA LOD 采样器）→**DX12 46/46 功能全绿**；像素门（vs Vulkan 参照）：静态 SSAO 0.16/PBR_Base 4.62/Defer 5.35/SkyBox 4.39(滤波核解释) PASS，动态 Cube/Diffuse/Shadow 受跨进程动画相位限制（Shadow 最优对齐 8.91，双动画参数本质不可同帧）；GL 在 Windows 为离群参照（GL-vs-VK 基线同样超阈，SSAO R=G 通道异常待查） | 已知项：AdvancedGLSL gl_PointSize 平台 GAP；Rect 描述符 linkage 预先问题（渲染正常，另案）；工具 tools/frame_compare.py |
| 2026-08-24 | Rect 三后端 Y 镜像根因修复 | 修复 | 2bc80bf+fef0c12：DX swapchain 视口去除负高度（D3D NDC y-up 无需 VK 式翻转，直绘样例曾整体镜像）；离屏 RT 改负高度对齐 VK 行序；内部 blit VS uv.y 修正（每级 mip 曾垂直翻转） | 真机复测 Rect/Triangle/SSAO 等 9 例 ≤0.4%>20 占比；brief"Triangle DX↔VK PASS"被实测推翻；GL dumpFrame 未翻行序+Defer/Bloom 既有失配已记录 task-fix-rect-report.md |
| 2026-08-23 | DX12 终验：视口朝向根因修复+像素门达标 | 验证 | 真机发现 DX12 照搬 VK 负高度视口（D3D NDC 本就 y-up）致全部直绘 swapchain 样例 Y 镜像——GL dump 行序未翻恰好抵消掩盖了早期比对；修复（2bc80bf+fef0c12，含 blit uv.y 同源错）后像素门 vs Vulkan 参照：Triangle 0.17/Rect 0.16/SSAO 0.15/PBR_Base 0.26/SkyBox 0.77/Shadow 2.85/Defer 5.42 全 PASS（>20 占比 ≤3.9%）；Cube 6.72(9%)、SimpleLight_Diffuse 2.09(5.15%) 为残余级微差（mip 滤波核/动画相位类，均值远低于阈值）记 KNOWN-RESIDUAL；功能扫描维持 46/46；Linux GL 46/46 零回归 | 已知项汇总：AdvancedGLSL gl_PointSize 平台 GAP；Cube/Diffuse 残余；VK Linux 5 例环境性失败（既有） |
| 2026-08-24 | DX12 后端终审（review-7a67fc4..261bb7a） | 评审 | 终审报告 .superpowers/sdd/2026-08-22-dx12-backend/final-review-report.md：Critical 0 / Important 2 / Minor 6，总裁决**可交付（附条件）** | F1=OMSetStencilRef 全后端缺失（TemplateTest ref 恒 0，VKPipeline.cpp:276 对照）；F2=color cube 换面 EndPass 读已变更 _face 致旧面滞留 RT+无效屏障（靠 decay/promotion 洗白）；Minor：bind 窗>1024 回绕、UBO ring 同帧回绕、resize 死代码+_backBufferBound 隐患、_clearColors 地址键、VB stride 缓存、VB/IB update 无越界校验；生命周期/同步/构建隔离查无虞 |
| 2026-08-24 | DX12 终审 F1/F2 修复 | 修复 | b8c6ca5(F1)=prepareDraw 补 OMSetStencilRef 每 draw 下发+DXPipeline::stencilRef() 访问器+_stencilRef 移出 stateHash（VK/GL 核查无缺失未动）；c51e8f3(F2)=DXRenderTarget BeginPass 快照 _boundFace 等、EndPass 按快照恢复 cube 面（消除无效屏障+旧面滞留 RT） | Windows MSVC 构建 rc=0；dx12 复测 TemplateTest/SkyBox/PBR_IBL_Irradiance 全 rc=124 零 error；fix 记录已追加终审报告 |
| 2026-08-24 | DX12 mipgen 升级精确盒平均 + 复测根因转移 | 修复+验证 | dcc743b(mipdown(.frag/_array.frag).hlsl Gather 角点等权盒平均+IDXBlitContext 扩 MipdownPsoFor/Array+DXRenderer PSO 缓存参数化)→5cc3705(DXTexture2D/3D mipgen 切换)；对齐 vkCmdBlitImage linear 2:1 盒式语义，RT↔RT 颜色拷贝保留线性 Sample | 非回归全 PASS：SkyBox 0.76/0.17%、SimpleTexture 0.19/0.09%、PBR_Base 0.26/0.11%、Triangle 0.17/0.07%、Rect 0.16/0.07%（DX@f30 vs VK 参照，0 error）；门内两项根因转移：Cube 残差=墙钟动画相位（AppHost curTime 墙钟累加+DX 首帧 PSO 预热 ~0.45s，skip 扫描 2.67%→10.85% 单调 +0.39%/帧，相位匹配点 DX@f4 vs VK@f30=mean1.99/2.67% PASS），SimpleLight_Diffuse 无纹理无 mip 路径（核切换前后逐位不变）实为跨后端光照着色分歧（VKvsGL 13.2%/GLvsDX 13.8%，亮区朝向互异，另案）；详见 task-mipdown-report.md |
