file(GLOB NATIVE_CPP_FILES "*.cpp")
file(GLOB NATIVE_HPP_FILES "*.hpp")
set(ALL_CPP_FILES ${ALL_CPP_FILES} ${NATIVE_CPP_FILES} CACHE STRING "all source files" FORCE)
set(ALL_HPP_FILES ${ALL_HPP_FILES} ${NATIVE_HPP_FILES} CACHE STRING "all header files" FORCE)

if(ENABLE_OPENGL)
    file(GLOB NATIVE_OPENGL_CPP_FILES "GL/*.cpp")
    file(GLOB NATIVE_OPENGL_HPP_FILES "GL/*.hpp")
    set(ALL_CPP_FILES ${ALL_CPP_FILES} ${NATIVE_OPENGL_CPP_FILES} CACHE STRING "all source files" FORCE)
    set(ALL_HPP_FILES ${ALL_HPP_FILES} ${NATIVE_OPENGL_HPP_FILES} CACHE STRING "all header files" FORCE)
endif()

if(ENABLE_DX11)
    file(GLOB NATIVE_DX11_CPP_FILES "DX11/*.cpp")
    file(GLOB NATIVE_DX11_HPP_FILES "DX11/*.hpp")
    set(ALL_CPP_FILES ${ALL_CPP_FILES} ${NATIVE_DX11_CPP_FILES} CACHE STRING "all source files" FORCE)
    set(ALL_HPP_FILES ${ALL_HPP_FILES} ${NATIVE_DX11_HPP_FILES} CACHE STRING "all header files" FORCE)
endif()
