file(GLOB UTILS_CPP_FILES "*.cpp")
file(GLOB UTILS_HPP_FILES "*.hpp")

if(ENABLE_OPENGL)
    file(GLOB UTILS_OPENGL_CPP_FILES "GL/*.cpp")
    file(GLOB UTILS_OPENGL_HPP_FILES "GL/*.hpp")
    set(ALL_CPP_FILES ${ALL_CPP_FILES} ${UTILS_OPENGL_CPP_FILES} CACHE STRING "all source files" FORCE)
    set(ALL_HPP_FILES ${ALL_HPP_FILES} ${UTILS_OPENGL_HPP_FILES} CACHE STRING "all header files" FORCE)
endif()


set(ALL_CPP_FILES ${ALL_CPP_FILES} ${UTILS_CPP_FILES} CACHE STRING "all source files" FORCE)
set(ALL_HPP_FILES ${ALL_HPP_FILES} ${UTILS_HPP_FILES} CACHE STRING "all header files" FORCE)
